interface:
  display_name: "AreaMatrix Codex OS"
  short_description: "Run start, validation, repair, closeout, and ops flows"
  default_prompt: "Use $areamatrix-codex-os to run start-flow, validation, repair-plan, close-flow, and ops-flow for this AreaMatrix Codex OS task safely."
policy:
  allow_implicit_invocation: true
