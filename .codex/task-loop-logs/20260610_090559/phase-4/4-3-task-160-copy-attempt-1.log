一、执行结论

已完成 `4-3/task-160`。

二、执行范围

单任务，仅在 `apps/**` 内改动：

- `apps/macos/AreaMatrix/Models/RepositorySettingsConfigModel.swift`
- `apps/macos/AreaMatrix/Views/Settings/RepositorySettingsConfigSection.swift`
- `apps/macos/AreaMatrix/Views/Settings/RepositorySettingsPane.swift`
- `apps/macos/AreaMatrixTests/RepositorySettingsConfigPageFeatureTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况

- 核对清单：已按 `S4-X-08 + C4-20` 接入，不实现 C4-17、watcher、重连、诊断导出或相邻能力。
- 完成标准：新增 `Repository config` 区，基于真实 `RepoConfigSnapshot` 展示/编辑 C4-20 配置，并通过 `CoreConfigurationUpdating.updateConfig` 保存完整 config payload。
- 平台能力约束：保存控件会根据 `PlatformCapabilities.securityBookmark` 禁用并解释不可用原因。
- Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

代码拆成独立 model/view/test，避免把既有 `RepositorySettingsPane.swift` 推过 500 行。保存失败走 `CoreErrorMapping`，成功/失败都有可访问反馈；没有新增依赖，没有 mock-only 生产路径，没有硬编码通过态。测试覆盖成功保存 payload 和失败错误映射。

五、验证情况

- `./dev check task 4-3/task-160`：通过。包含 prompt doctor OK、`git diff --check` OK、macOS `xcodebuild build` succeeded。
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build-for-testing CODE_SIGNING_ALLOWED=NO`：通过，新增测试 target 可编译。
- 失败/无法运行：无。Xcode 输出了 CoreSimulatorService 连接警告，但 build 和 test build 均成功。

六、风险与后续

剩余风险：未做真实窗口点击冒烟；本 task validation 未要求 UI smoke。工作区中仍有既有 dirty 文件 `.codex/task-loop-runs/20260610_090559/summary.json` 和 `tasks/prompts/_shared/progress.json`，我没有把它们作为本 task 改动处理。

建议下一个任务：`4-3/task-161`，做 `S4-X-08` 页面集成验收，检查 C4-17 + C4-20 整页闭环。