一、执行结论

未完成。

C2-03 Core 范围本身仍然通过，但本任务 manifest 指定的唯一验证命令 `./dev check all` 仍失败；失败点在 [AreaMatrixPerfTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/AreaMatrixPerfTests.swift:181)，属于本任务明确禁止触碰的 `apps/**`，所以我没有越界修改。

二、执行范围

- 单任务：`4-1/task-13`
- 本轮修改文件：无
- Forbidden Touches：未触碰 `apps/**`
- 当前工作树中 `core/src/**`、`core/area_matrix.udl`、`core/tests/**`、`apps/**` 无本轮 diff

三、完成情况

- 空态、非法输入、权限、IO/DB 错误和错误码映射：Core 专项测试通过。
- 失败不留下半成品：Core saved-search 事务与失败恢复测试通过。
- 隐私/AI/远程调用：C2-03 不涉及远程 AI，相关失败测试通过。
- 不吞错或静默降级：Core 错误路径专项测试通过。
- 完成标准：Core 侧可证明通过；整任务因指定验证失败不能判定完成。

四、验证情况

已运行：

```bash
cargo test -p area_matrix_core saved_search -- --nocapture
./dev check all
```

结果：

- `cargo test -p area_matrix_core saved_search -- --nocapture`：通过，`saved_search_contract_api` 5 个、`saved_search_failure_recovery` 11 个、`saved_search_implementation` 9 个测试通过。
- `./dev check all`：失败，退出码 1。
- 失败细节：`xcodebuild test` 受本地 sandbox/testmanagerd 阻断后 fallback 到 direct XCTest；`AreaMatrixTests.xctest` 共 484 个测试，1 个失败。
- 失败测试：`AreaMatrixPerfTests.testApplicationLaunchToFirstScreenBaselineUnderStage1Threshold`
- 失败错误：`unexpectedFirstScreenRoute`

五、风险与后续

剩余 blocker 不在 C2-03 Core 允许范围内，而在 macOS app 测试路径。初步定位是 hostless fallback 中 `NSHostingView(rootView: MainWindow(model: model))` 触发 `MainWindow.task { await model.bootstrapIfNeeded() }`，和测试里随后显式 `await model.bootstrapIfNeeded()` 之间存在 route bootstrap 竞态，导致检查时 `model.route` 还不是 `.mainEmpty`。

建议下一个动作：单独开启或授权一个 app-scope repair，允许修改 `apps/macos/AreaMatrixTests/AreaMatrixPerfTests.swift` 或相关 app bootstrap 代码后，再重跑 `./dev check all`。当前任务边界下不能合法完成这个修复。