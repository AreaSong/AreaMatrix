一、执行结论

已完成 `2-1/task-17`。

二、执行范围

修改了 2 个文件：

- [InitDoneEmptyRepositoryTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/InitDoneEmptyRepositoryTests.swift:58)：新增 S1-07 `Open Repository` 先进入 `mainLoading`、Core 打开完成后再进入 ready 的测试证据。
- [C1-03-adopt-existing-repo.md](/Users/as/Ai-Project/project/AreaMatrix/docs/core/capability-specs/stage-1-mvp/C1-03-adopt-existing-repo.md:8)：修复 Exact Docs 与 control map 漂移，补齐 `S1-07 init-done`、`load_config`、`list_tree_json` 合同。

三、完成情况

- page-feature 依赖：`2-1/task-15`、`2-1/task-16` 的 verify log 均为 `VERIFY_RESULT: PASS`。
- 页面闭环：S1-07 展示 repoPath、create/adopt 摘要、Finder 非阻断错误、Open Repository inline retry 错误；Open Repository 经 CoreBridge 打到 `load_config` + `list_tree_json`。
- mock / fixture：生产路径未新增 mock；测试 fixture 仅用于隔离异步状态断言。
- control map 外功能：未新增。
- Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 无输出。

四、工程质量

结构清晰，改动集中在测试证据和直接相关文档漂移修复；没有新增生产抽象、依赖、硬编码通过态或一次性脚本。测试文件 357 行，仍低于 500 行阈值。

五、验证情况

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：编译完成，但本地沙箱阻断 `com.apple.testmanagerd.control`，退出 65。
- `bash scripts/check-macos-tests.sh`：通过；按仓库规则 fallback 到 `xcrun xctest`，62 tests，0 failures。
- `python3 tasks/prompts/_shared/prompt_pipeline.py audit --pages`：通过，S1-07 Missing/Extra 均为 None。
- `git diff --check -- ...`：通过。
- `swiftformat` / `swiftlint`：未运行，当前环境 `command -v` 无输出，工具不可用。

六、风险与后续

剩余风险是标准 `xcodebuild test` 在当前沙箱环境仍无法完成 testmanagerd 通信；非沙箱 Xcode/CI 环境需要补跑同一条命令。建议下一个任务继续 `2-1/task-18` first-launch integration verify。

