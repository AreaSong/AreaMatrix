一、验收结论

- 不通过

二、验收范围

- 单任务：`2-1/task-17`
- 对应文件路径：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`、`core/area_matrix.udl`
- Manifest 禁止范围：`core/src/**`

三、完成度摘要

- 已覆盖项数：10
- 未覆盖项数：0
- 通过项数：8
- 不通过项数：2
- 阻塞项数：1

四、逐项验收结果

- page-feature 覆盖：通过。S1-07 在 control map 中仅声明 `C1-02`、`C1-03`，对应 `2-1/task-15`、`2-1/task-16`。
- 页面闭环：通过。`Open Repository` 先进入 `.mainLoading`，再经真实 `CoreBridge` 的 `loadConfig` / `listTreeJson` 进入 `.mainEmpty` 或 `.mainList`。
- 错误态：通过。打开失败返回完成页并显示 inline retry error；Finder 失败写入 toast 并触发 VoiceOver announcement。
- mock / fixture / 硬编码：通过。生产路径走真实 CoreBridge；fixture 仅在测试内。
- control map 外能力：通过。未发现 S1-07 新增 `C1-02` / `C1-03` 之外能力。
- Expected New Paths：通过。三个期望路径均存在。
- Forbidden Touches：通过。`git status -- core/src` 无输出。
- 任务边界：不通过。当前工作树存在 task-17 相关的 `docs/**`、`tasks/prompts/**`、`.codex/**` 改动，超出 manifest 的 Expected New Paths；因此不能判定本 task 范围干净完成。

五、工程质量验收

- 代码结构与逻辑：通过。关键手写 Swift 文件均 ≤500 行，路由与 CoreBridge 开仓逻辑拆分清晰。
- 注释 / rustdoc / 文档同步：不通过。存在 out-of-scope 的文档 / prompt / manifest 改动，且本轮未运行 prompt doctor/status 来证明这些变更可接受。
- 错误处理与边界处理：通过。打开失败、Finder 失败、VoiceOver 宣告均有测试证据。
- 测试与验证覆盖：通过。`bash scripts/check-macos-tests.sh` 通过，`xcodebuild build` 通过。
- 占位、硬编码、mock-only、一次性实现检查：通过。未发现生产路径 mock-only 闭环。

六、阻塞项

- P1：任务范围漂移。当前修改包含 `docs/core/capability-specs/stage-1-mvp/C1-03-adopt-existing-repo.md`、`tasks/prompts/_shared/manifests/phase-2.md`、`tasks/prompts/phase-2/2-1-first-launch-main/task-17-s1-07-page-integration-verify.md`、`tasks/prompts/_shared/copy-ready/phase-2/2-1-task-17.md`、`tasks/prompts/_shared/verify-ready/phase-2/2-1-task-17.md`、`.codex/task-loop-*` 等，超出 manifest 允许的 macOS app / tests / xcodeproj 路径。按 Expected New Paths 边界和 validation-driver，这些变更需要单独的 prompt/governance 验证，当前不能直接判 PASS。

七、验证情况

- 已运行并通过：`bash scripts/check-macos-tests.sh`。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断后，脚本按规则 fallback 到 `xcrun xctest`；62 tests，0 failures。
- 已运行并通过：`xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/areamatrix-xcode-build-verify-task17 build CODE_SIGNING_ALLOWED=NO`。
- 已运行并通过：`git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj`。
- 已运行并通过：`git status --short -- core/src`，无输出。
- 未运行：`swiftlint --strict`、`swiftformat --lint .`，当前环境未找到 `swiftlint` / `swiftformat`。

八、最终判定说明

- S1-07 功能闭环本身已有通过证据，但当前仓库状态存在 task 范围漂移和对应验证缺口。本次执行尚未达到验收标准，不能视为完成。

VERIFY_RESULT: FAIL

