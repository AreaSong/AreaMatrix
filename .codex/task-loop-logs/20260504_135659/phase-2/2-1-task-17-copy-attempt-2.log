一、执行结论

- 已完成。
- 说明：原始 `xcodebuild test` 在当前 Codex sandbox 里仍被 `com.apple.testmanagerd.control` 阻断；按仓库 `docs/development/testing.md` 定义的本地等价门禁，已通过 `scripts/check-macos-tests.sh` fallback 直接执行同一 XCTest bundle，62 个测试全部通过。

二、执行范围

- 单任务：`2-1/task-17`
- 本轮实际修改：
  - [InitDoneEmptyRepositoryTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/InitDoneEmptyRepositoryTests.swift:58)
- 改动内容：把 S1-07 “Open Repository 先进入 main-loading，Core 打开完成后进入 repositoryReady” 的测试改成确定性 continuation 控制，移除固定 sleep 等待。

三、完成情况

- page-feature task 覆盖：通过，S1-07 仍只覆盖 `2-1/task-15` / `2-1/task-16`，对应 C1-02 / C1-03。
- 页面入口、退出、错误态、CoreBridge 闭环：通过，`initializationDone -> mainLoading -> repositoryReady` 有测试证据，失败会回完成页并保留 inline error。
- mock / fixture / 硬编码状态：通过，生产路径未新增 mock-only；fixture 仅在测试中。
- control map 外功能：未新增。
- Forbidden Touches：未触碰，`git status --short -- core/src` 无输出。

四、工程质量

- 结构清晰：测试 helper 现在由 actor 明确控制 start/finish，不依赖时间窗口。
- 错误与边界：保留打开失败回完成页、Finder 非阻断失败、真实 `load_config` + `list_tree_json` 测试覆盖。
- 注释 / rustdoc：本轮未新增 public Rust API，无 rustdoc 需求。
- 依赖：未新增依赖。

五、验证情况

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 失败，code 65。
  - 原因仍是当前 sandbox 无法访问 `com.apple.testmanagerd.control`，xcresult：`apps/macos/DerivedData/AreaMatrix/Logs/Test/Test-AreaMatrix-2026.05.04_14-58-50-+0800.xcresult`
- `KEEP_DERIVED_DATA=1 DERIVED_DATA_PATH=apps/macos/DerivedData/AreaMatrix bash scripts/check-macos-tests.sh`
  - 通过；先确认 `xcodebuild test` 是 testmanagerd sandbox block，再用 `xcrun xctest` 执行 `AreaMatrixTests.xctest`。
  - 结果：62 tests, 0 failures。
- `git diff --check`
  - 通过。
- `swiftformat` / `swiftlint`
  - 未运行：当前环境未安装这两个命令。

六、风险与后续

- 剩余风险：当前 sandbox 不能证明原始 `xcodebuild test` 本身成功，只能按仓库定义的本地 fallback 证明 XCTest 断言成功；CI 或非 sandbox macOS 环境仍应跑原始 `xcodebuild test`。
- 建议下一个任务：`2-1/task-18`。

