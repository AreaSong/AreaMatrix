一、执行结论

已完成 `2-1/task-17` repair retry。上次两个 P1 阻塞项已修复：Finder 失败现在有 toast + VoiceOver announcement 证据；`AreaMatrixShellTests.swift` 已降到 483 行，满足单文件 ≤500 行门禁。

二、执行范围

单任务：`2-1/task-17`

本轮直接修改：
- `apps/macos/AreaMatrix/App/AreaMatrixApp.swift`
- `apps/macos/AreaMatrix/Models/AppShellModel.swift`
- `apps/macos/AreaMatrix/Models/OnboardingInitializationProgress.swift`
- `apps/macos/AreaMatrix/Views/Onboarding/InitializingStepView.swift`
- `apps/macos/AreaMatrixTests/AreaMatrixShellTests.swift`
- `apps/macos/AreaMatrixTests/InitDoneEmptyRepositoryTests.swift`
- `apps/macos/AreaMatrixTests/CoreBridgeRepositoryTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况

- page-feature 覆盖：通过，S1-07 仍只覆盖 `C1-02` / `C1-03`，对应 `2-1/task-15` / `2-1/task-16`。
- 页面闭环：通过，`Open Repository` 先进入 `.mainLoading`，再按真实 `load_config` / `list_tree_json` 结果进入 `.mainEmpty` 或 `.mainList`。
- Finder 失败态：通过，失败写入 `toastMessage`，并经 `AccessibilityAnnouncing` 发送 VoiceOver announcement；测试已覆盖。
- mock / fixture / 硬编码：通过，生产路径仍走真实 `CoreBridge`；fixture 仅在测试内。
- control map 外能力：未新增。
- Forbidden Touches：未触碰，`git diff --name-only -- core/src` 无输出。

四、工程质量

- 测试拆分后相关手写 Swift 文件均 ≤500 行：`AppShellModel.swift` 499 行，`AreaMatrixShellTests.swift` 483 行。
- 新增 `AccessibilityAnnouncing` 适配器，生产实现使用 `NSAccessibility.announcementRequested`，测试用注入 recorder 验证。
- 未改 Rust public API、UDL 或 `core/src/**`，无 rustdoc/doc sync 需求。
- 无新增依赖。

五、验证情况

已运行并通过：
- `bash scripts/check-macos-tests.sh`：`xcodebuild test` 被本地 `testmanagerd` sandbox 阻断后，脚本按设计 fallback 到 `xcrun xctest`，62 tests / 0 failures。
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/areamatrix-xcode-build-task17 build CODE_SIGNING_ALLOWED=NO`：BUILD SUCCEEDED。
- `git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：通过。
- `git diff --name-only -- core/src`：无输出。
- 行数检查：相关手写 Swift 文件均未超过 500 行。

未运行：
- `swiftlint --strict`、`swiftformat --lint .`：当前环境 `command -v swiftlint` / `command -v swiftformat` 无输出。

六、风险与后续

剩余风险：未做真实 VoiceOver 手工朗读验证，但代码路径和注入测试已证明 announcement 被触发。建议下一个任务进入 `2-1/task-18` 或重新跑 verify-ready 验收 `2-1/task-17`。

