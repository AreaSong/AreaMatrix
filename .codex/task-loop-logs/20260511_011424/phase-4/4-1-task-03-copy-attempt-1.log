一、执行结论

已完成 `4-1/task-03: C2-01 failure-edge`。

二、执行范围

修改文件：
- `core/src/search.rs`
- `core/src/search/engine.rs`
- `core/src/search/parser.rs`
- `core/src/search/validation.rs`
- `core/tests/search_query_files_failure_recovery.rs`

未触碰 `apps/**`。进入任务前已有的 `.codex/task-loop-runs/20260511_011424/summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是既有脏改，我没有修改它们。

三、完成情况

核对清单：
- 空态：新增空 repo 搜索返回空页测试。
- 非法输入：新增 filter、tag、时间范围、pagination 显式 `Config` 错误，不再静默降级。
- 权限 / IO / DB：新增 metadata 损坏、DB 权限、目录权限相关失败证据。
- 错误码映射：验证 C2-01 仍按 `Db` / `Config` / `InvalidPath` 合同暴露。
- 半成品：C2-01 是只读搜索；测试断言不写 DB、不写 `change_log`、不写 staging/generated、不改用户文件。
- 隐私 / AI / 远程调用：测试确认不创建 AI、remote、secrets 相关状态。
- 吞错：非法请求改为结构化错误；query parse error 保持 diagnostics，不伪装成普通空结果。

四、工程质量

代码结构保持在 `core/src/search` 内：新增 `validation.rs` 做请求预检，`engine.rs` 只串接验证，`parser.rs` 只修正 quoted/escaped colon 的 token 语义。没有新增依赖、mock-only wiring、硬编码通过态或一次性脚本。

五、验证情况

已运行并通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo test --test search_query_files_contract_api --test search_query_files_implementation --test search_query_files_failure_recovery`
- `git diff --check`
- `./dev check all`

`./dev check all` 完成且退出码为 0；其中 `xcodebuild test` 被本地 sandbox 的 `testmanagerd` 通信限制阻断后，脚本按仓库策略复用已构建 XCTest bundle 走 `xcrun xctest`，随后 SwiftFormat 与 SwiftLint 均通过。

六、风险与后续

剩余风险：无本任务阻断项。C2-01 的 UI 消费闭环仍应由后续 `4-1/task-04` / `4-1/task-05` 覆盖。

建议下一个任务：继续 `4-1/task-04`，做 C2-01 validation。  
