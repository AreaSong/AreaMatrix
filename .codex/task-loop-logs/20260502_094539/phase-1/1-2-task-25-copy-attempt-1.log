一、执行结论

已完成：`1-2/task-25` 的 C1-10 `resolve-name-conflict` contract-api 范围已落地。

二、执行范围

- 单任务：`1-2/task-25`
- 修改文件：
  - [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:238)：补齐 C1-10 在 `import_file` / `rename_file` 公共 Rust API 上的合同意图、错误码和副作用边界。
  - [core/tests/resolve_name_conflict_contract_api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/resolve_name_conflict_contract_api.rs:22)：新增 contract-api 测试，交叉锁定 capability spec、control map、Core API 文档、UDL、错误码和 Rust 入口说明。
- 未触碰 `apps/**`。
- `core/area_matrix.udl` 未修改；现有 UDL 已包含本任务需要的 `import_file`、`rename_file`、`override_filename`、`DuplicateStrategy`、`Conflict` 合同面，并由新增测试验证。

三、完成情况

- 核对清单：5/5 完成。
- 完成标准：2/2 完成。
- Forbidden Touches：未触碰。
- 说明：本任务只完成 contract-api 证据，不实现 C1-10 运行时自动改名逻辑；该实现属于后续 `1-2/task-26`。

四、工程质量

- 结构清晰：只补 Rust API rustdoc 与 contract 测试，没有引入新抽象或依赖。
- 注释 / rustdoc：新增 public API 合同说明，覆盖输入、输出、错误、Replace 边界和副作用。
- 错误与边界：测试覆盖 `Conflict` / `InvalidPath` / `PermissionDenied` / `Io` / `Db` 合同可见性。
- 无占位、硬编码通过态或 mock-only wiring；`rename_file` 原有未实现状态未在本任务扩大或伪装为实现。

五、验证情况

- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace resolve_name_conflict`：通过，6 个定向测试通过。
- 额外补跑 `cd core && cargo test --workspace`：通过。
- `git diff --check`：通过。
- 无无法运行项。首次 `fmt --check` 发现新增测试格式问题，已用 `cargo fmt --all` 修正后复跑通过。

六、风险与后续

剩余风险：C1-10 真实运行时行为仍未实现，例如同名不同内容自动落到 `name_1.ext`、`rename_file` 实际更新 FS/DB/change_log。建议下一个任务执行 `1-2/task-26`。