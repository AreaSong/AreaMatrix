**一、执行结论**

已完成 `1-2/task-17`。

**二、执行范围**

单任务：C1-08 `import-index-file` failure-recovery。

修改文件：

- [core/src/db/import.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/import.rs:90)
- [core/src/db/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/mod.rs:15)
- [core/src/storage/import.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/storage/import.rs:129)
- [core/tests/import_index_file_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_failure_recovery.rs:114)

未触碰 `apps/**`。`core/area_matrix.udl` 无需改动，API 合同未变。

**三、完成情况**

- 只处理 C1-08 failure-recovery：完成。
- 已读取 Exact Docs，并按文档 SSOT 实现：完成。
- 未实现相邻 C1 能力或 UI 页面：完成。
- 覆盖失败、回滚、重复执行、权限错误路径：完成。
- 未删除、覆盖、移动未确认的用户文件：完成。

核心变化：Indexed import 现在用单个 SQLite transaction 同时写入 active `files` row 和 `change_log.imported`，不再先提交 staging row 再 promote，降低 DB 失败或中断时留下中间 metadata 的风险。

**四、工程质量**

结构保持在现有 `storage/import` 与 `db/import` 模式内，没有新增外部依赖、mock-only、硬编码通过态或一次性脚本。新增测试使用 tempdir 和 SQLite trigger 证明 DB 回滚、可重试、重复执行、iCloud marker、metadata internal source、权限拒绝都不会产生最终文件副本、staging 残留或脏 DB 行。

当前工作区仍有执行前已存在的 `tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260502_094539/` 变更；我没有把它们纳入本任务改动。

**五、验证情况**

已运行，全部通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace import_index_file`
- 额外补跑：`cd core && cargo test --workspace`
- 额外补跑：`git diff --check`

无法运行项：无。

**六、风险与后续**

剩余风险：C1-08 已覆盖 Core 层 indexed failure-recovery；跨重启外部路径授权/bookmark 仍按文档属于 macOS app 层延后范围，不在本任务实现。

建议下一个任务：继续执行 `1-2/task-18`。  
回滚方式：回退上述 3 个 Core 源码文件改动，并删除新增的 `core/tests/import_index_file_failure_recovery.rs`；无 DB migration 或用户文件变更需要回滚。

