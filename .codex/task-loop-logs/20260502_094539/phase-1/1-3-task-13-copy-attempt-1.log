**一、执行结论**
已完成 `1-3/task-13` 的 C1-14 `read-write-note` contract-api 范围。未实现真实读写逻辑，避免越界到 `1-3/task-14`。

**二、执行范围**
修改文件：
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:390)：补齐 `read_note` / `write_note` 的 public rustdoc，明确输入、输出、错误码、副作用和失败一致性边界。
- [core/tests/read_write_note_contract_api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/read_write_note_contract_api.rs:1)：新增 C1-14 contract-api 测试，交叉锁定 capability spec、control map、Core API docs、error codes、UDL 和 Rust API 意图。

未触碰 `apps/**`。工作区仍有开始前已存在的脏文件：`.codex/task-loop-runs/20260502_094539/summary.json`、`tasks/prompts/_shared/progress.json`。

**三、完成情况**
- 只处理 C1-14 contract-api：通过。
- Exact Docs 已读取并作为 SSOT：通过。
- 未实现相邻 C1 能力或 UI 页面：通过。
- Core API / UDL 合同意图校准：通过，UDL 现有签名已匹配并由测试锁定。
- 输入、输出、错误码、副作用边界：通过，已写入 rustdoc 并由 contract test 覆盖。
- Forbidden Touches：未触碰。

**四、工程质量**
结构清晰，沿用现有 `*_contract_api.rs` 测试模式，没有新增依赖、mock-only wiring 或一次性脚本。`read_note` / `write_note` 仍返回既有 `not_implemented()`，这是后续 implementation task 的范围，不作为本次 contract-api 交付内容扩展。

**五、验证情况**
全部通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace read_write_note`：3 个 C1-14 contract tests 通过
- 额外补跑 `cd core && cargo test --workspace`
- 额外补跑 `git diff --check`

**六、风险与后续**
剩余风险：真实 `notes` upsert、sidecar `.md` 写入、失败回滚和 `edited_note` change log 还未实现，属于 `1-3/task-14` / `task-15` 范围。建议下一个任务执行 `1-3/task-14`。  

