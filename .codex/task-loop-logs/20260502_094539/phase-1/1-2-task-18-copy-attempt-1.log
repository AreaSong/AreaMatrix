一、执行结论

已完成 `1-2/task-18`。

二、执行范围

单任务：C1-08 `import-index-file` validation。

修改文件：
- [import_index_file_validation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_validation.rs:1)

仅新增 `core/tests/**`，未触碰 `apps/**`。当前工作区另外已有 `.codex/task-loop-runs/20260502_094539/summary.json` 和 `tasks/prompts/_shared/progress.json` 修改，执行前已存在，我未改动它们。

三、完成情况

核对清单全部完成：
- 只处理 C1-08 validation 范围。
- 已读取 manifest Exact Docs，并以 C1-08 / Core API / control map / testing 文档为 SSOT。
- 未实现相邻 C1 能力或 UI 页面。
- 新增 Rust integration validation tests。
- 覆盖成功路径和关键错误路径。

新增测试覆盖：
- Indexed 成功导入不复制、不移动源文件，不创建 repo 最终副本，并写入 `files` / `change_log`。
- 缺失源文件返回 `FileNotFound` 且无 DB / staging 副作用。
- `.areamatrix` 内部源路径和 metadata 目标路径返回 `InvalidPath`。
- 重复 hash 返回 `DuplicateFile`，保留源文件和既有状态。
- 删除外部 indexed 源文件后 `list_files` 返回 `FileNotFound`。

四、工程质量

代码是 task-scoped integration tests，没有新增 public API、依赖、mock-only 分支或硬编码通过态。测试使用真实 tempdir、SQLite、文件系统和 change_log 断言，覆盖用户文件安全、DB 一致性、staging 清理和错误路径。

五、验证情况

已运行并通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace import_index_file`：C1-08 相关 23 个测试通过，其中新增 validation 6 个通过。
- `cd core && cargo test --workspace`
- `git diff --check`

失败：无。  
无法运行：无。

六、风险与后续

剩余风险：未做 macOS UI 消费验证，按本 task 边界属于后续 `2-*` 页面功能 / integration verify 范围。

建议下一个任务：继续 `1-2/task-19`，按 manifest 进入下一 C1 能力，不把本 task 的 Indexed validation 范围继续扩散。

