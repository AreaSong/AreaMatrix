一、执行结论：已完成。

二、执行范围：单任务 `1-3/task-20`。新增 [build_tree_validation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/build_tree_validation.rs:95)，只在 `core/tests/**` 内补 C1-15 validation 测试；未修改 `core/src/**`、`core/area_matrix.udl`、`apps/**`。工作区里已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是执行前已存在的改动，我未触碰。

三、完成情况：核对清单全部满足。测试覆盖空库 Swift 兼容 JSON、大目录稳定排序与稳定 path key、只读无副作用，以及 `RepoNotInitialized` / `Db` / `Io` 错误路径。未扩展到相邻 C1 能力或 UI 页面；Forbidden Touches：未触碰。

四、工程质量：新增内容是集成测试，不引入依赖、不改 public API、不加 mock-only 或硬编码通过态。测试使用真实临时 repo、真实 `init_repo` 和 `list_tree_json` 路径，包含文件系统快照验证只读边界。

五、验证情况：最终全部通过。

- `cd core && cargo fmt --all -- --check`：通过
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过
- `cd core && cargo test --workspace build_tree`：通过，新增 `build_tree_validation` 4 个测试通过
- `cd core && cargo test --workspace`：通过
- `git diff --check`：通过

中途出现过测试代码自身问题：一次格式检查失败、一次空数组类型推断、一次 fixture 字节数期望错误；均修正后重跑通过。

六、风险与后续：剩余风险是未做 macOS UI 消费验证，但这不在本 atomic core-validation 范围内。建议下一个任务进入 `1-3/task-21`，做 C1-15 integration verify。

