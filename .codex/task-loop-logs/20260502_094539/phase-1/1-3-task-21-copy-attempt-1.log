一、执行结论：已完成

二、执行范围：单任务 `1-3/task-21`，只新增了 [core/tests/build_tree_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/build_tree_integration_verify.rs:117)。未修改产品实现、`core/area_matrix.udl` 或 `apps/**`。工作区原有的 `.codex/task-loop-runs/20260502_094539/summary.json`、`tasks/prompts/_shared/progress.json` 仍是既有脏状态，本次未处理。

三、完成情况：已补齐 C1-15 integration-verify 证据。新增测试覆盖：
- C1-15 capability spec、Core API、UDL、control map、S1-08/S1-09/S1-10 页面消费一致性。
- 真实 `list_tree_json` 空库默认树、列表树、loading 可复用稳定树快照。
- `RepoNotInitialized` / `Db` 错误边界、忽略 generated 输出、只读无副作用。
- 未扩展到相邻 C1 能力，未触碰 `Forbidden Touches: apps/**`。

四、工程质量：测试结构已拆分为命名 helper，避免单个测试函数过长；没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本。错误路径和只读边界有真实临时 repo 证据。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过
- `cd core && cargo test --workspace build_tree`：通过，C1-15 相关 16 条测试通过，其中新增 integration-verify 3 条通过
- 额外补跑 `cd core && cargo test --workspace`：通过
- `git diff --check`：通过

中途 clippy 抓到新增 helper 的 needless borrow，已修正并复跑通过。

六、风险与后续：剩余风险是 macOS UI 真实消费未运行，因为本 task 禁止触碰 `apps/**`，且当前范围是 Core integration verify。建议下一个任务按 manifest 进入 `1-4/task-01`。

