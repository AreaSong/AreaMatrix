一、执行结论

已完成 `1-3/task-08`。

二、执行范围

新增测试文件：[core/tests/get_file_detail_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/get_file_detail_integration_verify.rs:1)

未修改产品实现、Core API 文档、UDL 或 `apps/**`。`git status` 中已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是执行前已存在的脏状态，本任务未处理。

三、完成情况

核对清单全部满足：只覆盖 C1-12；已按 manifest 读取 Exact Docs；没有实现相邻 C1 能力或 UI；交叉检查了 capability、control map、S1-12、S1-15、Core API、UDL 与 Rust API；新增测试使用真实 `init_repo`、`import_file`、SQLite metadata 和 `get_file`，不是 mock/空壳。

Forbidden Touches：未触碰 `apps/**`。

四、工程质量

新增 integration-verify 覆盖：
- 文档/API/UDL/消费页面一致性：[core/tests/get_file_detail_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/get_file_detail_integration_verify.rs:121)
- 真实详情查询无副作用：[core/tests/get_file_detail_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/get_file_detail_integration_verify.rs:200)
- 未初始化、缺失、deleted、staging 行结构化错误：[core/tests/get_file_detail_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/get_file_detail_integration_verify.rs:231)

无新增依赖；无产品路径 `unwrap/panic`；测试中的 `expect` 都有明确上下文。

五、验证情况

已运行并通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace get_file_detail`
- `cd core && cargo test --workspace`
- `git diff --check`

失败：无。无法运行：无必需项未运行。macOS/UI 验证未运行，因为本任务禁止触碰 `apps/**`，且 UI 消费闭环属于后续页面任务范围。

六、风险与后续

剩余风险：未验证真实 macOS Detail UI 渲染，这符合当前 Core integration-verify 范围。回滚方式：删除新增的 `core/tests/get_file_detail_integration_verify.rs` 即可回退本任务改动。

建议下一个任务按 manifest 顺序进入 `1-3/task-09`。

