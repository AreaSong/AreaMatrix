一、验收结论

- 不通过

二、验收范围

- 单任务：`1-3/task-02`
- 对应文件路径：`tasks/prompts/phase-1/1-3-query-detail/task-02-c1-11-implementation.md`
- 代码范围：`core/src/**`、`core/area_matrix.udl`、`core/tests/**`
- 禁止触碰范围：`apps/**`

三、完成度摘要

- 已覆盖项数：3
- 未覆盖项数：1
- 通过项数：8
- 不通过项数：1
- 阻塞项数：1

四、逐项验收结果

- C1-11 最小真实实现：通过。`core/src/db/mod.rs:176` 使用真实 SQLite 查询，支持 category、imported_at 窗口、limit clamp、offset，并按 `imported_at DESC` 排序。
- UDL / API 暴露：通过。`core/area_matrix.udl:55` 暴露 `list_files`，`core/area_matrix.udl:125` 暴露 `FileFilter`。
- 测试覆盖：通过。`core/tests/list_files_implementation.rs:67` 起覆盖空库、未初始化、默认隐藏 deleted/staging、分类/时间过滤、分页和 clamp。
- Forbidden Touches：通过。`git diff --name-only -- apps` 无输出；`git status --short -- apps core/src core/area_matrix.udl core/tests` 仅显示 `core/src/db/mod.rs` 与 `core/tests/list_files_implementation.rs`。
- 未扩展到未绑定能力：不通过。`core/src/api.rs:332` 在 `list_files` 后调用 `ensure_indexed_sources_available`，`core/src/api.rs:406-415` 会对 indexed source 做文件系统探测并返回 `FileNotFound` 等错误；但 C1-11 spec 只允许 `RepoNotInitialized` / `Db`，见 `docs/core/capability-specs/stage-1-mvp/C1-11-list-files.md:31-34`。

五、工程质量验收

- 代码结构与逻辑：通过。`core/src/db/mod.rs:176-203` 查询路径清晰，辅助函数拆分后文件长度 471 行，未超过 500 行。
- 注释 / rustdoc / 文档同步：不通过。`core/src/api.rs:321-331` 公开 rustdoc 记录了 indexed source 缺失错误，但 Exact Docs 的 C1-11 错误码未同步。
- 错误处理与边界处理：不通过。`list_files` 实际可返回 `FileNotFound` / `PermissionDenied` / `InvalidPath` / `Io`，超过 C1-11 文档错误边界。
- 测试与验证覆盖：通过。指定验证和 validation-driver 补充全量测试均通过。
- 占位、硬编码、mock-only、一次性实现检查：通过。未发现 C1-11 路径使用 mock、静态示例或硬编码通过态。

六、阻塞项

- P1：`list_files` 暴露了 C1-11 文档未声明的 indexed source 文件探测错误。
  说明：C1-11 SSOT 要求错误码为 `RepoNotInitialized`、`Db`；当前实现会在返回 DB 列表后探测 indexed source，并把缺失源文件映射为 `CoreError::FileNotFound`。现有测试也证明该行为：`core/tests/import_index_file_implementation.rs:240-254`、`core/tests/import_index_file_validation.rs:298-312`。
  绝对路径：`/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:332`
  为什么导致不通过：任务要求“只处理 C1-11 implementation 范围”且“没有扩展到未绑定能力”；该行为把 C1-08 indexed import / missing-source 语义混入 C1-11 list-files，导致实际 API 错误边界与 Exact Docs 不一致。

七、验证情况

- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace list_files`：通过，包含 `list_files_contract_api` 2 个测试、`list_files_implementation` 6 个测试，以及 import_index 中按名称匹配的 2 个测试。
- `cd core && cargo test --workspace`：通过。

八、最终判定说明

- 本次执行尚未达到验收标准，不能视为完成。验证命令通过，但 C1-11 实际错误边界与 SSOT 不一致，且存在未绑定能力语义混入当前 API 的工程质量阻塞。

VERIFY_RESULT: FAIL