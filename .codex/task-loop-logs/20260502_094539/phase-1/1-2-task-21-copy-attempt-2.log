**一、执行结论**

已完成。`1-2/task-21` 上次验收失败的 C1-09 `Overwrite / Replace` 缺口已修复，并完成本地验证。

**二、执行范围**

单任务：`1-2/task-21`，未触碰 `apps/**`，`core/area_matrix.udl` 无需修改。

修改文件：
- `core/src/storage/dedup.rs`
- `core/src/storage/destination.rs`
- `core/src/storage/import.rs`
- `core/src/storage/mod.rs`
- `core/src/storage/safe_move.rs`
- `core/src/db/import.rs`
- `core/src/db/mod.rs`
- `core/src/api.rs`
- `core/src/domain.rs`
- `core/tests/detect_duplicate_implementation.rs`

**三、完成情况**

- Skip / Ask：仍返回 `DuplicateFile { existing_path }`，无副作用。
- KeepBoth：仍保留两份，并走去冲突命名。
- Overwrite：已改为继续导入；旧 active row 软删除，新 row 激活，写入 `deleted` + `imported` change_log。
- 旧 repo-owned 文件不硬删，先归档到 `.areamatrix/trash/replace-...`，用于 safe replace 和失败回滚。
- DB promotion 失败时会删除新 final、恢复旧 final、清 staging row。
- Forbidden Touches：`git status --short -- apps` 无输出。

**四、工程质量**

结构已拆分：重复策略在 `dedup.rs`，替换目标/归档/guard 在 `destination.rs`，导入主流程仍保持在 `import.rs`；相关文件均低于 500 行。  
`api.rs` / `domain.rs` 的 Overwrite rustdoc 已同步，不再写“后续任务处理”。  
未新增依赖，未引入 mock-only、硬编码通过态或一次性脚本逻辑。

**五、验证情况**

通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace detect_duplicate`
- `cd core && cargo test --workspace`
- `git diff --check`
- `python3 tasks/prompts/_shared/prompt_pipeline.py verify --task 1-2/task-21`

失败：无。  
无法运行：无。

**六、风险与后续**

剩余风险：Overwrite 仍必须由 UI 二次确认后才传入 Core；Core 侧已按 safe replace 语义处理，不直接硬删旧文件。工作区还存在 task-loop 生成的 `.codex` / `progress.json` 变更，我没有回滚它们。

建议下一个任务：继续 `1-2/task-22`，进入 C1-09 后续 failure/edge 或 validation 验收链路。

