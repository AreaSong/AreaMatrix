一、执行结论

已完成。`1-3/task-18` 的 repair retry 已把上次验收失败的 Core API SSOT 漂移、真实 Tree JSON schema、错误码边界和测试证据不足全部收口。

二、执行范围

单任务：`1-3/task-18` / C1-15 build-tree contract-api。

本次有效修改：
- [core/src/tree/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/tree/mod.rs:63)：实现真实 `list_tree_json` TreeNode JSON，递归目录扫描、稳定排序、`relative_path` path key、classifier display name、错误码收敛。
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:376)：校准 public rustdoc 的输入、输出、错误和副作用边界。
- [docs/api/core-api.md](/Users/as/Ai-Project/project/AreaMatrix/docs/api/core-api.md:320)：同步 `RepoNotInitialized / Db / Io`，补 TreeNode schema、输入、输出、副作用和 Stage 2 边界。
- [core/tests/build_tree_contract_api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/build_tree_contract_api.rs:103)：新增真实 API contract 测试。
- 同步调整旧测试对 Tree JSON schema 的断言：`init_empty_repo_*`、`import_copy_file_integration_verify.rs`。

`core/area_matrix.udl` 未改，现有 `string list_tree_json(string repo_path, string locale);` 签名仍与 Core API 对齐。`apps/**` 无差异。

三、完成情况

核对清单全部完成：
- 仅处理 C1-15 contract-api。
- 已按 manifest Exact Docs 对齐，`docs/api/core-api.md` 作为 SSOT 已修正。
- 未实现相邻 C1 能力或 UI 页面。
- Core API 合同意图已同步；UDL 签名保持一致。
- 输入、输出、错误码、副作用边界已在 rustdoc、Core API 文档和真实测试里覆盖。

完成标准：
- C1-15 contract-api 有真实 API 调用证据：空树、递归目录、稳定排序、Swift-compatible schema、错误路径、无副作用快照。
- 未扩展到未绑定能力。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

代码结构清晰，没有 mock-only、硬编码通过态或静态 fixture 作为完成证据。`tree` 实现拆分为扫描、分类显示名、node 构建、错误归一化等小函数；生产路径无 `unwrap()` / `expect()` / `panic!()`。新增测试覆盖真实输出 schema、`RepoNotInitialized`、只读副作用边界和文档/UDL 对齐。

High 风险记录：影响面是 Core API/UDL 合同相关，但本次没有破坏 UDL 签名、没有 DB migration、没有用户文件写入。回滚方式是回退上述 core/docs/tests 文件；无用户文件移动、删除、覆盖风险。

五、验证情况

全部通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace build_tree`
- `cd core && cargo test --workspace`
- `git diff --check`
- `git diff --name-only -- apps` 无输出

六、风险与后续

剩余风险：独立 verify-ready 还应由 task-loop 下一步只读验收执行；当前实现侧和本地验证已通过。建议下一个动作是进入 `1-3/task-18` verify-ready，通过后再推进 `1-3/task-19`。

