一、执行结论

已完成 `1-3/task-12`。

二、执行范围

单任务：`C1-13 list-change-log integration-verify`。

修改文件：
- 新增 [core/tests/list_change_log_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/list_change_log_integration_verify.rs:1)

未修改产品实现、未修改 `core/area_matrix.udl`、未触碰 `apps/**`。工作区里已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是本轮前已存在的脏状态，我没有改动或回滚。

三、完成情况

- 只处理 C1-13：通过新增 integration verify 测试锁定 `list_changes`，未实现相邻 C1/UI。
- 已读取 Exact Docs，并以 docs 为 SSOT。
- 已交叉检查 capability、control map、Core API、UDL、Rust 入口、消费页面。
- 已确认无 mock/空壳：测试覆盖真实 `import_file -> rename_file -> list_changes` 查询路径，以及 seeded Stage 1 action 查询。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

新增测试拆成文档/合同校验、真实查询路径、过滤与错误路径三类；没有新增依赖，没有一次性脚本或硬编码通过态。错误路径覆盖 `RepoNotInitialized` 与非法 `detail_json -> Db`，并验证 `list_changes` 不改变源文件、repo 文件、active rows、change_log 计数或 staging 状态。

五、验证情况

已通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace list_change_log`
- 额外按 validation-driver 扩展：`cd core && cargo test --workspace`
- `git diff --check`

六、风险与后续

剩余风险：未做 macOS UI 接入验证，因为本任务禁止触碰 `apps/**`，且当前范围是 Core integration verify。建议下一个任务继续按 manifest 推进 `1-3/task-13`。

