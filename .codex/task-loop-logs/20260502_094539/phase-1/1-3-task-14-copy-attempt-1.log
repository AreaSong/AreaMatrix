一、执行结论

已完成 `1-3/task-14`。

二、执行范围

单任务：C1-14 `read-write-note` Core implementation。

本次修改：
- [core/src/note.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/note.rs:14)：实现 `read_note` / `write_note` 的 sidecar 与一致性边界。
- [core/src/db/note.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/note.rs:22)：实现 `notes` upsert + `change_log.edited_note` 同事务写入。
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:404)：API 壳接入真实实现。
- [core/src/db/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/mod.rs:15)、[core/src/lib.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/lib.rs:12)：模块注册。
- [core/tests/read_write_note_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/read_write_note_implementation.rs:112)：新增实现级测试。

未触碰 `apps/**`。`core/area_matrix.udl` 已与文档签名一致，未改。

三、完成情况

核对清单全部完成：只做 C1-14；已按 Exact Docs 作为 SSOT；未实现相邻 C1/UI；实现最小真实路径；保持 Core 分层，DB 逻辑在 `db`，文件边界在 `note`。

完成标准满足：写入后 DB `notes`、sidecar `.md`、`change_log.edited_note` 可由测试证明一致；未扩展到未绑定能力。

四、工程质量

实现不是 mock 或占位。写入会校验 active file、拒绝覆盖未登记 sidecar、检测 DB/sidecar 不一致；DB 写失败会恢复旧 sidecar，避免伪成功日志。新增代码为私有模块，无新增 public Rust API；现有 public API rustdoc 继续覆盖 C1-14 语义。

五、验证情况

全部通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace read_write_note`：8 个相关测试通过。
- 额外按 validation-driver 补跑：`cd core && cargo test --workspace`：全 workspace 通过。
- `git diff --check`：通过。

六、风险与后续

剩余风险：未做 macOS UI/CoreBridge 接入验证，因为本 task 禁止触碰 `apps/**`。工作区另有 task-loop 相关既有脏文件：`.codex/task-loop-runs/20260502_094539/summary.json` 与 `tasks/prompts/_shared/progress.json`，不属于本次 Core 改动。

建议下一个任务：`1-3/task-15`，按 manifest 做 C1-14 failure/recovery 验证与补强。

