一、执行结论

已完成。

二、执行范围

- 单任务：`1-3/task-17`
- 实际产品改动：
  - [core/src/note.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/note.rs:143)
  - [core/tests/read_write_note_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/read_write_note_integration_verify.rs:180)
- 未触碰 `apps/**`。`git status --short -- apps` 无输出。
- 工作区中 `.codex/task-loop-*` 与 `progress.json` 仍有 task-loop 既有状态文件，不属于本次产品修复范围。

三、完成情况

- C1-14 范围控制：通过，只修复 `read_note/write_note` 相关 sidecar 写入证据。
- Exact Docs 作为 SSOT：通过，已按 task/manifest/Exact Docs/code 交叉核验。
- 不实现相邻 C1 或 UI：通过，未改 UDL、API docs、Swift/UI。
- capability/control map/S1-14 消费一致性：通过，integration verify 测试继续覆盖。
- 无 mock/空壳：通过，修复是真实文件写入路径。

四、工程质量

`write_note` 现在按旧 sidecar 状态选择写入策略：无旧 sidecar 时使用 final-time no-clobber 落盘，目标已存在则返回 `CoreError::PermissionDenied`，不再用 `rename` 覆盖校验后出现的未确认 `.md` 文件。copy fallback 只在 `create_new` 成功后才允许失败清理 final，避免误删用户已有 sidecar。

新增了回归证据：unit test 覆盖 final-time existing destination，integration verify 覆盖未确认 sidecar 不被覆盖、DB 不写入、change_log 不新增。没有新增依赖，没有 public API 变更。

五、验证情况

全部通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace read_write_note`
- `cd core && cargo test --workspace`
- `git diff --check`
- `git status --short -- apps`

六、风险与后续

本次阻塞项已修复。剩余建议：让 task-loop 再跑一次 `1-3/task-17` 的 verify-ready 只读验收；通过后进入 `1-3/task-18`。

