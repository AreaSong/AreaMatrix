一、执行结论

已完成 `1-4/task-02: C1-16 recover-on-startup implementation`。

二、执行范围

单任务，仅修改/新增 Core 范围文件：

- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:155)
- [core/src/lib.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/lib.rs)
- [core/src/db/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/mod.rs)
- [core/src/recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/recovery.rs:12)
- [core/src/db/staging_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/staging_recovery.rs:12)
- [core/tests/recover_on_startup_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/recover_on_startup_implementation.rs:100)

三、完成情况

- 核对清单：全部完成。
- C1-16 最小真实路径：`recover_on_startup` 已接入真实 Core API，读取 initialized repo DB，清理 `.areamatrix/staging/` 残留文件，回滚 `files.status = 'staging'` 行。
- 边界：未实现 reindex、FSEvents、overview、UI 或相邻 C1 能力。
- 文件安全：只删除安全判定为 `.areamatrix/staging/**` 的 staging 文件；异常 staging row 指向 final/user 路径时只回滚 DB 行并返回 warning，不删除文件。
- Forbidden Touches：未触碰 `apps/**`。
- UDL：未修改；现有 `core/area_matrix.udl` 已包含 `recover_on_startup` 与 `RecoveryReport` 合同。

四、工程质量

代码结构保持分层：API 层只转发，DB helper 只做 staging row 查询/删除，recovery 模块负责恢复规则。没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本逻辑。公共 API 原有 rustdoc 保留；本次新增的是 `pub(crate)` 内部实现。错误处理覆盖 `RepoNotInitialized`、`Db`、`Io`、`PermissionDenied` 路径，并避免静默吞掉可恢复失败。

五、验证情况

已运行并通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace recover_on_startup`
- `cd core && cargo test --workspace`
- `git diff --check -- core/src/api.rs core/src/db/mod.rs core/src/lib.rs`

失败：无。无法运行：无。

六、风险与后续

剩余风险：本任务只完成 Core 层；未验证 macOS UI 对 `RecoveryReport.warnings` 的实际展示，这属于后续 UI/page-feature 范围。当前工作区还有两个非本任务既有脏文件：`.codex/task-loop-runs/20260502_094539/summary.json` 与 `tasks/prompts/_shared/progress.json`，我没有把它们纳入本次修改范围。

建议下一个任务：`1-4/task-03`，即 `C1-16 failure-recovery`，继续补齐失败路径和恢复边界验收。

