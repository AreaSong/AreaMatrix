一、执行结论：已完成。

二、执行范围：
- 修改 [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:348)：`get_file(repo_path, file_id)` 接到真实 DB active-row 查询。
- 新增 [core/tests/get_file_detail_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/get_file_detail_implementation.rs:84)：覆盖 C1-12 implementation 行为。
- 未修改 `core/area_matrix.udl`，因为 UDL 已有 `FileEntry get_file(string repo_path, i64 file_id);` 且与文档一致。
- 未触碰 `apps/**`。

三、完成情况：
- 只处理 C1-12：通过。
- Exact Docs 已按 manifest 读取并以文档为 SSOT：通过。
- 未实现相邻 C1 能力或 UI 页面：通过。
- 最小真实路径：通过，复用现有 `db::get_active_file_by_id`，无 mock/硬编码。
- layered design：通过，API 层调用 DB helper，未引入反向依赖。

四、工程质量：
- 代码结构清晰，没有新增抽象或一次性脚本化实现。
- `get_file` 保留既有 rustdoc；新增 public API 文档不需要变更。
- 错误边界覆盖 `RepoNotInitialized`、`FileNotFound`、active/deleted/staging 可见性。
- Indexed 缺失源文件场景不探测外部路径，符合“可选 metadata 检查但不得修改文件”的边界。
- 无新增依赖。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace get_file_detail`：通过，C1-12 contract 3 条 + implementation 4 条通过。
- 额外按 validation-driver 扩展：`cd core && cargo test --workspace`：通过。
- `git diff --check`：通过。

六、风险与后续：
- 剩余风险：未做 macOS UI 消费验证，按本 task 范围不需要；后续由 `2-3/task-01` 等页面任务接入。
- 工作区已有非本次改动的脏文件仍存在：`.codex/task-loop-runs/20260502_094539/summary.json`、`tasks/prompts/_shared/progress.json`。
- 建议下一个任务：继续 `1-3/task-07`，即 C1-12 failure/edge 或 manifest 中的下一片。

