一、执行结论

已完成。

二、执行范围

单任务：`1-2/task-19`，仅补齐 C1-08 integration-verify 证据。

修改文件：
- [core/tests/import_index_file_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_integration_verify.rs:1)

仓库原本已有未提交改动：`.codex/task-loop-runs/20260502_094539/summary.json`、`tasks/prompts/_shared/progress.json`，我没有改它们。

三、完成情况

核对清单全部通过：
- 只处理 C1-08：通过。
- Exact Docs 已读取并作为 SSOT：通过。
- 未实现相邻 C1 能力或 UI 页面：通过。
- 已交叉检查 capability、control map、消费页面、Core API、UDL：通过。
- 无 mock/空壳伪完成：通过，测试走真实 `import_file`、SQLite、`change_log`、`list_files` 和 staging 目录。

Forbidden Touches：未触碰 `apps/**`。

四、工程质量

新增测试覆盖：
- 文档/API/UDL/control map/消费页面一致性。
- Indexed 模式真实导入：源文件保留、不创建最终仓库副本、DB row 为 active/indexed、`source_path` 保留、写入 `change_log.imported`。
- 删除外部源后 `list_files` 返回 `FileNotFound` 可恢复错误。
- DB 写入失败时不改源文件、不留 active/staging row、不写 change_log、不留 staging 文件。

没有新增依赖、没有产品实现改动、没有占位或硬编码通过态。

五、验证情况

全部通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace import_index_file`
- `cd core && cargo test --workspace`
- `git diff --check`

六、风险与后续

剩余风险：未做 macOS UI 真机消费验证，因为本任务禁止触碰 `apps/**`，且当前范围是 Core integration-verify。

建议下一个任务：先运行 verify-ready 对 `1-2/task-19` 做只读验收；通过后进入 `1-2/task-20`。