一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-25`，仅修改 `core/src/**` 与 `core/tests/**`。主要文件：`core/src/storage/dedup.rs`、`core/src/storage/rename.rs`、`core/src/db/import.rs`、`core/src/api.rs`、`core/tests/resolve_name_conflict_contract_api.rs`，并校准相关 import / duplicate 旧断言。未触碰 `apps/**`，`core/area_matrix.udl` 未改，因为现有 `import_file` / `rename_file` 签名已满足合同。

三、完成情况：C1-10 同名不同内容默认安全编号已接入真实导入路径；`rename_file` 已从占位改为真实 repo-owned 文件重命名，保持分类不变，自动避让同名目标，并在 DB 事务中更新 `files.path/current_name` 与写入 `change_log.renamed`。DB 写入失败时会回滚文件系统 rename。

四、工程质量：无 mock-only、硬编码通过态或一次性脚本实现；新增 rename 路径有显式输入校验、repo-relative path 校验、文件存在校验、冲突编号和 rollback guard。未新增依赖。旧测试中与 C1-10 SSOT 冲突的 `Conflict` 断言已改为真实 FS/DB/change_log 一致性证据。

五、验证情况：全部通过。

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace resolve_name_conflict`：9 个目标测试通过
- `cd core && cargo test --workspace`：全量通过
- `git diff --check`

六、风险与后续：当前 C1-10 阻塞项已修复；剩余未覆盖的是 macOS UI 消费侧真实接入，这属于后续 `S1-23` / `S1-24` 页面任务，不在本 task 范围。注意：工作区里仍有本轮开始前已存在的 task-loop artifact / progress 变更，我没有回滚或改写它们。

