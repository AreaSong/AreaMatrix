一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-26`。本次修复集中在 `core/src/**`、`core/tests/**`，未触碰 `apps/**`。主要修改了 `safe_move.rs`、`import.rs`、`destination.rs`、`api.rs`，并补齐 C1-10 与相关 overwrite 路径测试及测试用系统 Trash 隔离 helper。

三、完成情况：C1-10 的两个验收失败点已修复。最终落位和 rename 改为不覆盖语义，raced target 会返回 `Conflict`，不会替换已有目标。Replace 旧文件路径已从内部 `.areamatrix/trash` 修正为系统 Trash 语义，并保留 DB 失败时可回滚的内部 pending archive。Forbidden Touches 检查为空。

四、工程质量：实现走真实文件系统路径，没有 mock-only、硬编码通过态或一次性脚本逻辑。错误路径覆盖了 raced final target、Replace 成功、DB 失败回滚和 Trash 证据。`import_file` rustdoc 已同步 Replace 行为。测试中用临时 `HOME/.Trash` 隔离系统 Trash 行为，避免触碰真实用户 Trash。

五、验证情况：全部通过。

```bash
cd core && cargo fmt --all -- --check
cd core && cargo clippy --all-targets --all-features -- -D warnings
cd core && cargo test --workspace resolve_name_conflict
cd core && cargo test --workspace
git diff --check
python3 tasks/prompts/_shared/prompt_pipeline.py doctor
git diff --name-only -- apps
git ls-files --others --exclude-standard apps
```

六、风险与后续：剩余风险主要是生产环境系统 Trash 依赖平台能力；实现已先尝试 macOS Trash API，并保留 fallback，不会删除用户文件本体。建议下一个任务继续按队列进入 `1-2/task-27`。

