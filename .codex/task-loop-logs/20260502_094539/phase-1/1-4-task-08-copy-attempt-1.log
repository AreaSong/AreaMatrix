一、执行结论

已完成 `1-4/task-08`。

二、执行范围

单任务：C1-17 `sync-external-created` failure-recovery。

修改文件：
- [core/src/sync/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/sync/mod.rs:35)：权限读取失败映射为 `CoreError::PermissionDenied`，并补齐错误说明。
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:473)：同步 public Rust API rustdoc 的错误范围。
- [core/tests/sync_external_created_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_created_failure_recovery.rs:73)：新增 failure-recovery 测试。

三、完成情况

核对清单均完成：只处理 C1-17；已按 Exact Docs 定界；未实现相邻 C1 能力或 UI；覆盖 DB 失败回滚、缺失文件失败后重放、权限失败；未删除、覆盖、移动用户文件。

完成标准满足：C1-17 failure-recovery 有测试证据；未扩展到未绑定能力。`apps/**` 未触碰。`core/area_matrix.udl` 已读取但无需修改。

四、工程质量

实现保持在现有 sync 错误映射路径内，没有新增依赖、mock-only、硬编码通过态或一次性脚本。新增测试使用临时 repo，验证 DB/cursor/用户文件状态一致性；DB 故障只在测试临时库中制造，不改变产品 schema。

五、验证情况

已通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace sync_external_created`
- 额外补跑：`cd core && cargo test --workspace`

无法运行项：无。

六、风险与后续

剩余风险：真实 macOS FSEvents/iCloud 平台流未在本任务覆盖，按文档属于 app 层延后范围；Core 侧失败恢复已用临时文件系统和 SQLite 证据覆盖。

建议下一个任务：`1-4/task-09`，C1-17 validation。

注意：工作区中 `.codex/task-loop-runs/20260502_094539/summary.json` 和 `tasks/prompts/_shared/progress.json` 在本次修改前已是 dirty，本次未触碰。

