一、执行结论

已完成。S1-21 `import-result` 已按 page spec/control map 收口到 C1-06 + C1-13 的整页闭环。

二、执行范围

单任务：`2-2/task-21`

修改范围主要在：
- `apps/macos/AreaMatrix/**`
- `apps/macos/AreaMatrixTests/ImportResultCopyRetryTests.swift`

未触碰 `core/src/**`。当前工作树里还存在 task-loop evidence / progress 文件改动和既有运行目录，我未回滚这些记录。

三、完成情况

核对清单：
- 已逐项核验 `2-2/task-19` / `2-2/task-20` 对应 C1-06、C1-13。
- S1-21 结果页现在包含结果摘要、筛选、详情、Show existing file、Export Details、Done、Retry Failed。
- `Retry Failed` 已改为先进入 S1-20 `importProgress`，只用失败项组成重试队列，再通过真实 `import_file` 路径完成后回到 S1-21。
- C1-13 通过 `CoreChangeLogListing` / `CoreBridge.listChanges` 加载最近 imported change log，并有失败映射。
- 未新增 control map 外能力。

四、工程质量

代码仍走 `CoreBridge`，没有绕过 UniFFI；导出内容使用脱敏路径，不包含用户文件内容；Show existing file 走相对路径安全解析，避免越出 repo。没有新增依赖。新增测试覆盖了重试路由、真实 importer 调用、失败映射、change log 加载、导出脱敏、重复项定位。

五、验证情况

已运行：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 结果：编译通过，但测试 runner 被当前沙箱阻断：`com.apple.testmanagerd.control ... Sandbox restriction`。
- `./dev test macos`
  - 结果：通过。该命令先运行同款 `xcodebuild test`，确认是 testmanagerd 沙箱阻断后复用已构建 XCTest bundle，`xcrun xctest` 执行通过：262 tests, 0 failures。

六、风险与后续

剩余风险：未做真实 macOS UI 手工交互，只完成 build/test 级验证；原始 `xcodebuild test` 在当前执行环境仍会被 testmanagerd 沙箱阻断。建议下一个任务进入 `2-2/task-22`，继续按 manifest 边界执行。

