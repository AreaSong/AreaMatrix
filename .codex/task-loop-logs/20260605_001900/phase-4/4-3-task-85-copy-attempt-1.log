一、执行结论

已完成。C4-17 `platform-capabilities` 可作为后续 page-feature task 的真实 Core 依赖。

二、执行范围

单任务：`4-3/task-85`

修改文件清单：
- [core/tests/platform_capabilities_validation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/platform_capabilities_validation.rs)：仅 `cargo fmt` 格式化测试代码。

开始前已存在且仍未处理的本地状态改动：
- `.codex/task-loop-runs/20260605_001900/summary.json`
- `tasks/prompts/_shared/progress.json`

三、完成情况

核对清单：
- capability spec、Core API、UDL、Rust 实现、测试已交叉核对：通过。
- 消费页面 `S4-LNX-03`、`S4-X-02`、`S4-X-08` 需要的状态、错误、副作用边界已覆盖：通过。
- 未发现实现 control map 之外能力：通过。
- 未触碰 `apps/**` Forbidden Touches：通过。

四、工程质量

代码结构保持清晰，C4-17 是 side-effect free 的结构化能力矩阵，不读 repo、不写 DB、不触碰用户文件、不启动 watcher、不调用云盘/Trash/分享扩展能力。  
本次没有新增 public API、依赖或业务逻辑；只修正测试格式。现有 rustdoc、错误处理、边界测试和文档同步证据满足当前任务范围。

五、验证情况

已运行：
- `./dev check task 4-3/task-85`：第一次失败在 `cargo fmt --all -- --check`，原因是 `core/tests/platform_capabilities_validation.rs` 格式不符合 rustfmt。
- `cargo fmt --all`：通过，修正上述测试格式。
- `./dev check task 4-3/task-85`：最终通过。

最终验证覆盖：
- prompt doctor：OK，`tasks: 637`，`manifests: 637`。
- `git diff --check`：通过。
- `cargo fmt --all -- --check`：通过。
- `cargo clippy --all-targets --all-features -- -D warnings`：通过。
- targeted tests：
  - `platform_capabilities_contract_api`：5 passed。
  - `platform_capabilities_failure_recovery`：5 passed。
  - `platform_capabilities_implementation`：5 passed。
  - `platform_capabilities_validation`：3 passed。

六、风险与后续

剩余风险：未运行全量 `./dev check all`，符合本 task validation 上限；UI 实际接入仍由后续 page-feature/page integration tasks 验收。  
建议下一个任务：继续 `4-3/task-86`，进入 C4-18 contract-api。