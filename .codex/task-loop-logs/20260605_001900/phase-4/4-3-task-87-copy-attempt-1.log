一、执行结论

已完成。

二、执行范围

单任务：`4-3/task-87`，仅实现 C4-18 missing-file-recovery Core 行为。

修改文件清单：

- `core/src/missing_file_recovery.rs`
- `core/src/missing_file_recovery/filesystem.rs`
- `core/src/db/missing_file_recovery.rs`
- `core/src/db/mod.rs`
- `core/tests/missing_file_recovery_implementation.rs`

三、完成情况

核对清单全部完成：只实现 C4-18；实现 `get_missing_file_state`、`relink_missing_file`、`remove_missing_file_record` 的真实 Core 路径；DB 更新和 change log 写入已落地；Rust Core 不依赖平台 UI API；补了 targeted implementation test。

完成标准已满足：C4-18 可通过 Rust test 验证；Remove Record 只软删除 metadata，不删除用户文件；Relink 只在 hash 匹配时更新 metadata，hash mismatch 不写 DB、不关联文件。

未触碰 Forbidden Touches：`apps/**` 未修改。

四、工程质量

代码结构已拆分：入口、报告构造在 `missing_file_recovery.rs`；文件系统只读检查、hash、路径边界在 `missing_file_recovery/filesystem.rs`；DB 事务写入在 `db/missing_file_recovery.rs`。文件长度均低于 500 行。

错误处理覆盖 `FileNotFound`、`PermissionDenied`、`Db`；没有 mock-only、硬编码通过态或一次性脚本实现。未新增依赖。

五、验证情况

已通过：

- `cargo test --test missing_file_recovery_implementation -- --nocapture`
- `cargo test --test missing_file_recovery_contract_api -- --nocapture`
- `cargo fmt --all -- --check`
- `./dev check task 4-3/task-87`

最终 task gate 输出包含：doctor OK、diff check OK、contract test 4/4、implementation test 4/4。

六、风险与后续

剩余风险：未跑 broad gate，这是按任务要求不升级验证范围；当前验证以上述 task gate 为准。

建议下一个任务：继续 `4-3/task-88`，补 C4-18 failure / edge / rollback 语义。

另外，工作区开工前已有的 `.codex/task-loop-runs/20260605_001900/summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是脏改，我没有修改或回滚它们。

