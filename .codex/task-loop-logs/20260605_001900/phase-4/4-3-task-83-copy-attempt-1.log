一、执行结论

已完成。

二、执行范围

单任务：`4-3/task-83`。

修改文件：
- [core/src/platform_capabilities.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/platform_capabilities.rs)
- [core/tests/platform_capabilities_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/platform_capabilities_failure_recovery.rs)

未触碰 `apps/**`。工作树里原本已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是既有脏改动，未纳入本次任务范围。

三、完成情况

核对清单：
- 空态 / 非法输入 / 错误码映射：已覆盖，`Unknown platform`、空版本、空白版本、非法字符、超长版本均返回 `CoreError::Config`，并验证 `to_error_mapping()`。
- 权限、IO/DB 错误：C4-17 合同本身不读 repo、不写 DB、不触碰用户文件；新增测试证明不会探测 IO/DB/权限，也不会修改用户文件或 `.areamatrix` 派生目录。
- 写入、移动、删除、导入或同步：C4-17 不涉及这些副作用；测试和文档断言覆盖“无 FS / DB 变化”和 transactional import 的失败不留半成品语义。
- 隐私/AI/远程调用：不涉及远程调用；新增测试确认实现不包含网络、日志、secret/key/token 相关路径。
- 吞错 / 静默降级：未使用吞错，非法输入显式返回 `Config`。

完成标准：
- C4-17 失败路径有明确返回和测试证据。
- 用户文件安全保证已通过 tempdir sentinel 文件和静态副作用检查覆盖。
- 高风险路径的回滚/恢复说明通过 Exact Docs 断言保持对齐。

四、工程质量

代码结构保持表驱动，未引入新依赖、mock-only、硬编码通过态或一次性脚本。新增测试聚焦 failure-edge，不扩大到相邻能力。`platform_capabilities.rs` 只增加了一个小型字符校验 helper。

五、验证情况

已运行：
- `cargo fmt --manifest-path core/Cargo.toml`：通过。
- `./dev check task 4-3/task-83`：通过。
  - prompt doctor：OK。
  - diff check：OK。
  - `platform_capabilities_contract_api`：5 passed。
  - `platform_capabilities_failure_recovery`：5 passed。
  - `platform_capabilities_implementation`：5 passed。

未运行：
- 未运行 `cargo test --workspace` / `cargo clippy` / `./dev check core` / `./dev check all`，因为本 atomic Core task 的 manifest 验证上限是 `./dev check task 4-3/task-83`。

六、风险与后续

剩余风险：未跑全量 CI，合并前仍需远端 CI / review 兜底。  
建议下一个任务：继续 `4-3/task-84`，补 C4-17 validation 任务的验证证据。