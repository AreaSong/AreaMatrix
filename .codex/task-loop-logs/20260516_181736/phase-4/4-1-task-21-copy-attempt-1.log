一、执行结论

已完成。C2-05 tag CRUD 的 Core API / UDL 合同已存在并通过本 task 指定验证；本轮未新增代码 diff。

二、执行范围

- 单任务：`4-1/task-21`
- 本任务相关文件：
  - `core/src/tags.rs`
  - `core/src/lib.rs`
  - `core/area_matrix.udl`
  - `core/tests/tag_crud_contract_api.rs`
- 本轮修改文件清单：无
- 当前 dirty 状态仅见既有 runner 状态：`tasks/prompts/_shared/progress.json`、`.codex/task-loop-runs/20260516_181736/`

三、完成情况

- 已读取 C2-05 能力规格、Stage 2 control map、Core API、错误码。
- 已确认 API：`add_tag(repo_path, file_id, tag)`、`remove_tag(...)`、`list_tags(repo_path, file_id)`。
- 已确认输出类型：`TagSet` / `TagRecord` 覆盖当前文件标签、候选标签、recent tags、更新时间。
- 已确认错误边界：`FileNotFound` / `Db` / `InvalidPath`。
- 已确认消费方：S2-07 可获得当前 chip、候选、recent、失败重试状态；S2-08 可复用 `available_tags`，但 scope/count/selected facet 仍由 C2-02 负责。
- 未触碰 Forbidden Touches：`apps/**` 未触碰。

四、工程质量

代码结构保持清晰：C2-05 合同集中在 `core/src/tags.rs`，UDL 与 Core API 文档一致，合同测试覆盖 docs/API/UDL/control map/UX 消费状态。当前实现明确停在 contract/API 层，返回未实现的 `Db` 错误，不伪造成功态，也未提前实现批量标签、Undo、非 AI 建议或 AI 自动标签。

五、验证情况

已运行：

```bash
./dev check task 4-1/task-21
```

结果：通过。

验证内容包括：
- `prompt_pipeline.py doctor`：OK，tasks `637`，manifests `637`
- `git diff --check`：通过
- `cargo test --test tag_crud_contract_api -- --nocapture`：4 passed

六、风险与后续

剩余风险：C2-05 目前只是合同/API 层，真实 DB 持久化、幂等写入、change_log 写入和失败恢复属于后续 implementation / failure / validation task。建议下一个任务进入 `4-1/task-22`，实现 C2-05 的真实 tag CRUD 持久化边界。

