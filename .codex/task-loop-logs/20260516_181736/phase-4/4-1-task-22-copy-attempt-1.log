已完成。

执行范围
- 单任务：`4-1/task-22`
- 修改文件：`core/src/tags.rs`、`core/src/db/tags.rs`、`core/src/db/mod.rs`、`core/tests/tag_crud_contract_api.rs`、`core/tests/tag_crud_implementation.rs`
- 未触碰 `apps/**`
- `core/area_matrix.udl` 未改，合同已在既有实现里对齐

完成情况
- `C2-05` 的 `add_tag / remove_tag / list_tags` 已接到真实 SQLite `tags` 表和 `change_log`
- 重复添加、重复删除是幂等的
- `file_id <= 0`、非法 tag、未初始化 repo、已删除文件都映射到既有 `CoreError`
- `list_tags` 是只读
- 未实现任何相邻 `C*` 能力
- Forbidden Touches：未触碰

工程质量
- 结构清楚：`core/src/tags.rs` 做门面，`core/src/db/tags.rs` 做 DB 细节
- 新增测试覆盖了契约、正常流、幂等、只读、错误映射和 change log 查询
- 没有 mock-only、硬编码通过态或页面接入
- 变更日志沿用 `external_modified`，用 detail JSON 的 `kind=tag_added/tag_removed` 区分语义，避免扩 schema

验证情况
- `cargo fmt --all -- --check` 通过
- `./dev check task 4-1/task-22` 通过
- 任务级检查内含 `doctor`、`git diff --check`、`tag_crud_contract_api`、`tag_crud_implementation`

风险与后续
- 这次只收口 `C2-05`
- 唯一保留点是 tag 变更日志仍复用 `external_modified`，后续如果要按动作做报表，需要读 detail JSON
- 建议下一步继续 `4-1/task-23`

