一、执行结论

已完成。

二、执行范围

单任务：`4-2/task-52`，仅实现 `S3-02 + C3-02 local-model-status` 的 macOS 页面功能与测试。

修改的实现/测试文件：
- `apps/macos/AreaMatrix/**`
- `apps/macos/AreaMatrixTests/ClassifierRuleEditorCoreBridgeTests.swift`

工作区里原本已有的 runner/progress 状态文件仍是 modified：`.codex/task-loop-runs/.../summary.json`、`tasks/prompts/_shared/progress.json`，未作为产品实现范围处理。

三、完成情况

核对清单全部完成：
- 只处理 S3-02 页面中的 C3-02。
- 已按 S3-02 页面规格、Stage 3 control map、Core API、C3-02 capability spec 接入。
- UI 通过 `CoreBridge` 读取 `getLocalModelStatus` / `locateLocalModelFolder`，没有直接从 View 调 UniFFI。
- 未实现同页其他能力、远程 provider、下载器、训练器、repair 真实写操作或 fallback 能力。

完成标准：
- AI 设置页 `Local model status` 可打开 S3-02 状态页。
- `Check status` / `Retry status check` 走真实 C3-02 CoreBridge。
- `Open model location` 走 C3-02 目录定位结果，不创建目录。
- diagnostics 只展示/复制 Core 返回摘要。
- 未使用 mock/fixture 伪造最终 Core 闭环；测试里 mock 只用于 model 行为隔离，另有默认 `CoreBridge` 真实路径测试。
- 未触碰 `Forbidden Touches`：`core/src/**` 无 diff。

四、工程质量

代码结构按现有 SwiftUI + model + CoreBridge snapshot 模式落地；新增状态映射、错误反馈、平台动作封装和 CoreBridge 转换都在 macOS 层。所有相关 Swift 文件保持在 500 行以内。

错误路径包含 CoreError 映射、目录不可打开、帮助打开失败、诊断复制失败。`Repair` 仅按合同显示为禁用入口并说明需独立 repair capability，没有越界实现写操作。

五、验证情况

已运行并通过：

```bash
./dev check task 4-2/task-52
```

该命令内通过：
- `prompt doctor`
- `git diff --check`
- `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`

第一次验证发现 Swift actor isolation 编译错误，已修复后重跑通过。未运行 `cargo test --workspace`、`./dev check core` 或 `./dev check all`，因为本 atomic task 的 manifest validation 上限就是 `./dev check task 4-2/task-52`。

六、风险与后续

剩余风险：未做真实 UI 手动点击截图验证；当前证据来自编译和 XCTest 覆盖。`Repair`、远程 provider、fallback、模型下载/训练仍按文档属于后续独立任务。

建议下一个任务按 phase 顺序继续 `4-2/task-53` 或对应 Stage 3 AI 后续 page-feature。