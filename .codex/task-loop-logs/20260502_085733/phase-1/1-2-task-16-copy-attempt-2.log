一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-16`。本次 repair 只改了允许范围内的 Core 文件：
- [api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:292)：`get_file` 回到未实现，并明确 C1-12 才拥有详情查询实现；C1-08 只保留 `list_files` 的 indexed missing-source 证据。
- [domain.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/domain.rs:265)：修正 `FileEntry.path` rustdoc，说明 imported indexed rows 可指向外部 source path。
- [import_index_file_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_implementation.rs:6)：移除越界的 `get_file` 测试和导入，保留 `list_files` missing-source 测试。

三、完成情况：
- C1-08 implementation 范围：通过。
- Exact Docs 作为 SSOT：通过。
- 未实现相邻 C1 / UI：通过，`get_file` 不再实现 C1-12。
- 最小真实路径：通过，indexed import 仍走真实 hash + DB + change_log 路径。
- Forbidden Touches：通过，`git status --short -- apps` 无输出。

四、工程质量：
- 代码结构清晰，没有新增抽象或一次性脚本。
- public rustdoc 已修正 indexed path 语义。
- 错误路径仍由测试覆盖：missing source、duplicate hash、DB failure rollback、无 staging/final 副作用。
- 未新增依赖；未发现 mock-only、硬编码通过态或占位实现替代 C1-08 主路径。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace import_index_file`：通过，C1-08 contract 6 个 + implementation 5 个相关测试通过。
- `cd core && cargo test --workspace`：通过。
- `git diff --check`：通过。
- `git status --short -- apps`：通过，无 Forbidden Touches。

六、风险与后续：
- 剩余风险：本地验证已覆盖 Core；CI / task-loop 最终 verify evidence 仍应由自动流程记录。
- 建议下一个任务：进入 `1-2/task-16` verify-ready；通过后再推进后续 `1-2/task-17`。  
