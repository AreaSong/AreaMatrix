一、执行结论

已完成。

二、执行范围

单任务：`1-2/task-16` repair retry。

本次 retry 修改文件：

- [docs/modules/storage.md](/Users/as/Ai-Project/project/AreaMatrix/docs/modules/storage.md:61)
- [docs/architecture/transactional-import.md](/Users/as/Ai-Project/project/AreaMatrix/docs/architecture/transactional-import.md:22)

说明：这两个文件不在原始 `Expected New Paths`，但上次验收明确指出它们是当前 task 的 Exact Docs / SSOT 漂移阻塞项，且自动执行授权允许修复直接相关 Exact Docs 漂移。未触碰 `apps/**`。

三、完成情况

核对清单全部满足：

- 只处理 C1-08 implementation 范围：通过。
- Exact Docs 作为 SSOT：通过，已把 storage / transactional-import 中 Indexed 的旧 staging/final 语义收敛为 index-only。
- 未实现相邻 C1 能力或 UI：通过。
- 最小真实路径：通过，沿用已存在 C1-08 Rust 实现与测试证据。
- layered design：通过，无 UI / apps 反向依赖。

完成标准：

- C1-08 implementation 目标有证据证明：通过。
- 没有扩展到未绑定能力：通过。
- Forbidden Touches：未触碰，`git diff --name-only -- apps` 为空。

四、工程质量

文档已明确区分 Copy/Move 的 staging/final 流程与 Indexed 的 index-only 流程；`Indexed` 分支现在在文档示例中提前返回，不再 hard-link/copy 到 staging，也不再写最终资料库副本。错误和回滚语义补齐了 Indexed DB 失败只回滚元数据、不改外部源文件的说明。

无新增依赖、无 mock-only、无硬编码通过态；本次没有新增 public Rust API，既有 rustdoc 与实现仍由 core 验证覆盖。

五、验证情况

已通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace import_index_file`：C1-08 contract 6 个 + implementation 5 个通过
- `cd core && cargo test --workspace`：完整 workspace 通过
- `git diff --check`：通过
- `git diff --name-only -- apps`：为空

未运行：

- macOS app 验证：本 task 禁止触碰 `apps/**`，且无 UI 绑定实现范围。

六、风险与后续

剩余风险：未发现当前 task 阻塞项。工作区里仍有前序 attempt 留下的 `.codex` 运行记录、progress 和 core 改动，我没有回退这些既有改动。

建议下一个任务：先进入 `1-2/task-16` 只读 verify-ready 验收；验收 PASS 后再推进 `1-2/task-17`。  
