一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-16`，范围限定在 Core。修改/新增文件清单：
- `core/src/storage/import.rs`
- `core/src/api.rs`
- `core/tests/import_index_file_implementation.rs`
- `core/tests/import_copy_file_implementation.rs`
- `core/tests/import_copy_file_integration_verify.rs`
- `core/tests/import_move_file_integration_verify.rs`

三、完成情况：
- 已实现 C1-08 Indexed import：只读取源文件 metadata/hash，不复制、不移动、不删除源文件。
- DB 写入 `files.storage_mode = indexed`、保留 `source_path`、写入 `change_log.imported`。
- `get_file` / `list_files` 对 Indexed 源文件缺失返回 `FileNotFound`，支持可恢复错误展示。
- 未实现相邻 UI 或其他 C1 能力。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量：
- 实现复用现有 import 路径、DB staging row guard 和显式错误传播，没有 mock-only、硬编码通过态或一次性脚本逻辑。
- Indexed 路径没有 staging/final file 副作用；失败路径覆盖 DB rollback、重复 hash、缺失源文件。
- 没有新增依赖；public API rustdoc 已覆盖 C1-08 行为和错误边界。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过
- `cd core && cargo test --workspace import_index_file`：通过，12 个相关测试通过
- 额外补跑 `cd core && cargo test --workspace`：通过

六、风险与后续：
- 剩余风险：未跑 macOS UI 侧验证，当前 task 无 UI 绑定且 `apps/**` 禁止触碰。
- 建议下一个任务：`1-2/task-17`，继续 C1-08 failure / edge 范围验证。

