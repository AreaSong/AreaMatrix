一、执行结论：已完成。

二、执行范围：单任务 `2-2/task-18`。本轮修复只落在允许范围内：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`。未触碰 `core/src/**`。

三、完成情况：
- 补齐 S1-20 到 S1-21 的结果退出闭环：新增 `importResult` route、`ImportResultRouteState`、`ImportResultView`。
- `Stop and view results`、`Stop after current file`、批量/文件夹导入需要结果摘要时，现在进入 `S1-21 import-result`，不再用 toast 伪闭环返回主窗口。
- 保留 C1-06 / C1-07 / C1-08 真实 CoreBridge import 路径，未新增 control map 外 Core 能力。
- 新增/调整测试覆盖 fatal stop、stop-after-current、安全结果页摘要与 Done 返回主窗口。

四、工程质量：
- 代码结构按现有 `OnboardingModel.Route`、`ImportProgressRouteState`、SwiftUI view 模式扩展，没有新增依赖。
- 错误/停止/失败结果都显式保留 imported / failed / stopped / pending 计数。
- 未引入 mock-only、硬编码通过态或测试专用生产分支。
- `git diff --check` 对本轮相关文件通过；文件长度已控制在规范阈值内。

五、验证情况：
- 已运行任务要求命令：`xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 结果：失败于本地 sandbox 的 `com.apple.testmanagerd.control` 通信限制，不是编译或断言失败。
- 已运行 fallback：`./dev test macos`
  - 结果：通过；`xcrun xctest` 执行同一 XCTest bundle，`249 tests, 0 failures`。
- 已运行：`git diff --check -- <本轮相关文件>`
  - 结果：通过。
- 已确认：`git diff --name-only -- core/src` 无输出。

六、风险与后续：
- 剩余风险：当前环境无法让原始 `xcodebuild test` 直接完成 test runner 通信；已按 repo 规则用 fallback 补足本地验证证据。
- 建议下一个任务：进入 `2-2/task-18` verify-ready 只读验收，重点复核 S1-20/S1-21 路由证据和 `Forbidden Touches`。当前 worktree 仍有本任务前序 retry 留下的其他改动与 task-loop 日志，我没有回滚。 

