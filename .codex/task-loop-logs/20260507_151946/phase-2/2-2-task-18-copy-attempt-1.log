一、执行结论

已完成 `2-2/task-18`。S1-20 的 `Stop after current file` 不再是 UI 立即硬切到 stopped，而是通过页面级协作停止状态，在当前 `CoreBridge.import_file` 返回后停止后续队列，避免中断正在进行的 Core 导入。

二、执行范围

修改的产品/测试文件均在允许范围内：

- `apps/macos/AreaMatrix/Models/*`
- `apps/macos/AreaMatrix/Views/Main/*`
- `apps/macos/AreaMatrixTests/ImportProgressCopyPageFeatureTests.swift`

另外存在 task-loop 运行副产物：`tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260507_151946/summary.json`。这些不是产品实现改动。

三、完成情况

- 核对 page-feature task：已围绕 S1-20 的 C1-06/C1-07/C1-08 导入闭环补齐整页 wiring。
- 页面状态/入口/退出/错误态/CoreBridge：已打通 `MainWindow -> ImportEntrySheetView -> batch/folder footer -> import model -> OnboardingModel route`。
- mock/fixture/硬编码：移除了“点击 stop 立即进入 stopped”的硬编码式通过态，改为 Core 单文件导入完成后的安全停止。
- control map 范围：未新增 C1-06/C1-07/C1-08 之外能力。
- Forbidden Touches：未触碰 `core/src/**`，也未修改 `core/area_matrix.udl`。

四、工程质量

代码结构保持在现有 SwiftUI/Model 分层里：新增 `ImportProgressControlState` 承载停止请求，批量和文件夹导入循环只在单个 Core 导入完成后检查停止信号。错误路径和已有失败映射没有被绕过；生产代码没有引入 mock-only wiring、fixture-only workflow 或测试专用分支。

High 风险记录：影响面是导入队列停止行为；风险点是不能中断正在处理的用户文件导入、不能直接改动用户原文件。当前实现只停止后续队列，不触碰 Core 文件写入逻辑。回滚方式是 revert 本次 macOS app/test 文件改动，以及按需清理 task-loop 运行元数据。

五、验证情况

- `git diff --check`：通过。
- 任务要求的 `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：构建进入测试阶段，但本机 sandbox 阻断 `testmanagerd.control`，无法用 xcodebuild 完成 test runner 通信。
- fallback：复用构建出的 XCTest bundle 运行 `xcrun xctest`，`AreaMatrixTests.xctest` 235 个测试全部通过，0 failures。

六、风险与后续

剩余风险主要是 `xcodebuild test` 在当前沙箱环境下无法完成原生命令验收；产品代码已通过直接 XCTest fallback。建议下一个动作是运行 verify-ready 的 `2-2/task-18` 只读验收，确认 task + manifest + 实际文件三方闭环。  
