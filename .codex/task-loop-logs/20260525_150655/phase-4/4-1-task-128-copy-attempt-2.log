一、执行结论

已完成。

二、执行范围

单任务：`4-1/task-128`。本次 repair 只修复上次验收指出的 `ClassifierCorrectionHandoffTests` 失败，实际修复点集中在：

- `apps/macos/AreaMatrix/Models/MainFileCategoryMoveState.swift`
- `apps/macos/AreaMatrix/Views/Main/ClassifierRuleHandoffRouteView.swift`
- `apps/macos/AreaMatrixTests/ClassifierCorrectionHandoffTests.swift`

三、完成情况

- 核对清单：仍只处理 S2-17 + C2-13 classifier rule save。
- 修复内容：把 S2-17 handoff 摘要抽成稳定 `summaryRows` 数据源，UI 使用同一数据渲染，测试不再依赖 SwiftUI `Mirror`；保存完成测试改为先进入真实选择文件 -> S2-16 -> S2-17 route，再验证 `statusBanner`。
- 完成标准：S2-17 保存成功状态可验收；真实 CoreBridge 保存测试仍覆盖 `.areamatrix/classifier.yaml` 写入且不重分类当前文件。
- Forbidden Touches：未触碰。`git diff --name-only -- core/src` 无输出。

四、工程质量

结构清晰，没有新增依赖、mock-only、硬编码通过态或一次性脚本。错误处理和 CoreBridge 保存路径沿用既有实现；本次 repair 只补稳定展示数据与测试状态前置条件。未改 Rust public API，因此无 rustdoc 变更。

五、验证情况

已运行并通过：

```bash
./dev test macos --only-testing AreaMatrixTests/ClassifierCorrectionHandoffTests
```

结果：通过。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 阻断，脚本按仓库规则复用已构建 XCTest bundle，`xcrun xctest` 通过：5 tests, 0 failures。

```bash
./dev check task 4-1/task-128
```

结果：通过。包含 `doctor: OK`、`git diff --check`、macOS build，最终 `** BUILD SUCCEEDED **`。

六、风险与后续

剩余风险：未做真实窗口点击 smoke，不在当前 manifest validation 上限内；远端 CI 未运行。建议下一个任务进入 verify-ready 验收本 task，若通过再标记 completed。

