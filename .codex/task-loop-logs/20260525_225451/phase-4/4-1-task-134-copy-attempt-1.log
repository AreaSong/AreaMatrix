一、执行结论

已完成。

二、执行范围

单任务：`4-1/task-134`。实现集中在 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**`：C2-17 的 `previewImportConflictBatch` / `applyImportConflictBatch` CoreBridge 接入、S2-21 批量冲突策略 UI/状态/错误映射、命令面板/导入进度入口，以及对应测试覆盖。

三、完成情况

- 核对清单：已按 S2-21、C2-17、Stage 2 control map、Core API 交叉实现。
- 完成标准：S2-21 的 C2-17 用户可见功能可触发；生产路径默认走 `CoreBridge()`，没有 mock-only 或静态状态伪造真实 Core 闭环。
- 同页 C2-07 未顺手实现，仅保留 C2-17 返回的 undo/change log 状态展示。
- Forbidden Touches：未触碰 `core/src/**`，未修改 `core/area_matrix.udl`，最终 `.xcodeproj` 无 diff。

四、工程质量

修复了上次 verify 的 P1 blocker：`MainEmptyImportEntryTests.swift` 从 532 行压回 449 行，S2-21 路由测试迁移到已纳入 target 的 `MainEmptyBuildTreeTests.swift`，所有相关文件均不超过 500 行。错误处理走 `CoreErrorMapping`，Replace 保持二次确认，Pending/Blocked/Failed 行不被本地误处理。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-134
```

结果：`doctor: OK`、`git diff --check` 通过、macOS `xcodebuild ... build CODE_SIGNING_ALLOWED=NO` 最终 `BUILD SUCCEEDED`。未升级运行 `cargo test --workspace`、`./dev check core` 或 `./dev check all`，符合本 task 验证上限。

六、风险与后续

残余风险：`git add` 被沙箱拒绝，错误为无法创建 `.git/index.lock`，所以当前工作树已是最终通过状态，但 Git index 里仍有旧 staged 版本痕迹；后续 checkpoint/提交前需要重新 stage。建议下一步运行 verify-ready 验收或进入 `4-1/task-135`。

