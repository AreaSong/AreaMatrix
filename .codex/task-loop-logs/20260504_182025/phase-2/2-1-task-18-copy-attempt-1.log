**一、执行结论**

已完成。

说明：任务要求的原始 `xcodebuild test` 已运行，但被当前本机 sandbox 的 `com.apple.testmanagerd.control` 限制阻断；按 repo-local validation driver，已用 `scripts/check-macos-tests.sh` fallback 跑完同一 XCTest bundle，65 tests / 0 failures。

**二、执行范围**

单任务：`2-1/task-18 first-launch integration verify`

主要改动：
- 拆分首次启动 onboarding 视图，降低 `MainWindow.swift` 体量。
- 将已初始化 repo 分支从“直接进入 loading”改为真实调用 Core open path，成功后再保存 repo path。
- 为 init-failed 增加 Core diagnostics snapshot wiring 和失败映射。
- 补齐/整理 macOS 测试 helper 与集成测试，覆盖 S1-01 到 S1-07 闭环。

修改范围：
- `apps/macos/AreaMatrix/**`
- `apps/macos/AreaMatrixTests/**`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`
- task-loop evidence/progress：`.codex/task-loop-*`、`tasks/prompts/_shared/progress.json`

**三、完成情况**

核对清单：
- 已完成：只做 first-launch 已有页面与能力之间的 wiring / 验收补齐。
- 已完成：`2-1/task-01` 到 `2-1/task-17` 在 `progress.json` 中均为 `completed`；当前 `2-1/task-18` 仍是 `in_progress`，等待 runner/verify 后续标记。
- 已完成：生产闭环未以 mock-only 通过；保留的 fixture/helper 仅在测试内使用。
- 已完成：未新增 control map 外页面或 Core 能力。

完成标准：
- 用户路径 S1-01 到 S1-07 已通过 macOS 单元/集成测试证据覆盖。
- 失败项有证据：原始 `xcodebuild test` 失败原因是本机 testmanagerd sandbox，不是编译或测试断言失败。

Forbidden Touches：
- 未触碰 `core/src/**`。
- `core/area_matrix.udl` 也无差异。

**四、工程质量**

结构上把大视图拆成独立 onboarding view / snapshot / diagnostics state，当前非生成 Swift 文件未超过 500 行。错误路径显式处理：打开已有 repo 失败进入 `mainRepoError`，诊断收集失败走 C1-21 error mapping。未新增依赖，未改 Rust public API，因此无 rustdoc 同步项。

**五、验证情况**

已运行：
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`：PASS。
- `git diff --check -- apps/macos/AreaMatrix.xcodeproj/project.pbxproj apps/macos/AreaMatrix apps/macos/AreaMatrixTests`：PASS。
- `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`：PASS，`BUILD SUCCEEDED`。
- `xcodebuild test ... CODE_SIGNING_ALLOWED=NO`：FAIL/BLOCKED，原因是 `com.apple.testmanagerd.control` sandbox restriction。
- `bash scripts/check-macos-tests.sh`：PASS，fallback `xcrun xctest` 执行 65 tests，0 failures。
- `core/src` / `core/area_matrix.udl` diff/status 检查：无输出，无改动。

**六、风险与后续**

剩余风险：当前没有做人工 UI 点击截图验证；证据来自 build 和 XCTest。原始 manifest test 在这个 sandbox 环境无法直接完成，但 repo-local fallback 已覆盖本地测试门禁。

建议下一个任务：让 task-loop 执行 verify-ready，把 `2-1/task-18` 从 `in_progress` 收到 `completed`，再进入 `2-1/task-19`。

