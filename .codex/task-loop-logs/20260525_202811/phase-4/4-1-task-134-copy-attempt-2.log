一、执行结论：已完成。

二、执行范围：单任务 `4-1/task-134`。修改集中在 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**`，未触碰 `core/src/**` 或 `core/area_matrix.udl`。主要文件包括：
`CoreImporting.swift`、`ImportBatchConflictActions.swift`、`ImportBatchCopyImport*`、`ImportBatchProgressItems.swift`、`ImportEntryRequest.swift`、`OnboardingModelInputs.swift`、`ImportBatchConflictSection.swift`、`ImportEntrySheetView.swift`、`MainRepositoryContentView.swift`、`MainRepositoryMultiSelectionActions.swift`、`MainWindow.swift`，以及测试 `ImportSingleFileNameConflictCoreTests.swift`、`MainEmptyImportEntryTests.swift`。`tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-*` 是 task-loop 运行状态/日志痕迹，不是产品功能实现。

三、完成情况：
- 核对清单 1：通过，只处理 S2-21 中 C2-17。
- 核对清单 2：通过，已按 S2-21、Stage 2 control map、Core API、C2-17 能力规格交叉实现。
- 核对清单 3：通过，通过 CoreBridge 接入 `previewImportConflictBatch` / `applyImportConflictBatch`，未用 mock 伪造生产闭环。
- 核对清单 4：通过，未实现同页 C2-07 或相邻页面能力。
- 完成标准 1：通过，S2-21 可从真实 active import conflict metadata 打开批量冲突 review。
- 完成标准 2：通过，生产默认走 `CoreBridge()`；测试注入只用于记录请求形状。
- 完成标准 3：通过，未触碰 control map 外 Core 能力。
- Forbidden Touches：未触碰 `core/src/**`。

四、工程质量：
- 修复了上次 P0：`MainRepositoryMultiSelectionActions` 现在只在 active import progress 中存在真实 `ImportConflictBatchProgressMetadata` 时打开 S2-21；`OnboardingModelInputs` 会把真实 `importSessionID` / `conflictIDs` 传入 sheet。
- 修复了上次 P1：`ImportBatchConflictActions` 的 Apply / Ask 判定按可操作行计算，`Pending` / `Blocked` / `Failed` 不再阻断其他可执行 included rows。
- 新增测试覆盖“有真实 route 才能打开”“无 route 不伪造”“route-backed request 创建”“部分 blocked 行不禁用可操作 scope”。
- 未新增 Rust public API，无需 rustdoc；未新增依赖；没有一次性硬编码通过态。

五、验证情况：
- 已运行：`./dev check task 4-1/task-134`
- 通过：`prompt doctor` OK，`git diff --check` 通过，`xcodebuild ... build CODE_SIGNING_ALLOWED=NO` 为 `BUILD SUCCEEDED`
- 失败：无
- 未运行：未升级到 `cargo test --workspace`、`./dev check core` 或 `./dev check all`，因为当前 manifest 验证上限就是 `./dev check task 4-1/task-134`。

六、风险与后续：
- 剩余风险：真实 Core 中 partial-blocked 的服务端 `can_apply` 语义如仍与 S2-21 文档不同，需要后续 Core 任务处理；本 task 禁止触碰 `core/src/**`，本次只修 UI 侧多余阻断。
- 建议下一个任务：进入该 task 的 verify-ready 验收；若通过，再推进同页其他能力或 page integration verify。

