一、执行结论

已完成 `4-3/task-120`。

二、执行范围

单任务：仅实现 `S4-WIN-02 main-window` 中 `C4-11 desktop-main-query` 的 Windows app 接入。

修改范围在 `apps/windows/**`，未触碰 `core/src/**`。主要新增/修改：

- `apps/windows/AreaMatrix/Features/Library/*`
- `apps/windows/AreaMatrix/Core/*`
- `apps/windows/AreaMatrix/MainWindow.xaml`
- `apps/windows/AreaMatrix/MainWindow.xaml.cs`
- `apps/windows/AreaMatrixTests/DesktopMainQuery/*`
- `apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj`
- `apps/windows/AreaMatrixTests/Program.cs`

三、完成情况

核对清单通过：

- 只处理 `S4-WIN-02 + C4-11`。
- 已读取页面规格、control map、Core API、C4-11 能力规格。
- 已接入 `list_files`、`get_file`、`list_tree_json`、`search_files` 的 Windows CoreBridge/native binding/state/error mapping。
- 未实现导入、watcher、rescan、OneDrive notice、缺失恢复、冲突处理等同页或相邻能力。

完成标准通过：

- Windows 主窗口可从 choose-repo 成功路由进入，并触发 C4-11 的列表、树、搜索、详情查询。
- 没有用 mock/fixture 伪造生产闭环；生产路径走 `DesktopMainQueryCoreBridge(coreClient)` 和 native Core 函数。
- 未触碰 control map 之外 Core 能力。
- Forbidden Touches：`core/src/**` 无 diff。

四、工程质量

代码按 bridge/model/view 分层，C4-11 native 编解码拆到 partial 文件，文件长度均低于 500 行。错误映射覆盖 `Db`、`RepoNotInitialized`、`FileNotFound`、`InvalidPath`、`PermissionDenied` 等查询相关状态；Refresh 错误保留缓存列表，不触发 rescan 或写 DB。无新增依赖，无 Rust public API 变化，无文档同步需求。

五、验证情况

已通过：

- `dotnet run --no-restore --project apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj`
- `./dev check task 4-3/task-120`

额外尝试但受环境阻塞：

- `dotnet build apps/windows/AreaMatrix/AreaMatrix.Windows.csproj`
- `dotnet build --no-restore apps/windows/AreaMatrix/AreaMatrix.Windows.csproj`

阻塞原因是 NuGet 源加载失败：`NU1301`，`System.Net.CookieContainer` initializer 抛错，`GetDomainName: -1`。这不是当前 task 代码路径的失败；Windows C# 测试入口已能编译并执行。

六、风险与后续

剩余风险：当前 macOS 环境无法完整验证 WinUI XAML app build/launch，需要在 Windows/可用 NuGet 环境补跑 WinUI app build 和启动验证。

建议下一个任务：进入 `4-3/task-121` 前，先在 Windows 环境补一次 `dotnet build apps/windows/AreaMatrix/AreaMatrix.Windows.csproj`，确认 XAML 编译和 Windows App SDK runtime 路径。