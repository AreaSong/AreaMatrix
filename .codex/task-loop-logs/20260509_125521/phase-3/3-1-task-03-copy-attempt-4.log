一、执行结论

- **未完成 / BLOCKED**。
- 已补齐 Stage 1 性能基线的大部分可重复证据，并修复 Rust bench 验证命令漂移。
- 仍不能判定任务通过：`应用启动到首屏 < 1.5s` 在当前 sandbox 环境没有合规实测值，且标准 `xcodebuild test` 仍因 `com.apple.testmanagerd.control` sandbox restriction 失败。

二、执行范围

修改/新增主要文件：

- `core/tests/stage1_performance_baseline.rs`
- `apps/macos/AreaMatrixTests/CoreBridgeRepositoryTests.swift`
- `scripts/dev_tools/cli.py`
- `scripts/dev_tools/macos.py`
- `scripts/dev_tools/macos_launch_probe.swift`
- `docs/development/stage-1-performance-baseline.md`
- `tasks/prompts/phase-3/3-1-stability-release/task-03-performance-benchmarks.md`
- `tasks/prompts/_shared/manifests/phase-3.md`
- `tasks/prompts/_shared/copy-ready/phase-3/3-1-task-03.md`
- `tasks/prompts/_shared/verify-ready/phase-3/3-1-task-03.md`

三、完成情况

- 已覆盖 testing / performance / observability / Stage 1 MVP 文档读取与对齐。
- 已形成基线表，记录环境、数据集、入口、阈值、实测和结论。
- 已覆盖单文件导入、100 文件批量导入、reindex、Tree/list、内存。
- 启动到首屏仍为 `BLOCKED`，不能放行 release checklist。
- 未触碰 Forbidden Touches；没有改产品逻辑或用户文件路径。
- Rust bench 命令已从 root `cargo bench --workspace --no-run` 修正为 `cargo bench --manifest-path core/Cargo.toml --workspace --no-run`。

四、工程质量

- Core perf 使用 release ignored integration test，数据集走临时目录。
- Swift perf 使用 `AreaMatrixPerfTests`，hostless fallback 会跳过启动 case，避免伪造首屏启动证据。
- `./dev test macos --only-testing ...` 增加过滤执行和 Release `.app` launch probe。
- 没有新增依赖，没有 mock-only 或硬编码通过态。
- baseline 文档明确保留 P1 阻断，不把 fallback 当成真实启动通过证据。

五、验证情况

通过：

- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`
- `python3 tasks/prompts/_shared/prompt_pipeline.py status`
- `python3 tasks/prompts/_shared/prompt_pipeline.py render --task 3-1/task-03 --mode verify`
- `cargo fmt --manifest-path core/Cargo.toml --all -- --check`
- `cargo clippy --manifest-path core/Cargo.toml --all-targets --all-features -- -D warnings`
- `cargo bench --manifest-path core/Cargo.toml --workspace --no-run`
- `cargo test --manifest-path core/Cargo.toml --workspace`
- `cargo test --manifest-path core/Cargo.toml --release --test stage1_performance_baseline -- --ignored --test-threads=1 --nocapture`
- `PYTHONPYCACHEPREFIX=/tmp/areamatrix-pycache python3 -m py_compile scripts/dev_tools/*.py`
- `git diff --check`

失败 / 阻断：

- 标准 `xcodebuild test ... AreaMatrixPerfTests ... CODE_SIGNING_ALLOWED=NO` 失败，code 65，原因是 `com.apple.testmanagerd.control` sandbox restriction。
- `./dev test macos --only-testing AreaMatrixTests/AreaMatrixPerfTests` 中非启动 perf tests 通过，但追加的 Release app launch probe 失败：`The application “AreaMatrix” could not be launched because it is corrupt.`

六、风险与后续

剩余 P1 风险只有启动到首屏性能证据。下一步应在 CI 或非 sandbox 本机补跑标准 XCTest performance / Instruments；如果真实启动超过 1.5s，再回到 Phase 1/2 的 startup recovery、`OnboardingModel.bootstrapIfNeeded()`、`CoreBridge.openConfiguredRepository` 和首屏 tree/list loading 路径做 profile。