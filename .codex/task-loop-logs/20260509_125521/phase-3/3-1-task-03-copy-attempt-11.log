一、执行结论

已完成。

二、执行范围

单任务：`3-1/task-03` repair retry。

主要修改：
- 补齐 Stage 1 性能基线文档、Rust bench/release perf、macOS XCTest perf。
- 将 `core/benches/**` 和 `apps/macos/AreaMatrix.xcodeproj/project.pbxproj` 正式纳入 `3-1/task-03` source manifest 的 `Expected New Paths`。
- 同步刷新 `3-1/task-03` copy-ready / verify-ready，使 task、manifest、导出验收材料一致。
- 修复 `./dev status/preflight` 在 sandbox 禁止 `ps` 时崩溃的问题，并隔离 `task-loop check` 的 fake-codex temp repo runtime env。

三、完成情况

核对清单已覆盖：testing / performance / observability / Stage 1 MVP 已读取；性能基线覆盖启动、单文件导入、100 文件批量导入、reindex、Tree/list、内存；每项指标有环境、数据集、入口、阈值、实测和结论。

Forbidden Touches：无，未触碰。

`.codex/task-loop-logs/20260509_125521/` 和 `.codex/task-loop-runs/20260509_125521/` 是本轮 task-loop 外部运行证据，仍为 untracked，不作为产品/任务文件改动提交。

四、工程质量

代码没有改产品业务逻辑；新增内容集中在 benchmark、测试、脚本验证和 `docs/development/**` 证据。真实 `.app` 启动证据缺失没有被 hostless fallback 掩盖，仍保留为 P1 release blocker。没有新增依赖。

五、验证情况

已通过：
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`
- `python3 tasks/prompts/_shared/prompt_pipeline.py status`
- `cargo bench --manifest-path core/Cargo.toml --workspace --no-run`
- `cargo test --manifest-path core/Cargo.toml --release --test stage1_performance_baseline -- --ignored --test-threads=1 --nocapture`
- `./dev test macos --only-testing AreaMatrixTests/AreaMatrixPerfTests`
- `PYTHONPYCACHEPREFIX=/private/tmp/areamatrix-pycache python3 -m py_compile scripts/dev_tools/*.py scripts/task_loop/*.py`
- `python3 -m unittest scripts.dev_tools.test_macos_runner`
- `./dev check prompts`
- `./dev check skills`
- `PYTHONPYCACHEPREFIX=/private/tmp/areamatrix-pycache ./task-loop check`
- `git diff --check`
- task-03 live render 与静态 copy/verify prompt diff 一致

关键实测：
- Rust 100 文件导入 + list：930 ms / 5s PASS
- Rust 1 MiB import：13 ms / 30ms PASS
- Rust 10k reindex：14.577s / 30s PASS
- Rust Tree：5 ms / 30ms PASS
- Rust list 200：632 us / 5ms PASS
- macOS perf tests：5 tests 全通过
- 真实 Release `.app` launch：当前 sandbox 阻断，release checklist 继续 BLOCKED

六、风险与后续

剩余风险只有一个：非 sandbox 环境下真实 `.app` 启动到首屏 / Instruments launch 证据仍未补齐，所以 Stage 1 release checklist 不能放行。建议下一个任务 `3-1/task-04` 继续处理 release checklist，并在非 Codex sandbox 的 macOS 桌面会话或 CI runner 补跑真实 launch / Instruments 证据。

