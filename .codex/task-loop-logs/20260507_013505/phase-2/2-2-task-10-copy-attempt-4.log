一、执行结论

- 已完成

二、执行范围

- 单任务：`2-2/task-10`
- 产品修改集中在 `apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`
- 另有 task-loop 运行证据文件变动：`.codex/task-loop-*`、`tasks/prompts/_shared/progress.json`

主要修改文件：
- `apps/macos/AreaMatrix/Models/ImportBatchDuplicatePrecheck.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchPreviewModel.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrixTests/ImportDropPreviewModelTests.swift`
- `apps/macos/AreaMatrixTests/ImportBatchPrecheckTestSupport.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况

- page-feature task 覆盖：通过。S1-18 的 `C1-05 / C1-06 / C1-09` 页面闭环已串起，重复与重名冲突预检不再依赖硬编码空结果。
- 页面状态、入口、退出、错误态和 CoreBridge 调用闭环：通过。批量预览现在通过 `CoreBridge.listFiles` 读取真实 Core 文件元数据，重复检测计算源文件 SHA-256，重名检测按目标相对路径匹配。
- mock / fixture / 硬编码状态移除：通过。`CoreImportBatchNameConflictPrechecker` 不再返回 `[:]`，`ImportBatchPreviewModel` 已保存并调用 name conflict prechecker。
- 不新增 control map 外功能：通过。修复仍限定在 S1-18 声明的 `C1-05 / C1-06 / C1-09`。
- Forbidden Touches：未触碰。`git status --short -- core/src` 无输出。

四、工程质量

- 代码结构：通过。预检抽成 `ImportBatchCoreFileLoading`，生产路径走 `CoreBridgeBatchFileLoader`，测试用注入 loader，不引入 mock-only 生产分支。
- 错误处理：通过。重复/重名预检失败会映射到 row error，不静默吞错；iCloud placeholder 仍作为不可导入预检态处理。
- 边界处理：通过。预检只读 Core 元数据和源文件 hash，不删除、移动、覆盖用户文件。
- 占位/硬编码：未发现当前 blocker 中的硬编码空闭环残留。
- 文档同步：本次未改文档；实现按已读 page spec、control map、Core capability specs 对齐。

五、验证情况

- 已运行：`xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 结果：失败，退出码 65。
  - 原因：本机 sandbox 阻断 `com.apple.testmanagerd.control`，错误为 `Connection init failed at lookup with error 159 - Sandbox restriction`。构建/链接阶段已走到测试 runner 通信失败点，不是代码编译失败。
- 已运行：`./dev test macos`
  - 结果：通过。
  - 证据：脚本识别同类 `testmanagerd` sandbox 限制后复用已构建 XCTest bundle，`xcrun xctest` 执行 `206 tests, 0 failures`。
- 新增关键回归：`testBatchPreviewUsesRealCoreMetadataForDuplicateAndNameConflictPrecheck`，覆盖真实 Core 元数据 loader 下的 `DUP` / `NAME` 预检输出。

六、风险与后续

- 剩余风险：未取得原始 manifest `xcodebuild test` 的直接 PASS，原因是当前环境 sandbox 限制；已用仓库认可的 macOS fallback 完成测试验证。
- 建议下一个任务：进入 `2-2/task-10` verify-ready 只读验收，重点核对 S1-18 整页闭环、`DUP/NAME` 证据、Forbidden Touches 和验证日志。

