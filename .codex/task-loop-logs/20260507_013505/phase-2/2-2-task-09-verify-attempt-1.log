一、验收结论

- 不通过

二、验收范围

- 单任务：`2-2/task-09`
- 对应文件路径：`tasks/prompts/phase-2/2-2-import-conflict/task-09-s1-18-c1-09-detect-duplicate.md`
- 重点核验：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`、`core/area_matrix.udl`
- 禁止触碰：`core/src/**`

三、完成度摘要

- 已覆盖项数：12
- 未覆盖项数：0
- 通过项数：9
- 不通过项数：3
- 阻塞项数：2

四、逐项验收结果

- 项目：Exact Docs / Existing Code 读取  
  结果：通过  
  证据：4 个 Exact Docs、`apps/macos/AreaMatrix/**`、`core/area_matrix.udl` 均已读取并交叉核验。

- 项目：Expected New Paths  
  结果：通过  
  证据：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj` 均存在。

- 项目：Forbidden Touches  
  结果：通过  
  证据：`git diff --name-only -- core/src` 无输出，未发现 `core/src/**` 改动。

- 项目：只处理 S1-18 页面中的 C1-09 功能点  
  结果：通过  
  证据：新增 C1-09 用户面集中在 S1-18 batch sheet 的重复预检、DUP 行、Review conflicts 和 duplicate strategy；未发现相邻页面新增实现。

- 项目：读取页面规格、control map、Core API、C1-09 能力规格  
  结果：通过  
  证据：已核对 S1-18、`mvp-control-map.md`、`core-api.md`、`C1-09-detect-duplicate.md`。

- 项目：只接入 C1-09 对应 CoreBridge / 状态 / 错误映射  
  结果：不通过  
  证据：用户在冲突表选择 `Keep both` 后，点击 Import 会先重跑 `prepareImport()`，重新 `applyPreviewRows()`，把 DUP 行策略重置为 `.skip`，导致用户选择无法进入 Core `import_file`。

- 项目：不顺手实现相邻页面  
  结果：通过  
  证据：未发现 S1-19/S1-22/S1-24 新页面闭环实现；Replace 仍未越界接入。

- 项目：S1-18 中 C1-09 用户可见功能可被触发和验收  
  结果：不通过  
  证据：同 hash 跨分类重复会被预检漏报；同页 Keep both 选择会被 Import 前重置，用户无法稳定触发对应 Core 策略。

- 项目：没有 mock、fixture 或静态状态伪造真实 Core 闭环  
  结果：通过  
  证据：生产路径使用 `CoreBridge.listFiles`、本地 SHA-256、`CoreBridge.importFile`；测试 fixture 未进入生产路径。

- 项目：未触碰 control map 之外 Core 能力  
  结果：通过  
  证据：相关能力仍在 `S1-18: C1-05, C1-06, C1-09` 范围内；未新增 control map 外 Core API。

- 项目：Manifest 验证命令  
  结果：通过  
  证据：`xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO` 通过，`** BUILD SUCCEEDED **`。

- 项目：validation-driver 扩展验证  
  结果：通过  
  证据：`./dev test macos` 通过；标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 阻断后，脚本按规则复用 XCTest bundle，`192/192` tests passed。

五、工程质量验收

- 代码结构与逻辑：不通过。核心状态链路存在 Import 前重置用户选择的问题。
- 注释 / rustdoc / 文档同步：通过。本 task 未新增 public Rust API，未改 UDL，未发现文档同步缺口。
- 错误处理与边界处理：不通过。跨分类 duplicate 与 Core 全库 hash 语义不一致，预检会漏报。
- 测试与验证覆盖：不通过。现有测试直接调用 model 的 `updateDuplicateStrategy + importReadyFiles`，没有覆盖真实页脚 `prepareImport()` 重置路径。
- 占位、硬编码、mock-only、一次性实现检查：通过。未发现生产路径 mock-only 或硬编码通过态。

六、阻塞项

- P0：Keep both 用户选择在真实 Import 路径中被重置。  
  说明：冲突表 Picker 会写入 `updateDuplicateStrategy`，但 `ImportBatchCopyFooterSection.importBatch()` 先调用 `prepareImport()`，该函数重新 `applyPreviewRows()`；`copyStatus(from:)` 又把 `.duplicate` 固定映射为 `.skip`。因此 UI 上选择 `Keep both` 后点击 Import，实际不会把 `.keepBoth` 传给 Core。  
  绝对路径：`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ImportBatchConflictSection.swift:60`、`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ImportBatchCopyFooterSection.swift:39`、`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ImportBatchCopyFooterSection.swift:56`、`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportBatchCopyImportModel.swift:424`。  
  为什么导致不通过：C1-09 要求 `KeepBoth` 策略继续导入；当前 S1-18 用户可见功能无法真实触发该策略。

- P0：重复预检按预测分类过滤，和 Core 全库 hash 重复语义不一致。  
  说明：`CoreImportBatchDuplicatePrechecker` 用 `predictedCategory` 过滤 `listFiles`，只查某一分类；但 Core 的 duplicate 检测是 `WHERE hash_sha256 = ? AND status = 'active'`，不按分类过滤。  
  绝对路径：`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportBatchPreviewModel.swift:218`、`/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportBatchPreviewModel.swift:228`、`/Users/as/Ai-Project/project/AreaMatrix/core/src/db/import.rs:28`。  
  为什么导致不通过：同 hash 文件在其他分类时，S1-18 的预计重复数量和 Review conflicts 会漏报，最终 Import 才由 Core 抛 DuplicateFile，不能满足“页面中 C1-09 对应用户可见功能可被触发和验收”。

七、验证情况

- 已运行：`xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过。
- 已运行：`./dev test macos`：通过，`xcodebuild test` 因本地 sandbox 阻断 testmanagerd 后，`xcrun xctest` 执行 `192/192` 通过。
- 缺失：未发现能覆盖真实页脚 Import 后保留 duplicate strategy 的测试证据。

八、最终判定说明

- 本次执行尚未达到验收标准，不能视为完成。

VERIFY_RESULT: FAIL