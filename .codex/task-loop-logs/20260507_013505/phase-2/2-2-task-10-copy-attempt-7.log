一、执行结论

已完成。

二、执行范围

单任务：`2-2/task-10` repair retry。  
本次修复主要改动：

- `apps/macos/AreaMatrix/Bridge/CoreImporting.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchCopyImportModel.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportBatchOverviewSections.swift`
- `apps/macos/AreaMatrixTests/ImportBatchCopyImportModelTests.swift`
- `apps/macos/AreaMatrixTests/ImportBatchCopyPageIntegrationTests.swift`
- `apps/macos/AreaMatrixTests/ImportBatchICloudPageIntegrationTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`
- `docs/core/capability-specs/stage-1-mvp/C1-09-detect-duplicate.md`

三、完成情况

- page-feature 覆盖：通过，`S1-18` 仍只覆盖 `C1-05 / C1-06 / C1-09`。
- CoreBridge 闭环：通过，batch import 非 Copy 现在被禁用并在 `CoreBridge.importBatchFile` 拒绝，不再接通真实 Move / Index-only。
- mock / fixture / 硬编码：未新增 mock-only 闭环；测试 fixture 只在测试内使用。
- control map 越界：已修复。
- 文件长度：已修复，`ImportEntrySheetView.swift` 426 行，`ImportBatchCopyPageIntegrationTests.swift` 485 行。
- 文档漂移：已同步 `C1-09` 服务页面，补入 `S1-18 import-batch-sheet`。
- Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 和 untracked 检查均无输出。

四、工程质量

结构上把批量摘要、目标/存储模式、列表拆到 `ImportBatchOverviewSections.swift`，避免单页视图继续膨胀。错误处理保留显式阻断：非 Copy batch 导入会显示禁用原因，生产桥接层也会拒绝非 Copy，避免 UI 绕过。未新增依赖，未新增 public Rust API。

五、验证情况

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：最终编译通过，但测试执行被本地 `com.apple.testmanagerd.control` sandbox restriction 阻断，退出 65。
- `./dev test macos`：通过；fallback 用 `xcrun xctest` 执行同一 XCTest bundle，213 tests，0 failures。
- `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`：通过。
- `python3 tasks/prompts/_shared/prompt_pipeline.py audit --pages`：通过，`S1-18` 为 OK。
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`：通过，637 tasks / 637 manifests。
- `swiftformat --lint ...`：无法运行，当前环境缺少 `swiftformat`。
- `swiftlint --strict`：无法运行，当前环境缺少 `swiftlint`。

六、风险与后续

剩余风险主要是原始 `xcodebuild test` 在当前沙箱不能直接完成 testmanagerd 通信，已用仓库认可 fallback 补证。建议下一个任务继续进入 `2-2/task-11`。