一、执行结论

- 已完成

二、执行范围

- 单任务：`2-2/task-09`
- 本次 repair 直接修复：
  - [ImportBatchPreviewModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportBatchPreviewModel.swift:185)
  - [CoreFileListing.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Bridge/CoreFileListing.swift:30)
  - [ImportEntrySheetView.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift:24)
  - [ImportDropPreviewModelTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ImportDropPreviewModelTests.swift:216)

三、完成情况

- 核对清单：通过。只处理 S1-18 的 C1-09 duplicate precheck，没有实现相邻页面或其他 Core 能力。
- 完成标准：通过。批量预览现在会在预览阶段触发真实 duplicate precheck，生成 `DUP` 行、预计重复数量和 Review conflicts 消费状态。
- 真实闭环：通过。默认产品路径使用 `CoreBridge`，通过源文件 SHA-256 + `listFiles` 读取 Core DB 中的真实 hash/path 证据，不再只等点击 Import 后捕获 `DuplicateFile`。
- Forbidden Touches：未触碰。`git diff -- core/src` 无输出；`core/area_matrix.udl` 未修改。

四、工程质量

- 代码结构清晰：通过。新增 `ImportBatchDuplicatePrechecking` 注入点，产品默认实现仍是 `CoreBridge`，测试可替换为 recording prechecker。
- 错误与边界处理：通过。hash/listFiles/precheck 错误会进入现有 row error 映射，预览中仍禁用 Import。
- 占位 / mock-only：通过。测试有 fake，但产品默认路径不是 fake；新增真实 CoreBridge 集成测试证明预检不写入 incoming 文件、不新增 DB 行。
- 依赖：未新增依赖。
- 格式风险：`swiftformat`、`swiftlint` 当前 PATH 不存在，未能运行。

五、验证情况

- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过，`BUILD SUCCEEDED`。
- `./dev test macos`：通过。标准 `xcodebuild test` 被本地 `testmanagerd` 沙箱阻断后，脚本按仓库规则用 `xcrun xctest` 执行，196 tests，0 failures。
- `git diff --check`：通过。
- `python3 tasks/prompts/_shared/prompt_pipeline.py verify --task 2-2/task-09`：已生成 verify-ready prompt。

六、风险与后续

- 剩余风险：未做真机 UI 手工点击验证；SwiftFormat/SwiftLint 未运行，因为本机 PATH 找不到工具。
- 建议下一步：进入 `2-2/task-09` 的只读 verify-ready 验收；通过后再进入 `2-2/task-10`。