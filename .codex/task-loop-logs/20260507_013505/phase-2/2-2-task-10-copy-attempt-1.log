一、执行结论

已完成。

二、执行范围

修改了 S1-18 批量导入整页闭环相关 macOS 代码与测试：

- `apps/macos/AreaMatrix/Views/Main/ImportBatchCopyFooterSection.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrix/Views/MainWindow.swift`
- `apps/macos/AreaMatrix/Models/OnboardingModelInputs.swift`
- `apps/macos/AreaMatrix/Models/ImportProgressRouteState.swift`
- `apps/macos/AreaMatrixTests/ImportBatchCopyPageIntegrationTests.swift`

工作区里原本已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是未提交状态；本轮没有回退它们。

三、完成情况

- `2-2/task-07/08/09` 对应的 C1-05/C1-06/C1-09 已在 S1-18 中串起来：分类预览、重复检测、copy 导入、Duplicate KeepBoth/Skip、错误映射都有测试证据。
- 修复了整页 integration 缺口：批量 Import 现在会进入 `S1-20 import-progress` 路由，并传递 batch completed/failed/remaining/currentPath。
- 批量导入失败会保留在 progress 路由并显示 mapped Core error，不再停留在 sheet 内静默失败。
- 全部 DUP Skip 时不会打开空的 progress route，因为没有文件进入 Core 导入队列。
- 未新增 control map 之外能力。
- 未触碰 Forbidden Touches：`core/src/**` 未修改。

四、工程质量

代码保持在现有 SwiftUI/Model 分层里：sheet footer 只负责发出 progress/failure 回调，`OnboardingModel` 负责路由状态，`ImportProgressRouteState` 负责展示 batch 计数。没有新增依赖、没有 mock-only 通过态、没有硬编码成功路径。错误路径继续使用 `CoreErrorMappingSnapshot`，批量失败状态和进度都有测试覆盖。

五、验证情况

已运行：

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 编译完成，但本机 sandbox 阻止 `testmanagerd.control`，raw xcodebuild 以 65 退出。
- `xcrun xctest apps/macos/DerivedData/AreaMatrix/Build/Products/Debug/AreaMatrixTests.xctest`
  - 通过，199 tests，0 failures。
- `./dev test macos`
  - 按仓库规则识别 xcodebuild testmanagerd sandbox block，并复用 built XCTest bundle；最终通过，199 tests，0 failures。
- `git diff --check`
  - 通过。

六、风险与后续

剩余风险：未做真实 UI 手动点击验证，只完成了 macOS build/test 与模型/路由测试。S1-18 的 name conflict、iCloud download、Replace 二次确认仍按现有 Stage 1 能力边界处理，未在本任务扩展 control map 之外功能。

建议下一个任务继续执行 `2-2/task-11`，进入 S1-19 import-folder-sheet。  
