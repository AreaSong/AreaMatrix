**一、执行结论**

已完成 `2-2/task-13`。

**二、执行范围**

单任务：S1-19 import-folder-sheet 的 C1-08 `import-index-file` 接入。

修改文件：
- [CoreImporting.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Bridge/CoreImporting.swift)
- [ImportFolderPreviewModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportFolderPreviewModel.swift)
- [ImportFolderImporting.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportFolderImporting.swift)
- [ImportFolderScanState.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ImportFolderScanState.swift)
- [ImportFolderPreviewSections.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ImportFolderPreviewSections.swift)
- [ImportFolderPreviewModelTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ImportFolderPreviewModelTests.swift)
- [ImportFolderTestSupport.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ImportFolderTestSupport.swift)
- [project.pbxproj](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix.xcodeproj/project.pbxproj)

**三、完成情况**

核对清单已完成：只处理 S1-19 + C1-08；已读取并按 UX、control map、Core API、C1-08 规格交叉实现；未实现相邻页面。S1-19 现在可选择 `Index-only`，并通过 `CoreBridge.importBatchFile(... storageMode: .indexOnly ...)` 走真实 `import_file` / `StorageMode.Indexed`。

完成标准已满足：没有 mock-only 闭环；真实 CoreBridge 测试证明源文件不移动、不复制到资料库最终路径，返回 `storageMode = Indexed` 和 `sourcePath`。Move 仍禁用并明确标为 C1-07 后续能力。

Forbidden Touches：未触碰 `core/src/**`，也未修改 `core/area_matrix.udl`。

**四、工程质量**

代码拆分后改动文件均低于 500 行；新增导入执行逻辑独立在 `ImportFolderImporting.swift`，测试支持独立在 `ImportFolderTestSupport.swift`。错误路径继续走 `CoreErrorMapping`，没有静默吞错、`try!`、强制解包、mock-only 或硬编码成功态。未新增依赖。

**五、验证情况**

已运行：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：最终通过。首次运行暴露 Swift 访问控制问题，已修复后重跑通过。
- `./dev test macos`：通过。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 通信阻断，脚本按仓库规则 fallback 到 `xcrun xctest`，226 tests 全部通过。
- `git diff --check -- <本任务路径>`：通过。
- 文件长度检查：本轮 Swift 源/测试文件均低于 500 行。

未运行：
- `swiftformat --lint .`：当前环境未安装 `swiftformat`。
- `swiftlint --strict`：当前环境未安装 `swiftlint`。

**六、风险与后续**

剩余风险：未做真实 UI 手工操作，只完成 build 与 XCTest；本地 Xcode 有 CoreSimulator/testmanagerd sandbox 告警，但不影响最终 build 与 fallback 测试结果。工作树中原本已有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 本地状态改动，我未把它们纳入本任务修改。

建议下一个任务：先运行 `2-2/task-13` 的 verify-ready 验收；通过后按队列进入 `2-2/task-14`。

