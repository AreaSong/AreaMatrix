一、执行结论

已完成 `1-5/task-20`。

二、执行范围

新增了 [core/tests/list_icloud_conflicts_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/list_icloud_conflicts_integration_verify.rs:1)。

未修改产品实现、`core/area_matrix.udl`、文档或 `apps/**`。工作区里仍有执行前已存在的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 变更，我未回滚也未纳入本任务。

三、完成情况

- 核对清单：已只处理 `C1-25/list_icloud_conflicts` integration-verify 范围。
- Exact Docs：已按 manifest 读取并用作 SSOT。
- 相邻能力/UI：未实现相邻 C1 能力，未触碰 UI。
- 交叉检查：新增测试覆盖 capability spec、control map、Core API、UDL、Rust wiring、S1-36 和 S1-25 消费上下文。
- mock/空壳：新增真实临时 repo 测试，验证字段结构、排序、`NeedsReview`、只读边界和 placeholder 错误路径。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

新增测试是 task-scoped integration evidence，没有引入依赖、mock-only wiring 或一次性产品逻辑。测试证明 `list_icloud_conflicts` 不写 `files`、不写 `change_log`、不删除/移动/改写冲突副本，也不触发 iCloud placeholder 下载。新增文件 328 行，未超过仓库阈值。

五、验证情况

已运行并最终通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace list_icloud_conflicts`
- `cd core && cargo test --workspace`（Mission-Critical 额外加宽回归）
- `git diff --check`

过程中 targeted test 首次因新增测试里的文档断言字符串过严失败，已修正后重跑通过。无无法运行项。

六、风险与后续

剩余风险：本任务只验证 Core C1-25，不验证 macOS UI 真实接线；S1-36 的 UI 消费闭环仍应由后续 `2-4/task-04` 覆盖。按当前 phase 队列，下一个任务建议继续 `1-5/task-21`。