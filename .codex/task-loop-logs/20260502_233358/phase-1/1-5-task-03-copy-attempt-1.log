一、执行结论

已完成 `1-5/task-03: C1-22 rename-file failure-recovery`。

二、执行范围

本次修改仅限：

- [core/src/storage/rename.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/storage/rename.rs:93)
- [core/tests/rename_file_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/rename_file_failure_recovery.rs:111)

未修改 `core/area_matrix.udl`，因为现有 `rename_file(repo_path, file_id, new_name) -> FileEntry` 已与 docs / UDL 对齐。未触碰 `apps/**`。

三、完成情况

核对清单均完成：

- 只处理 C1-22 rename-file failure-recovery。
- 已读取 task、manifest、Exact Docs、Existing Code，并以 docs 为 SSOT。
- 未实现相邻 C1 能力或 UI 页面。
- 覆盖 DB 失败回滚、indexed DB 失败、rollback 失败后 FS/DB 一致性、重复执行、权限拒绝。
- 未删除、覆盖或移动未确认的用户文件；测试全部使用 tempdir。

核心改动：当 repo-owned rename 因 overview 失败进入回滚，但 DB rollback 又失败时，新增 `restore_committed_state()` 兜底，把物理文件恢复到 DB 已提交的路径，避免 DB 指向 final 但文件被还原到 original 的半提交状态。

四、工程质量

结构保持小范围：只在现有 rename rollback guard 上补偿，不新增 API、不改 UDL、不引入依赖。新增测试是真实 FS + SQLite 路径，不是 mock-only 或硬编码通过态。注释只说明 failure-recovery 的一致性原因。

五、验证情况

已通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace rename_file`
- 额外补跑：`cd core && cargo test --workspace`
- 额外检查：`git diff --check`

六、风险与后续

剩余风险：没有 macOS UI 层验证，本任务绑定 Core 能力且 `apps/**` 禁止触碰，所以这是范围内未覆盖项，不阻断当前 task。

工作区还有开始前已存在的脏文件：`.codex/task-loop-runs/20260502_233358/summary.json`、`tasks/prompts/_shared/progress.json`；本次未把它们纳入改动范围。

建议下一个任务：继续 `1-5/task-04`，但先按 manifest 重新读取边界，不复用本 task 的假设。

