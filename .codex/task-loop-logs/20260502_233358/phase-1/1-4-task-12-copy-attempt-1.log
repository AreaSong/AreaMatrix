一、执行结论：已完成。

二、执行范围：单任务 `1-4/task-12`，只做 C1-18 `sync-external-renamed` Core implementation。  
本轮修改：

- [core/src/sync/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/sync/mod.rs:45)：接入 `ExternalEventKind::Renamed` 分支，按 hash 配对旧 DB 行，更新 `SyncResult.detected_renames` 和 cursor。
- [core/src/db/sync.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/sync.rs:38)：把 created/renamed 合并到同一事务批处理，保证 DB 行、change_log、cursor 一起提交或回滚。
- [core/tests/sync_external_renamed_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_renamed_implementation.rs:132)：新增 C1-18 成功、幂等、无法配对、跨分类越界、DB 回滚测试。
- [core/tests/sync_external_created_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_created_integration_verify.rs:214)：更新 C1-17 源码片段断言，适配 sync 入口已同时支持 C1-18。

未触碰 `apps/**`。工作区进入前已有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 脏状态，我未回退。

三、完成情况：

- 核对清单：5/5 完成。
- 完成标准：C1-18 有真实 DB + change_log + cursor 证据；未扩展到 UI 或 C1-19 move/removed/modified。
- Forbidden Touches：未触碰。

四、工程质量：

实现不移动、删除、复制、重命名用户文件；只读确认新路径存在并计算 hash。无法唯一配对、跨分类 move、目标缺失、DB 日志失败都有显式错误或事务回滚。无新增依赖，无 mock-only / 硬编码通过态；生产代码无 `unwrap/expect/panic`。

五、验证情况：

- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace sync_external_renamed`：通过，C1-18 contract 3 条 + implementation 5 条。
- 额外 `cd core && cargo test --workspace`：通过。
- `git diff --check`：通过。

六、风险与后续：

剩余范围边界是刻意保留的：跨分类 move、removed/modified 同步、复杂 duplicate hash 配对优化不在 C1-18 内，当前返回 `Conflict`，由后续能力处理。建议下一个任务执行 `1-4/task-13`，补 C1-18 failure/recovery 验证边界。

