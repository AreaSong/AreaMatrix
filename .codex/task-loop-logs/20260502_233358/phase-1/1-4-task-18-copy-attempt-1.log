一、执行结论：已完成。

二、执行范围：单任务 `1-4/task-18`，只新增 [sync_external_removed_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_removed_failure_recovery.rs:1)。未改 `core/src/**`、`core/area_matrix.udl`、`apps/**`。工作区中 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是开始前已有脏状态，未纳入本任务改动。

三、完成情况：
- C1-19 failure-recovery 范围：已覆盖。
- Exact Docs：已读取并按 SSOT 对齐；现有实现/API/UDL 已匹配，无需改业务代码。
- 相邻能力/UI：未实现、未触碰。
- 失败、回滚、重复执行、权限错误：新增 4 个真实 tempdir 集成测试，覆盖批次失败后重放、DB change_log 失败回滚、重复 removed 事件 no-op、目录权限拒绝保持 metadata/cursor。
- Forbidden Touches：`apps/**` 未触碰。

四、工程质量：
- 测试走真实 `init_repo`、SQLite、文件系统和 `sync_external_changes`，不是 mock-only。
- 没有新增依赖，没有硬编码通过态，没有产品路径 `unwrap/expect` 变更。
- 用户文件安全证据在测试中体现：失败路径不删除、不移动、不覆盖无关文件；DB 失败不推进 cursor、不留下 deleted 半状态。

五、验证情况：全部通过。
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace sync_external_removed`
- 额外按 validation-driver 补跑：`cd core && cargo test --workspace`
- `git diff --check`

六、风险与后续：
- 剩余风险：无当前 task 阻断项；权限测试在 Unix 下运行，当前环境已通过。
- 回滚：删除新增的 `core/tests/sync_external_removed_failure_recovery.rs` 即可回到本任务前产品代码状态。
- 建议下一个任务：按 manifest 顺序进入 `1-4/task-19`。

