一、执行结论

已完成。`1-5/task-15` 的 P0 阻塞已修复：C1-24 现在有非写入的 `preview_move_to_category(...) -> MoveToCategoryPreview` Core 合同，可在确认前解析最终路径、C1-10 自动编号结果和 Index-only 行为，并由测试证明不移动文件、不创建分类目录、不写 DB。

二、执行范围

单任务：`1-5/task-15`

修改文件清单：
- `docs/core/capability-specs/stage-1-mvp/C1-24-move-to-category.md`
- `docs/architecture/mvp-control-map.md`
- `docs/api/core-api.md`
- `core/area_matrix.udl`
- `core/src/domain.rs`
- `core/src/api.rs`
- `core/src/storage/mod.rs`
- `core/src/storage/move_to_category.rs`
- `core/tests/move_to_category_contract_api.rs`
- `core/tests/move_to_category_integration_verify.rs`
- `core/tests/move_to_category_preview.rs`

工作区里原本已有 `.codex/task-loop-*` 和 `tasks/prompts/_shared/progress.json` 变更，我没有把它们纳入本次产品实现范围，也没有触碰 `apps/**`。

三、完成情况

核对清单全部通过：
- 只处理 C1-24 integration-verify 范围。
- Exact Docs 已按 SSOT 对齐，Core API / UDL / Rust / 测试同步。
- 未实现相邻 C1 能力或 UI 页面。
- 已交叉检查 C1-24、S1-35、control map。
- 无 mock、空壳或硬编码通过态。

Forbidden Touches：未触碰。`git diff --name-only -- apps` 和 `git ls-files --others --exclude-standard -- apps` 均无输出。

四、工程质量

实现保持在 Core 层，无 macOS 依赖、无新增依赖。新增 public Rust API 有 rustdoc 和 `# Errors`，preview 与 confirm 共用目标路径解析逻辑，避免 UI preview 和真实 move 漂移。测试使用临时目录和真实 SQLite/文件系统路径，覆盖 repo-owned、同名自动编号、缺失目标目录无创建、Indexed metadata-only、未知分类失败无副作用。

五、验证情况

已运行并通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace move_to_category`
- `cd core && cargo test --workspace`
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`
- `python3 tasks/prompts/_shared/prompt_pipeline.py audit --pages`
- `git diff --check`

无法运行项：无。

六、风险与后续

剩余风险：Swift UI 消费 `preview_move_to_category` 不在本 task 范围内，且 `apps/**` 被禁止触碰；后续由 S1-35 对应 `2-3/task-34..36` 页面任务接入。建议下一个任务在本 task verify-ready 通过后继续 `1-5/task-16`。  
