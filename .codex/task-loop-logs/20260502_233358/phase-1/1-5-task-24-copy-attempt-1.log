一、执行结论

已完成。

二、执行范围

单任务：`1-5/task-24`，只处理 C1-26 validation。  
新增文件：[repair_reindex_metadata_validation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/repair_reindex_metadata_validation.rs)

未触碰 `apps/**`。执行前已有的脏文件 `.codex/task-loop-runs/20260502_233358/summary.json`、`tasks/prompts/_shared/progress.json` 保持未处理。

三、完成情况

- 已读取 task、共享规则、切片规则、工程质量规则、编码规范、repo-local skills、dependency graph、phase manifest、Exact Docs、Existing Code。
- 新增 5 个 Rust validation 测试，覆盖：
  - full repair 保留 diagnostics，reindex 后 List/Tree 可重新加载。
  - `README.md`、用户文件、根 `AREAMATRIX.md` 不被改写。
  - `full_rescan=false` 不偷偷 reindex 用户文件。
  - 未初始化 repo / metadata 内部路径 / 缺失 `index.db` 的关键错误路径。
- 没有扩展到相邻 C1 能力或 UI 页面。
- Forbidden Touches：未触碰。

四、工程质量

测试使用 `tempfile` 隔离真实文件系统路径，直接断言用户文件快照、metadata 诊断路径、scan session 状态、List/Tree 可见性和结构化错误。没有新增生产 API、依赖、mock-only wiring、硬编码通过态或一次性脚本逻辑；不需要 rustdoc 或文档同步。

五、验证情况

通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace repair_reindex_metadata`
- `cd core && cargo test --workspace`
- `git diff --check -- core/tests/repair_reindex_metadata_validation.rs`

六、风险与后续

剩余风险：未做 macOS UI 消费验证，本 task 绑定范围是 Core validation，未触碰 `apps/**`。  
建议下一个任务：继续执行 `1-5/task-25`。