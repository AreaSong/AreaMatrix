**一、执行结论**

已完成。

**二、执行范围**

单任务：`1-5/task-15`，只补 C1-24 integration-verify 证据。

修改文件：
- [move_to_category_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/move_to_category_integration_verify.rs:413)

未触碰 `apps/**`。当前工作区里 `.codex/task-loop-runs/20260502_233358/summary.json` 和 `tasks/prompts/_shared/progress.json` 是执行前已有脏状态，我没有修改或回滚它们。

**三、完成情况**

核对清单全部通过：
- 只处理 C1-24。
- 已读取 task、共享规则、切片规则、工程质量规则、编码规范、repo-local skill、dependency graph、phase manifest 和全部 Exact Docs。
- 交叉检查了 C1-24 spec、`mvp-control-map`、`core-api.md`、`S1-35-change-category-sheet`、UDL、Rust API、storage/db 实现。
- 未实现相邻 C1 能力或 UI 页面。
- 新增测试使用真实 temp repo、SQLite、文件系统、Core API 查询链路；没有 mock/空壳。

**四、工程质量**

新增测试文件 475 行，单函数均不超过 50 行。测试覆盖：
- docs/API/UDL/UX/control map/source alignment。
- repo-owned 文件移动：安全编号、不覆盖目标、保留 source、README、note、tag，更新 List/Detail/Tree/Log。
- Indexed 文件：只更新元数据，不移动外部源文件。
- SQLite `integrity_check` 和 `foreign_key_check`。

未新增依赖，未改产品实现，未改 UDL/API 文档。

**五、验证情况**

已通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace move_to_category`
- 额外按 validation-driver 补跑：`cd core && cargo test --workspace`
- `git diff --check`

**六、风险与后续**

剩余风险：S1-35 UI 的目标路径预览仍属于后续 macOS page-feature/page integration 任务验收范围；本任务只证明 C1-24 Core 能力与消费合同一致。建议下一个任务继续执行 `1-5/task-16`。  

