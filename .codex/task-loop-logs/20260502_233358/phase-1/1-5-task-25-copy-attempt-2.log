一、执行结论

- 已完成。`1-5/task-25` 的 repair retry 已修复上轮 P0：DB corrupted 场景下，`repair_metadata(full_rescan=true)` 不再在 full rescan 前直接失败，而是先保留诊断快照，再只在 `.areamatrix/` 内重建可用 `index.db`，随后重扫并恢复 List/Tree 可加载。

二、执行范围

- 单任务：`1-5/task-25`
- 修改 / 新增文件：
  - [core/src/repair.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/repair.rs:54)
  - [core/tests/repair_reindex_metadata_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/repair_reindex_metadata_failure_recovery.rs:68)
  - [core/tests/repair_reindex_metadata_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/repair_reindex_metadata_integration_verify.rs:234)
- 未触碰 `apps/**`；`git status --short -- apps` 为空。

三、完成情况

- 只处理 C1-26：通过。
- Exact Docs / control map / S1-37、S1-11、S1-32 语义交叉：通过。
- 未实现相邻 C1 能力或 UI 页面：通过。
- mock / 空壳检查：通过，走真实 SQLite、文件系统扫描、scan session、List/Tree 查询。
- 上轮阻塞项：已修复，损坏 DB 的 confirmed full rescan 主路径现在有测试证明可重建索引。

四、工程质量

- `repair_metadata` 结构保持在 C1-26 内：健康 DB 走原 reindex；损坏 metadata 走诊断快照 + replacement DB + reindex。
- DB 替换只处理 `.areamatrix/index.db` 与 SQLite companion 文件；不会移动、重命名、删除、覆盖用户文件。
- 替换流程避免依赖平台 rename 覆盖行为：先 retired 旧 DB，安装新 DB，失败时尝试恢复旧 DB。
- 无新增依赖；无 mock-only、硬编码通过态或一次性脚本实现。

五、验证情况

全部通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace repair_reindex_metadata`
- 扩展验证：`cd core && cargo test --workspace`
- 额外检查：`git diff --check`

六、风险与后续

- 剩余风险：未做 macOS UI 真实交互验证；本 task 范围是 Core integration-verify，`apps/**` 明确禁止触碰。
- 当前工作区仍有 task-loop 运行证据文件和 `progress.json` 变更，它们是本次进入前已有/runner evidence 状态；我没有回滚或清理。
- 建议下一个任务：进入该 task 的 verify-ready / checkpoint 流程，通过后由 task-loop 选择下一个 pending task。

