**一、执行结论**

已完成 `1-4/task-11` 的 C1-18 `sync-external-renamed` contract-api 范围。

**二、执行范围**

单任务，仅 Core 合同与测试：

- 修改：[core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:461)
- 新增：[core/tests/sync_external_renamed_contract_api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_renamed_contract_api.rs:1)
- `core/area_matrix.udl` 未修改：现有 UDL 已包含 `ExternalEventKind::Renamed` 与 `SyncResult.detected_renames`，新增测试已锁定该对齐关系。
- `apps/**` 未触碰。

**三、完成情况**

核对清单全部完成：只处理 C1-18 contract-api；已读取 task、共享规则、manifest、Exact Docs、Existing Code；未实现 C1-17/C1-19 或 UI；明确了 rename 事件输入、输出、错误码和副作用边界。

完成标准满足：C1-18 的合同证据由新增 contract test 覆盖，且没有扩展到未绑定能力。

**四、工程质量**

代码结构清晰，没有新增依赖、mock-only wiring、硬编码通过态或一次性实现。新增 public API 说明补齐在 `sync_external_changes` rustdoc 中；测试交叉检查 C1-18 capability spec、control map、Core API、error-codes、UDL、Rust API 和 domain 类型。

注意：本任务是 contract-api，不实现真实 rename 同步行为；真实 `files.path` 更新、`change_log.renamed` 写入和降级 removed+created 的运行路径应由下一个 `1-4/task-12` implementation 完成。

**五、验证情况**

全部通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace sync_external_renamed`，命中新 C1-18 3 个测试
- `cd core && cargo test --workspace`
- `git diff --check`

未运行项：无。

**六、风险与后续**

剩余风险是实现层仍未落地，这符合当前 contract-api 任务边界。建议下一个任务执行 `1-4/task-12`，实现 C1-18 rename 真实同步路径。

当前工作区还有任务开始前已存在的未纳入本任务脏状态：`tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260502_233358/`，我未改动它们。

