一、执行结论

已完成 `1-4/task-17: C1-19 sync-external-removed implementation`。

二、执行范围

修改文件：
- [core/src/sync/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/sync/mod.rs:49)
- [core/src/db/sync.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/db/sync.rs:26)
- [core/tests/sync_external_removed_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/sync_external_removed_implementation.rs:151)

未触碰 `apps/**`。`core/area_matrix.udl` 已与文档一致，本任务无需改动。

三、完成情况

已实现 `ExternalEventKind::Removed` 的最小真实路径：解析相对/绝对 repo 内路径、跳过 `.areamatrix/**` 与 `AREAMATRIX.md`、拒绝 iCloud placeholder marker、只读确认目标路径已缺失，然后把匹配 active row 标记为 `status='deleted'`，写 `change_log.action='deleted'`，推进 cursor 并返回 `SyncResult.detected_deletes`。

未实现相邻 `Modified`、restore、category drop、Trash 自动恢复或 UI 能力。

四、工程质量

实现保持在 sync/db 分层内：`sync` 负责事件规划与文件系统只读判断，`db::sync` 负责同事务写 DB、change_log 和 cursor。失败路径有显式回滚测试；不存在 mock-only、硬编码通过态或一次性脚本逻辑。新增测试覆盖成功、幂等、路径仍存在、DB 日志失败回滚、内部路径跳过、路径逃逸和 iCloud marker。

五、验证情况

已通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace sync_external_removed`
- 补充按 validation-driver 跑了 `cd core && cargo test --workspace`
- `git diff --check`

六、风险与后续

剩余风险：未覆盖真实 macOS FSEvents 端到端，只完成 Core 层临时目录自动化验证。工作区里 `.codex/task-loop-runs/20260502_233358/summary.json` 和 `tasks/prompts/_shared/progress.json` 是开始前已存在的脏状态，我未修改。

建议下一步执行 `1-4/task-17` 的 verify-ready；通过后进入 `1-4/task-18`。

