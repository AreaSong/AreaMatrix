一、执行结论：已完成。

二、执行范围：单任务 `1-4/task-22`，本轮 repair 直接修改：
- [core/src/storage/import.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/storage/import.rs:166)
- [core/src/storage/rename.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/storage/rename.rs:47)
- [core/src/sync/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/sync/mod.rs:92)
- [core/tests/overview_generated_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/overview_generated_implementation.rs:168)

三、完成情况：
- P0 已修复：`import_file` 不再吞掉 overview regeneration 错误，直接向调用方返回。
- P1 已修复：`rename_file` 成功后刷新当前 category 概览；`sync_external_changes` 对实际 created/renamed/removed 影响到的节点刷新概览。
- 未实现相邻 C1 能力、未改 UI、未改 `apps/**`。
- `Forbidden Touches`：未触碰，`git diff --name-only -- apps` 无输出。

四、工程质量：
- 错误路径显式传播，没有 warn 后返回成功。
- `sync` 只收集受影响顶层节点并复用 overview 模块，没有引入 mock、硬编码或一次性路径。
- 新增测试覆盖导入 overview 失败暴露、手动 rename 后概览刷新、外部 sync rename 后概览刷新。
- 单文件行数仍低于 500 行。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过
- `cd core && cargo test --workspace overview_generated`：通过，contract 6 个 + implementation 7 个通过
- `cd core && cargo test --workspace`：通过
- `git diff --check`：通过
- `git diff --name-only -- apps`：通过，无输出

六、风险与后续：
- 无已知阻塞项。
- 当前工作区还存在 task-loop 运行态文件和进度文件的既有脏状态，本轮 repair 未修改这些运行态文件。
- 建议下一个动作：进入 `1-4/task-22` verify-ready 验收。

