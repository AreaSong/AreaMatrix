一、执行结论

已完成 `1-5/task-25`。

二、执行范围

单任务：`C1-26 repair-reindex-metadata integration-verify`

修改文件：
- [core/tests/repair_reindex_metadata_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/repair_reindex_metadata_integration_verify.rs)

没有触碰 `apps/**`。工作区中另有既有脏文件 `.codex/task-loop-runs/20260502_233358/summary.json` 和 `tasks/prompts/_shared/progress.json`，本次未修改它们。

三、完成情况

- 核对清单：已逐项完成。
- Exact Docs：已读取并按 C1-26 SSOT 交叉核验。
- Capability / control map / UX 页面：已覆盖 `S1-37` 的 C1-26 消费，以及 `S1-11`、`S1-32` 只跳转到修复确认页、不直接执行修复的边界。
- Mock / 空壳：没有。新增测试走真实 Core API：`create_diagnostics_snapshot`、`repair_metadata`、`list_files`、`list_tree_json`、`get_latest_scan_session`。
- Forbidden Touches：未触碰。

四、工程质量

新增的是集成验收测试，不改产品实现、不改 UDL、不扩展相邻能力。测试覆盖了 docs/API/UDL/control map 对齐、用户确认后的 full rescan 可重新加载 List/Tree、诊断快照路径位于 `.areamatrix/`、失败时用户文件和诊断信息保留。无新增依赖，无 mock-only 或硬编码通过态。

五、验证情况

通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace repair_reindex_metadata`
- `cd core && cargo test --workspace`
- `git diff --check`

失败：无。

无法运行：无。

六、风险与后续

剩余风险：本任务没有实现 macOS UI，也没有触碰 `apps/**`，所以 S1-37/S1-11/S1-32 的真实 UI wiring 仍由后续 Phase 2 页面任务覆盖。建议下一个任务继续进入 `2-4/task-05` 或对应 S1-37 page-feature 链路。  
