**一、执行结论**

已完成 `1-4/task-29`。

**二、执行范围**

单任务：C1-21 error-mapping validation。  
修改文件：新增 [core/tests/error_mapping_validation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/error_mapping_validation.rs:1)。

未触碰 `apps/**`、`core/src/**`、`core/area_matrix.udl`。工作区里已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是执行前已存在的脏状态，我没有修改它们。

**三、完成情况**

- 只处理 C1-21 validation：通过。
- Exact Docs 已逐个读取，并按 `C1-21-error-mapping.md`、control map、Core API、testing 作为 SSOT。
- 未实现相邻 C1 能力或 UI 页面。
- 补齐 Rust validation 测试：新增 4 条测试。
- 覆盖成功路径和关键错误路径：所有 `CoreError` variant 映射、FFI `ErrorMappingInput` 与 `CoreError::to_error_mapping()` 一致性、误导 payload 不改变 `ErrorKind` 分支、高严重错误不被降级或吞掉。

**四、工程质量**

代码只新增测试，无 API/UDL/生产逻辑改动。测试结构直接、无 mock-only 或硬编码通过态，断言绑定真实 public API：`map_core_error`、`CoreError::to_error_mapping`、`ErrorKind`、`ErrorSeverity`、`ErrorRecoverability`。未新增依赖。

**五、验证情况**

全部通过：

- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace error_mapping`
- 额外按 Mission-Critical/core 改动扩展：`cd core && cargo test --workspace`
- `git diff --check -- core/tests/error_mapping_validation.rs`

**六、风险与后续**

剩余风险：未做 macOS Swift `AppError` 包装层验证，因为本 task 绑定 Core validation，且 Forbidden Touches 禁止触碰 `apps/**`。  
建议下一个任务：进入 `1-4/task-30` 做 C1-21 integration verify，检查 Core API/UDL/Rust 测试与消费页面声明的一致性。  
