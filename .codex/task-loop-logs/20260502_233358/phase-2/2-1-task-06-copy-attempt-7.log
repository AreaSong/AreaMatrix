一、执行结论

已完成。代码层阻塞已修复；标准 `xcodebuild test` 在当前 sandbox 中仍被 `testmanagerd.control` 阻断，但仓库认可的 fallback `bash scripts/check-macos-tests.sh` 已执行 XCTest bundle，39/39 通过。

二、执行范围

单任务：`2-1/task-06`

本次实际修改：
- [CoreBridge.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Bridge/CoreBridge.swift:66)
- [AppShellModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/AppShellModel.swift:413)
- [ValidatePathStepView.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Onboarding/ValidatePathStepView.swift:164)
- [ValidatePathIntegrationTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ValidatePathIntegrationTests.swift:100)

三、完成情况

核对清单结果：
- page-feature 覆盖：维持 `C1-01 / C1-03 / C1-21`，未扩展 control map 外能力。
- 页面闭环：修复容量或外置卷结果缺失仍可继续的问题。
- mock / fixture / hardcoded：未引入生产 mock-only 或硬编码通过态。
- Forbidden Touches：未触碰 `core/src/**`，已用 `git status/diff/ls-files` 检查为空。

完成标准：
- S1-03 现在对环境检查缺失按错误态阻断，符合 page spec。
- Core 能力仍通过 `CoreBridge` 和现有 UniFFI 路径消费；未改 Core 实现。

四、工程质量

新增 `hasMissingEnvironmentChecks` 聚合判断，避免 UI/model 重复判断。`canContinueFromValidatePath` 现在会被缺失容量或卷信息阻断。UI checklist 将缺失结果显示为 `Failed: 检查结果缺失`，不再以 warning 放行。

新增两条回归测试覆盖：
- `availableCapacityBytes == nil`
- `isExternalVolume == nil`

文件长度仍在 500 行以内；无新增依赖；无 Rust public API / rustdoc 变更。

五、验证情况

已运行：
- `git diff --check`：通过。
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：通过，`** BUILD SUCCEEDED **`。
- `bash scripts/check-macos-tests.sh`：通过，`39 tests, 0 failures`。

失败：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：code 65，原因仍是当前 sandbox 无法访问 `com.apple.testmanagerd.control`，不是编译或测试断言失败。

六、风险与后续

剩余风险：标准 `xcodebuild test` 需要在非 sandbox 本机或 CI 补跑作为远端门禁证据。建议下一步进入 `2-1/task-06` 的 verify-ready 验收；验收通过后再推进 `2-1/task-07`。  
当前工作区仍有 task-loop 先前遗留的其他改动和日志，我没有清理或回滚。

