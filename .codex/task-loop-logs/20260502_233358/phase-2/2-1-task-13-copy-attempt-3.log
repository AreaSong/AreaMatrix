一、执行结论

已完成本轮 repair。代码侧和工程质量阻塞已修复；manifest 指定的 `xcodebuild test` 在当前沙箱仍被 `testmanagerd` 权限阻断，但仓库认可的 fallback `scripts/check-macos-tests.sh` 已成功执行 XCTest，52 个测试全部通过。

二、执行范围

单任务：`2-1/task-13`

本轮直接修改：
- [ValidatePathIntegrationTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ValidatePathIntegrationTests.swift:353)
- [AreaMatrixAppSmokeTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/App/AreaMatrixAppSmokeTests.swift:311)

三、完成情况

核对清单：
- page-feature 覆盖：通过，S1-05 仍覆盖 `C1-02 / C1-03 / C1-16`。
- 页面入口、退出、错误态、CoreBridge 调用链：通过，已复核 `initRepo`、`recoverOnStartup`、`getLatestScanSession` 路径。
- mock / fixture / 硬编码状态：生产路径未新增 mock；fixture 仅在 XCTest。
- control map 范围：未新增 control map 外能力。

Forbidden Touches：
- 未触碰 `core/src/**`；`git diff --name-only -- core/src` 无输出。

四、工程质量

两个上次失败的 Swift 测试文件已降到规范阈值：
- `ValidatePathIntegrationTests.swift`: 500 行
- `AreaMatrixAppSmokeTests.swift`: 500 行

未新增依赖、未新增 public Rust API、未触碰文档/UDL/Core 实现。`git diff --check` 通过。

五、验证情况

已运行：
- `git diff --check`：通过
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：通过，`BUILD SUCCEEDED`
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：失败，退出码 65；原因仍是当前沙箱无法访问 `com.apple.testmanagerd.control`
- `bash scripts/check-macos-tests.sh`：通过；检测到 sandbox block 后改用 `xcrun xctest`，52 tests / 0 failures

未运行：
- `swiftformat` / `swiftlint`：当前环境 `command -v` 无输出，工具不可用。

六、风险与后续

剩余风险：本地沙箱环境无法让原始 `xcodebuild test` 返回 0；CI 或非沙箱 macOS 环境仍需补跑原命令作为最终门禁。

建议下一个任务：在 CI/非沙箱补跑原始 `xcodebuild test` 后，进入 `2-1/task-14`。