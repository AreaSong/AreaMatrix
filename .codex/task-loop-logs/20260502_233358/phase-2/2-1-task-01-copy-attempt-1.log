一、执行结论：**未完成**

允许范围内的 UI 状态、错误映射和 C1-04 CoreBridge 调用边界已实现并通过构建，但生产路径仍无法证明真实 `load_config` 成功闭环：当前仓库缺少可用的 UniFFI generated bindings / 链接产物，`CoreBridge` 仍会在生产成功路径抛出 `generatedBindingsUnavailable`，我没有用 mock 或静态成功态伪造通过。

二、执行范围：单任务

修改文件：
- [CoreBridge.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Bridge/CoreBridge.swift)
- [AppShellModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/AppShellModel.swift)
- [MainWindow.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/MainWindow.swift)
- [AreaMatrixShellTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/AreaMatrixShellTests.swift)

三、完成情况

核对清单：已限制在 `S1-01 + C1-04`；已读取并交叉检查页面规格、control map、Core API、C1-04 spec；只接入 C1-04 的 Bridge / 状态 / 错误映射；未实现相邻页面。

完成标准：用户可见入口和失败态可触发；未使用 mock-only 成功闭环；未触碰 control map 外能力。阻塞点是真实 Core 成功读取路径仍缺 generated bindings。

Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

实现拆为 `CoreBridge` 协议边界、`OnboardingModel` 状态机和 SwiftUI 视图；错误路径包括配置读取失败、Core load 失败、Help 打开失败。没有新增依赖、没有写用户文件、没有硬编码成功态。

残余说明：按仓库规范本应优先用 `@Observable`，但当前本机 `xcodebuild` 在 sandbox 下无法加载 `ObservationMacros`，所以改用 `ObservableObject/@Published` 保证验证可运行。

五、验证情况

已运行并通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
- `bash scripts/check-macos-tests.sh`，共 7 个 XCTest 通过
- `git diff --check`

标准 `xcodebuild test` 的 test runner 启动被本机 sandbox 的 `com.apple.testmanagerd.control` 限制拦住；仓库脚本已 fallback 到 `xcrun xctest` 并通过。

六、风险与后续

下一步应先补齐 macOS 侧 generated UniFFI bindings / Core 链接，让 `CoreBridge.loadConfig(repoPath:)` 真正调用 Rust `load_config`，否则后续 S1 页面任务会持续卡在同一个 Bridge 成功路径缺口。

当前还有两个我未改动的既有脏文件：`.codex/task-loop-runs/20260502_233358/summary.json`、`tasks/prompts/_shared/progress.json`。

