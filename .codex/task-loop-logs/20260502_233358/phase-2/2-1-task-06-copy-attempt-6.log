一、执行结论

已完成。功能失败点已修复；manifest 原始 `xcodebuild test` 在当前沙箱仍被 `testmanagerd` 阻断，但仓库定义的 macOS fallback 测试通过。

二、执行范围

单任务：`2-1/task-06`

本轮直接修改：
- [AppShellModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/AppShellModel.swift:188)
- [MainWindow.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/MainWindow.swift:71)
- [ValidatePathIntegrationTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ValidatePathIntegrationTests.swift:54)

三、完成情况

- Page-feature 覆盖：已复核 S1-03 只绑定 `C1-01/C1-03/C1-21`，未扩展 control map 外能力。
- Settings 换库闭环：已修复 `Change Path -> Continue -> Open Repository` 后丢失 Settings 来源的问题；`showChoosePath()` 和 `continueFromChoosePath()` 会保留 Settings return route。
- 入口/退出：`ChoosePath` 的 Back 改为 `returnFromChoosePath`，Settings 来源返回 Settings，普通首次启动返回 Welcome。
- 回归测试：新增 `testSettingsChangePathKeepsSourceWhenOpeningExistingRepository`，证明候选 repo 在成功打开前不会写入当前配置。
- Forbidden Touches：`git diff --name-only -- core/src` 无输出，未触碰 `core/src/**`。

四、工程质量

- 状态机改动保持在现有 `OnboardingModel` 内，没有新增抽象或依赖。
- 三个相关 Swift 文件均低于 500 行，未发现超过 120 字符的行。
- 未新增 mock-only、fixture-only 生产逻辑；测试 fixture 仅限 XCTest。
- 没有新增 public Rust API，因此无 rustdoc 同步需求。
- 用户文件安全边界未被触碰：没有删除、移动、重命名或覆盖任何用户文件。

五、验证情况

已运行：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：失败，退出码 65；原因仍是当前沙箱无法访问 `com.apple.testmanagerd.control`。
- `bash scripts/check-macos-tests.sh`：通过；fallback 直接执行 XCTest，37 tests，0 failures。
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：通过。
- `git diff --check`：通过。
- `git diff --name-only -- core/src`：无输出。

无法运行：
- `swiftformat --lint ...`：当前环境无 `swiftformat`。
- `swiftlint --strict ...`：当前环境无 `swiftlint`。

六、风险与后续

剩余风险：原始 `xcodebuild test` 需要在非当前沙箱或 CI 中补跑取得原生命令 PASS；本地已用仓库认可的 fallback 取得 XCTest 证据。

建议下一个任务：进入 `2-1/task-06` verify-ready；验收通过后再推进 `2-1/task-07`。

