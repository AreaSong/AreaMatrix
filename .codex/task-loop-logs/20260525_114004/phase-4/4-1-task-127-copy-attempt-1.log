一、执行结论

已完成 `4-1/task-127`。S2-16 的 `Apply correction` 已接到 C2-12 `correct_file_category` CoreBridge 路径，并保留 Core 返回的 rule draft / remember / rule confirmation 状态用于完成后反馈。

二、执行范围

单任务：`S2-16 classifier-correct + C2-12 classifier-correction`。

修改文件清单集中在允许范围内：
`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`。关键变更包括 `CoreMoveToCategory.swift`、`MainFileCategoryMoveActions.swift`、`MainFileListState.swift`、`ChangeCategorySheet.swift`、`MainRepositoryContentCategoryMoveRefresh.swift`、`ChangeCategoryPageFeatureTests.swift`、`MainLoadingTestSupport.swift` 等。

工作区里仍有执行前已有的 `.codex/task-loop-runs/**` 和 `tasks/prompts/_shared/progress.json` dirty/staged 状态，我未回滚它们，也未把它们作为本轮功能实现范围。

三、完成情况

核对清单：只处理 S2-16 + C2-12；已读取并按 UX 页面、control map、Core API、C2-12 能力规格交叉实现；只接入 `correctFileCategory` 对应 CoreBridge、状态和错误路径；未实现 S2-17/S2-18/S2-19 或其他 Core 能力。

完成标准：S2-16 的纠错动作可触发真实 CoreBridge；没有用 mock/fixture 伪造产品闭环；未触碰 control map 之外能力。测试文件中补充了 CoreBridge 真实调用覆盖，但本 task validation 只编译 app target，不运行 XCTest。

Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

实现沿用现有 Change Category sheet / action routing / model state 模式，没有新增大抽象。C2-12 返回值现在不会丢失，`ruleConfirmationRequired` 会反映到纠错完成横幅；普通 change category 路径保持原行为。错误仍走现有 `mapCoreError` 和 `.correction` operation。未新增依赖，未写入文档或 Core 源码。

五、验证情况

已运行：

```bash
./dev check task 4-1/task-127
```

结果：通过。
- `prompt_pipeline.py doctor`: OK
- `git diff --check`: 通过
- `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`: `BUILD SUCCEEDED`

未运行：XCTest / 全量 macOS test / cargo 检查。原因是本 task 明确把验证上限限制为 `./dev check task 4-1/task-127`，该命令对 page task 执行 app build，不扩大到 test 或 core broad gate。

六、风险与后续

High 风险点在于 C2-12 可触发文件移动；本次没有删除、覆盖、移动真实用户原文件，只通过 CoreBridge 接入并由 Core 负责安全移动语义。回滚方式是撤回上述 macOS UI/model/test diff；无需 DB migration 或 Core rollback。

建议下一个任务按队列进入 `4-1/task-128`，继续 S2-17 + C2-13 rule save，补齐规则保存确认闭环。

