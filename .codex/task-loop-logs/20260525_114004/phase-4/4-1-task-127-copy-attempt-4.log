一、执行结论

- 已完成

二、执行范围

- 单任务：`4-1/task-127`
- 主要修改范围：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`
- 关键文件：`MainFileCategoryMoveActions.swift`、`MainFileCategoryMoveState.swift`、`MainFileListModel.swift`、`ChangeCategorySheet.swift`、`MainFileActionRoutingSheet.swift`、`MainRepositoryContentActionRouting.swift`、`ClassifierRuleHandoffRouteView.swift`、`ChangeCategoryPageFeatureTests.swift`、`ClassifierCorrectionHandoffTests.swift`

三、完成情况

- 核对清单：S2-16 + C2-12 单点修复完成；未实现相邻 S2-17/S2-18 的保存/影响预览 Core 能力，只做 C2-12 返回 `ruleDraft` 后的 handoff。
- 分类原因：S2-16 现在通过 `CoreCategoryPredicting.predictCategory` 加载真实分类来源，失败会映射为 UI 可见错误。
- 目标预览：classifier correction 不再拼接路径，改为必须等待 `previewMoveToCategory` 的 Core 预览；预览失败或未完成时禁用 Apply。
- Remember handoff：移除了生产路径中的合成 `safeRuleDraft`，进入 S2-17/S2-18 前先调用 `correctFileCategory`，并使用 C2-12 返回的 `ruleDraft`。
- Forbidden Touches：未触碰 `core/src/**`；`git diff/status/untracked -- core/src` 均无输出。

四、工程质量

- 代码结构：沿用现有 `MainFileListModel` + `CoreBridge` 注入模式，没有让 View 直接调 UniFFI。
- 错误处理：分类原因加载、目标预览、执行纠错均走 Core error mapping，不静默吞错。
- 占位/硬编码：生产路径已无 `safeRuleDraft`、固定分类来源文案、手写 `priority: 100` 规则草稿。
- 文档同步：未改 docs/API/UDL；本次按现有 SSOT 对齐实现。
- 新增依赖：无。

五、验证情况

- 已运行：`./dev check task 4-1/task-127`
- 通过：`prompt doctor: OK`、`git diff --check`、`xcodebuild ... build CODE_SIGNING_ALLOWED=NO`，最终 `BUILD SUCCEEDED`
- 额外证据：`rg` 未找到旧的 `safeRuleDraft`、固定分类来源、拼接路径和 `priority: 100` 痕迹。
- 未运行：未升级到 `cargo test --workspace`、`./dev check all` 或额外 XCTest；这是按当前 manifest validation 上限执行。

六、风险与后续

- 剩余风险：本地 manifest 检查只覆盖 build，不单独执行新增 XCTest；后续页面 integration verify 可补跑更完整的 UI/test target 证据。
- 建议下一个任务：进入 `4-1/task-128`，处理 S2-17 的 classifier rule save 闭环。