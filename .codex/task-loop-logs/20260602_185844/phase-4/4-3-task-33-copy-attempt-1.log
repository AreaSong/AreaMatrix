一、执行结论

已完成 `4-3/task-33`。

二、执行范围

单任务，仅涉及允许范围 `core/tests/**`：

- 新增 [mobile_detail_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/mobile_detail_failure_recovery.rs:1)
- 格式化 [mobile_detail_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/mobile_detail_implementation.rs:1)

未触碰 `apps/**`。工作区另有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 脏改动，未纳入本次实现范围。

三、完成情况

核对清单已覆盖：空态、非法输入、权限、IO/DB 错误、错误码映射。C4-07 是只读能力，不涉及写入、移动、删除、导入或同步；新增测试证明失败路径不创建 staging、不写 DB、不改用户文件、不覆盖 sidecar。未涉及隐私、AI 或远程调用。失败均显式返回 `CoreError`，没有吞错或静默降级。

完成标准已满足：C4-07 的 `get_file`、`list_changes`、`read_note` 失败路径有测试证据；Missing 状态保留 metadata row，可进入 S4-X-06；高风险只读边界通过临时目录和 DB 计数证明无半成品。

四、工程质量

新增测试是专用 failure-edge 集成测试，不是 mock-only 或硬编码通过态。测试直接调用真实 Core API、真实 SQLite repo metadata、真实文件/sidecar IO。未新增依赖，未改 public API、UDL 或 docs。`mobile_detail_implementation.rs` 仅被 `cargo fmt` 机械调整。

五、验证情况

已运行并通过：

```bash
cargo test --test mobile_detail_failure_recovery -- --nocapture
./dev check task 4-3/task-33
```

`./dev check task 4-3/task-33` 通过项包括：prompt doctor、diff check、`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`mobile_detail_contract_api`、`mobile_detail_failure_recovery`、`mobile_detail_implementation`。

六、风险与后续

剩余风险：未运行超出 manifest 的全量 `./dev check all`，这是本 task 明确限制的验证上限。建议下一个任务继续执行 `4-3/task-34`。