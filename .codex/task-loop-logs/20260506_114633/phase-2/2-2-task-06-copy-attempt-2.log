**执行结论**
已完成。`2-2/task-06` 的 S1-17 整页 integration 修复已收口；标准 `xcodebuild test` 在当前 sandbox 被 `com.apple.testmanagerd.control` 阻断，但 repo-local fallback 跑完 `170 tests, 0 failures`。

**执行范围**
单任务：`2-2/task-06`。

修改范围仅在 `Expected New Paths` 内：`apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`。未触碰 `core/src/**`，`core/area_matrix.udl` 也无改动。

主要改动：
- 增加 S1-17 导入前 preview/hash/conflict/iCloud preflight。
- 补齐 Replace hidden/enabled/disabled、确认 gating、iCloud Download & retry / Switch local repo。
- Import 成功后路由到 `S1-20 import-progress`，不再只关闭 sheet。
- 增加 S1-17 integration tests、真实文件系统 preflight tests，并补充 Copy/Move/Index-only 等待预检后的真实 CoreBridge 调用证据。

**完成情况**
核对清单均已覆盖：
- page-feature 覆盖 C1-05/C1-06/C1-07/C1-08：通过。
- 页面状态、入口、退出、错误态、CoreBridge 调用闭环：通过。
- mock/fixture-only 闭环移除：生产路径使用真实 preflight + CoreBridge；测试 fixture 仅作依赖注入验证。
- 未新增 control map 外 Core 能力：通过。

完成标准：
- S1-17 可按 page spec/control map 验收：通过。
- C1-05/C1-06/C1-07/C1-08 均有真实接入或测试证据：通过。
- Forbidden Touches：未触碰。

**工程质量**
代码结构保持在 macOS app 层：preflight、route state、view section、model wiring 分开，未改 Rust core，未引入新依赖。错误路径覆盖了来源缺失、不可读、iCloud placeholder、duplicate/name conflict、Replace 不可用、导入中/导入后禁用等状态。

文件长度检查未越界：最大测试文件 492 行，生产相关文件均低于 500 行。没有 `try!`、强制解包或 View body 内 IO；iCloud 下载仍显式由用户点击触发，不做静默下载。

**验证情况**
已运行：
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：通过。
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：失败，退出码 65；原因是当前 sandbox 无法连接 `com.apple.testmanagerd.control`，不是测试断言失败。
- `./dev test macos`：通过；标准 `xcodebuild test` 被同类 sandbox block 后，fallback 到 `xcrun xctest`，执行 170 tests，0 failures。
- `git diff --check`：通过。
- `git status --short -- core/src core/area_matrix.udl`：无输出。

**风险与后续**
剩余风险：未做真实 UI 手工点击验证；当前证据来自 build、unit/integration tests 和 filesystem preflight tests。Replace 的完整 S1-24 独立页面能力仍应由后续对应 task 覆盖，本任务只完成 S1-17 内的可见状态、确认 gating 和回写闭环。

建议下一个任务：进入 `2-2/task-06` verify-ready 验收；通过后继续 `2-2/task-07`。  
