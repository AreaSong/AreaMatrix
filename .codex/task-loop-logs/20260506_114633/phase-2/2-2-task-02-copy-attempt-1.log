一、执行结论：已完成。

二、执行范围：单任务 `2-2/task-02`。本次只改 macOS 侧 S1-17 + C1-05 分类预览：
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModel.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFilePreviewModelTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况：
- 已读取 task、共享规则、切片规则、工程质量规则、编码规范、repo-local validation skill、dependency graph、phase manifest、Exact Docs、现有 macOS/CoreBridge 代码与 `core/area_matrix.udl`。
- S1-17 单文件 sheet 现在会通过 `CoreCategoryPredicting` 默认注入 `CoreBridge()` 调用真实 `predictCategory(repoPath:filename:)`。
- UI 已展示文件信息、建议分类 Picker、建议命名输入框、`为什么？` 解释入口和分类预览状态。
- 未实现同页 C1-06/C1-07/C1-08 的导入执行、冲突、Replace、iCloud 下载等后续能力。
- Forbidden Touches：未触碰 `core/src/**`，也未修改 Core API/UDL。

四、工程质量：
- 没有新增依赖，没有 mock-only 产品路径；测试里使用注入 predictor 只验证状态模型边界。
- 错误路径显式处理 `CoreError.Config` / `CoreError.Classify`，失败时不伪造成功分类。
- 非单文件请求会跳过 C1-05 predictor，避免把批量/文件夹能力塞进本 task。
- 新增文件长度均低于规范阈值，未发现 `try!`、强制解包、静默吞错或生产路径直接 UniFFI 调用。

五、验证情况：
- 已运行 `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过，`BUILD SUCCEEDED`。
- 已运行 `./dev test macos`：通过。标准 `xcodebuild test` 被本地 sandbox 的 `testmanagerd` 通信限制阻断，脚本按仓库规则复用已构建 XCTest bundle 执行 `xcrun xctest`，146 tests，0 failures。
- 已运行 `git diff --check`：通过。
- 已确认 `core/src/**`、Exact Docs、Core API、UDL 无 diff。

六、风险与后续：
- 残余风险：未做真实 UI 手工交互截图；当前证据覆盖 build、单元测试和真实 CoreBridge C1-05 调用路径。
- 建议下一个任务：继续 `2-2/task-03`，处理 S1-17 同页下一个绑定能力，不要把 C1-06/C1-07/C1-08 回填到本 task。

注意：工作树里已有 `tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260506_114633/` 状态变化，我没有回滚这些 task-loop 运行状态文件。