一、执行结论

已完成 `2-2/task-05`。

二、执行范围

单任务：S1-17 import-single-sheet 中的 C1-08 `import-index-file`。

修改文件：
- `apps/macos/AreaMatrix/Bridge/CoreImporting.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModel.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFileIndexImportTests.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFilePreviewModelTests.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFileMoveImportTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

未触碰 `core/src/**`。工作区里已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是未处理的既有脏改动，我没有修改它们。

三、完成情况

- 已只处理 S1-17 + C1-08：新增 `Index-only` 存储模式，并接到真实 `CoreBridge.import_file(... mode: .indexed ...)`。
- 已读取并对齐 S1-17 页面规格、MVP control map、Core API、C1-08 capability spec。
- 未实现同页 C1-05 / C1-06 / C1-07 的新能力，只复用已有预览和 Copy/Move 结构。
- 新增测试覆盖模型触发 Indexed 导入、Core 错误映射、真实 CoreBridge 不移动/不复制源文件。

四、工程质量

结构保持在现有 `CoreFileImporting` 抽象层，SwiftUI View 没有直接调用 UniFFI。错误继续通过 `CoreErrorMapping` 走现有映射路径，没有 mock-only 或硬编码成功态。未新增依赖，未改文档或 Rust public API。

五、验证情况

已运行并通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
- `./dev test macos`：标准 `xcodebuild test` 被本地 sandbox 的 `testmanagerd` 通信限制阻断后，按仓库脚本回退到 `xcrun xctest`，156 个测试 0 失败。
- `git diff --check`

未运行：
- `swiftformat` / `swiftlint`：当前环境未找到可执行文件，也未发现对应配置文件。

六、风险与后续

剩余风险：未做真实 UI 手工交互验证；但 CoreBridge、模型状态、XCTest 和 macOS build/test 已覆盖本 task 的主要闭环。建议下一个任务进入 `2-2/task-06` 的 page integration / secondary capability 对齐范围，检查 S1-17 全页多能力整合，而不是在本 task 继续扩范围。

