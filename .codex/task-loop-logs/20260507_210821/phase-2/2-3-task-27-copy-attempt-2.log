一、执行结论

已完成。

二、执行范围

单任务：`2-3/task-27`，只处理 S1-31 settings-about。

修改文件：
- `apps/macos/AreaMatrix/Models/AboutSettingsModel.swift`
- `apps/macos/AreaMatrix/Views/Settings/AboutSettingsPane.swift`
- `apps/macos/AreaMatrix/Views/Settings/GeneralSettingsView.swift`
- `apps/macos/AreaMatrixTests/AboutSettingsPageFeatureTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况

- 只实现 S1-31 页面或页面区域：通过。
- 已按页面规格和 control map 核对：S1-31 Core 能力为 `none`，允许 Core API 仅 `get_version`。
- 不新增临时 Core API 或 mock 后端能力：通过。未改 `core/area_matrix.udl`，未改 `core/src/**`。
- 不顺手实现相邻页面：通过。
- 上次 P0 已修复：About 页默认诊断导出不再调用 `CoreBridge.createDiagnosticsSnapshot` / C1-26，改为本地 About-only 脱敏报告导出；默认错误映射也不再走 `CoreBridge`。
- Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

代码结构保持在 S1-31 内：model 负责状态和平台服务注入，view 只做展示和交互，测试覆盖版本读取、schema 失败、隐私确认、About-only 诊断导出、外链和日志失败恢复。未新增依赖、未新增 mock-only 生产路径、未引入临时 Core API。相关 Swift 文件行长检查通过，`AboutSettingsModel.swift` 保持 500 行以内。

五、验证情况

已运行：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过，`BUILD SUCCEEDED`。
- `./dev test macos`：通过。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断，脚本按仓库规则 fallback 到 `xcrun xctest`，398 tests，0 failures。
- `command -v swiftlint` / `command -v swiftformat`：当前环境未找到可执行文件。
- 行长检查：相关 3 个 Swift 文件无超过 120 字符的行。

六、风险与后续

剩余风险：未做真实 UI 手工点击验证；当前证据覆盖 build、单测和范围核对。建议下一个任务进入 `2-3/task-28`。  
另外，工作区已有 task-loop 的 progress/log/summary 文件改动和日志文件，不属于本次代码修复范围，我没有回退它们。

