一、执行结论

已完成。`S1-27 settings-repository` 里和 `C1-08 import-index-file` 相关的页面消费已经接到真实 Core 闭环，页面能显示真实的 indexed 文件数和健康状态。

二、执行范围

单任务：`2-3/task-16`

修改文件：
- [RepositorySettingsModel.swift](apps/macos/AreaMatrix/Models/RepositorySettingsModel.swift)
- [RepositorySettingsPane.swift](apps/macos/AreaMatrix/Views/Settings/RepositorySettingsPane.swift)
- [RepositorySettingsPageFeatureTests.swift](apps/macos/AreaMatrixTests/RepositorySettingsPageFeatureTests.swift)

未触碰 `core/src/**`。

三、完成情况

- 只处理了 `S1-27` 页面中的 `C1-08` 功能点：是
- 已读取页面规格、control map、Core API、`C1-08` 能力规格：是
- 只接入 `C1-08` 对应的 CoreBridge / 状态 / 错误映射：是
- 未顺手实现相邻页面：是

完成标准：
- 页面现在能展示 `.areamatrix/` metadata 状态，以及真实健康摘要里的 `Files indexed`、`Last scan`、`Watcher`：已满足
- indexed import 走真实 Core，源文件不移动、不复制，`storage_mode=Indexed`，`sourcePath` 保留：已用测试覆盖
- 没有用 mock/fixture/static state 伪造最终产品闭环：是

四、工程质量

- 结构上仍是 model/view 分层，没有把健康读数硬塞进视图层。
- 错误处理是显式的，健康读失败会落到可见 banner，不是静默吞掉。
- 没有新增 public Rust API，也没有碰 `core/src/**`。
- 测试里只对单测做了 deterministic fixture 注入，生产路径仍走真实 Core。
- 余下风险：真实运行时如果 repo DB 或 scan session 不可读，页面会显示健康告警，这属于预期行为。

五、验证情况

已通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`

已尝试但受环境限制失败：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' -only-testing:AreaMatrixTests/RepositorySettingsPageFeatureTests CODE_SIGNING_ALLOWED=NO`
  - 失败原因是 test runner/helper app 在当前 sandbox 下无法建立通信，不是这次改动的编译错误。

补充检查：
- `git diff --check` 通过

六、风险与后续

- 目前工作区还有既有脏文件：`.codex/task-loop-runs/20260507_210821/summary.json`、`tasks/prompts/_shared/progress.json`，我没有改它们。
- 下一个自然任务是 `2-3/task-17`，或者做 `S1-27` 的 page integration verify。