一、执行结论

已完成。`2-2/task-27` 的两项上次验收阻塞已修复：单文件导入先进入 S1-20 progress 再等待 Core 导入；批量 / 文件夹导入不再把 Move / Index-only 留作“后续能力”。

二、执行范围

修改范围集中在允许路径：

- `apps/macos/AreaMatrix/Views/Main/ImportEntrySingleFileFooter.swift`
- `apps/macos/AreaMatrix/Bridge/CoreImporting.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchCopyImportModel.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchCopyImportState.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchProgressItems.swift`
- `apps/macos/AreaMatrix/Models/ImportFolderPreviewModel.swift`
- `apps/macos/AreaMatrixTests/ImportConflictIntegrationVerifyTests.swift`
- `apps/macos/AreaMatrixTests/ImportFolderCoreBridgeModeTests.swift`
- 相关 XCTest 更新与 `project.pbxproj`

另有 task-loop 执行状态产物：`.codex/task-loop-*`、`tasks/prompts/_shared/progress.json`。`core/src/**` 未触碰。

三、完成情况

核对清单结果：

- 只做 wiring / 验收补齐：通过。
- 本闭环页面与能力覆盖：通过，新增 integration test 覆盖 S1-16 到 S1-24 与 C1-05/06/07/08/09/10/13。
- 移除最终验收阻断 mock / 后续能力文案：通过。
- 未新增未列页面或 Core 能力：通过。
- Forbidden Touches：通过，`git status --short -- core/src` 无输出。

完成标准：

- 用户路径走通：通过，单文件 Import 先触发 progress route，再完成导入。
- 失败项证据：通过，manifest 原始 `xcodebuild test` 的失败是本地 sandbox 限制，fallback XCTest 已全量通过。

四、工程质量

代码结构保持在现有模型边界内：单文件 runner 把路由顺序独立出来，批量 / 文件夹导入统一走 `importBatchFile(storageMode:)`，避免非 Copy 被静默降级。触碰的主要 Swift 文件均控制在 500 行以内；未新增依赖，未改 Rust public API，无 rustdoc 需求。

五、验证情况

已运行：

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`  
  结果：失败，exit 65；原因是 `com.apple.testmanagerd.control` sandbox restriction，测试 bundle 已编译/链接。
- `./dev test macos`  
  结果：通过 fallback，`xcrun xctest` 执行 279 tests，0 failures。
- `plutil -lint apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：通过。
- `git diff --check -- apps/macos/AreaMatrix.xcodeproj/project.pbxproj apps/macos/AreaMatrix apps/macos/AreaMatrixTests`：通过。

六、风险与后续

剩余风险是环境型：当前 sandbox 无法让原始 `xcodebuild test` 直接完成，只能由 repo fallback 复用 XCTest bundle 验证。建议下一个动作进入该 task 的 verify-ready 验收，重点核对 task + manifest + 实际文件三方证据。

