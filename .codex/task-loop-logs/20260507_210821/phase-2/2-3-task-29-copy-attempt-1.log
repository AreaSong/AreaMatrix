一、执行结论

已完成 `2-3/task-29`。S1-32 中 C1-21 error-mapping 已接入真实 `CoreErrorMappingSnapshot` 展示路径，没有改 `core/src/**`，没有实现同页 C1-16 之外的新恢复动作，也没有触碰 control map 之外能力。

二、执行范围

修改文件：
- [StartupRecoveryErrorRecoveryView.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/StartupRecoveryErrorRecoveryView.swift)
- [ErrorRecoveryStartupRecoveryPageFeatureTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/ErrorRecoveryStartupRecoveryPageFeatureTests.swift)

仓库里已有 dirty 文件 `.codex/task-loop-runs/20260507_210821/summary.json` 和 `tasks/prompts/_shared/progress.json` 保持未处理，未纳入本任务改动。

三、完成情况

核对清单全部满足：只处理 S1-32 + C1-21；已按 UX spec、control map、Core API、C1-21 spec 对齐；页面失败态现在通过 `ErrorRecoveryMappedErrorView` 展示 `kind / severity / recoverability / userMessage / suggestedAction / rawContext`；未实现相邻页面或高风险动作如 repair/remove/download。

完成标准满足：用户可见错误映射可触发和测试；没有 mock-only 或静态状态伪造真实 Core 闭环；未触碰 Forbidden Touches。

四、工程质量

结构上把 C1-21 展示拆成独立 SwiftUI view，保留 C1-16 retry 入口但不扩大行为边界。错误映射有空 `suggestedAction` / `rawContext` fallback，避免空 UI。未新增依赖，未改文档或 Rust public API，因此无 rustdoc/doc-sync 需求。没有 `try!`、强制解包、一次性脚本或硬编码通过态。

五、验证情况

已运行并通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`
- `xcodebuild build-for-testing ...`：`TEST BUILD SUCCEEDED`
- `xcrun xctest -XCTest AreaMatrixTests.ErrorRecoveryStartupRecoveryPageFeatureTests ...`：6 tests passed
- `./dev test macos`：标准 `xcodebuild test` 被本机 `testmanagerd.control` sandbox 限制挡住，脚本按规则 fallback 到 `xcrun xctest`，最终 404 tests passed

六、风险与后续

残余风险：未做手工 macOS UI 点击验收，只完成 build 和测试闭环。回滚方式是还原上述两个 Swift 文件。建议下一个任务进入 `2-3/task-30` 的 S1-32 page integration verify，用整页视角确认 C1-16 + C1-21 覆盖完整。

