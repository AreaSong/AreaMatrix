**执行结论**
- 已完成。

**执行范围**
- 单任务：`2-3/task-24`
- 修改文件：
  - [apps/macos/AreaMatrix/Views/Settings/AdvancedSettingsPane.swift](</Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Settings/AdvancedSettingsPane.swift:3>)
  - [apps/macos/AreaMatrix/Views/Settings/GeneralSettingsView.swift](</Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Settings/GeneralSettingsView.swift:103>)
  - [apps/macos/AreaMatrixTests/AdvancedSettingsPageFeatureTests.swift](</Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/AdvancedSettingsPageFeatureTests.swift:4>)

**完成情况**
- 核对清单：
  - 只处理了 `S1-30` 里的 `C1-16` 入口。
  - 读了页面规格、control map、Core API、`C1-16` 能力规格。
  - 只接了恢复入口到现有恢复路由，没有补做同页其他 Core 能力。
  - 没有顺手碰相邻页面。
- 完成标准：
  - `S1-30` 里的恢复工具入口已可触发。
  - 没有用 mock/fixture 伪造恢复闭环。
  - 没有触碰 `control map` 之外的 Core 能力。
- `Forbidden Touches`：未触碰 `core/src/**`。

**工程质量**
- `AdvancedSettingsPane` 现在在错误态和 Danger zone 内都能暴露 `Open recovery tools...`，入口只是转交到父级恢复路由，不在设置页里直接执行恢复。
- `GeneralSettingsView` 把 `onOpenRepositoryRecovery` 下传到高级设置页，路由链路和现有恢复确认流对齐。
- 新增测试覆盖了：
  - 恢复工具 section 的可见文案、按钮和点击触发。
  - 从高级设置入口转到恢复确认路由，但不在页面内直接触发 `recover_on_startup`。
- 没有新增占位、硬编码完成态或一次性脚本化逻辑。

**验证情况**
- 已运行：
  - `git diff --check`：通过
  - `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过
  - `./dev test macos`：`xcodebuild test` 被本地 `testmanagerd` sandbox 阻断；脚本回退到 `xcrun xctest`，通过
- 结果：
  - `xcrun xctest` 最终通过，386 个测试，0 失败。
- 未运行：
  - 无。

**风险与后续**
- 剩余风险：没有做真人工点按的 UI 冒烟，只靠 build/test 证明入口和路由。
- 建议下一个任务：继续做 `S1-30` 其余页面功能点的 page-feature 收口，最后再跑 page integration verify。