一、执行结论

已完成 `2-2/task-22`。

二、执行范围

单任务：只处理 `S1-22-conflict-duplicate` 中 `C1-09 detect-duplicate` 功能点。

修改文件清单：
- `apps/macos/AreaMatrix/Models/ImportSingleFileDuplicateResolution.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreflight.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreflightCorePreview.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModel.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModelState.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportSingleFileSheetSections.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFilePageIntegrationVerifyTests.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFilePreviewModelTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

工作树里另有执行前已存在的 `tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260507_210821/`，未纳入本任务改动范围。

三、完成情况

核对清单全部完成：已读取页面规格、control map、Core API、C1-09 能力规格，并只接入 S1-22 的重复文件检测/处理入口。没有实现相邻页面，也没有扩展 control map 外能力。

完成标准全部满足：S1-22 现在可在重复文件冲突处选择跳过、保留两份、替换确认；生产路径使用真实 `CoreBridge` duplicate strategy，未用 mock 或静态状态伪造 Core 闭环。`core/src/**` 未触碰。

四、工程质量

结构上把重复文件策略拆到独立模型，View 只负责展示和绑定，Model 负责状态、禁用原因和 CoreBridge 参数映射。错误和边界处理覆盖了 skip 不调用 Core、keepBoth 无可用名称时阻断、replace 必须走二次确认。未新增依赖，未修改 Rust public API，因此无 rustdoc/doc sync 变更。

五、验证情况

已通过：
- `./dev test macos`
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`

`xcodebuild` 有本机 CoreSimulator/testmanagerd sandbox 相关噪音，但 `./dev test macos` 已按仓库脚本 fallback 到直接 XCTest，并最终通过；manifest 要求的 build 也显示 `** BUILD SUCCEEDED **`。

六、风险与后续

剩余风险：尚未做真实 UI 人工点击截图验收，但 build/test 已覆盖状态与 CoreBridge 参数路径。高风险边界未写入、删除、移动或覆盖任何真实用户文件；回滚可直接 revert 上述 macOS app/test/project 文件。

建议下一个任务按队列继续执行 `2-2/task-23`。