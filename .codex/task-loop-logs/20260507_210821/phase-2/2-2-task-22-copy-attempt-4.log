一、执行结论

已完成。`2-2/task-22` 的 S1-22 + C1-09 修复已落地，并通过本地 build/test 验证。

二、执行范围

单任务：`2-2/task-22`

本次代码修复集中在：

- `apps/macos/AreaMatrix/Models/ImportSingleFilePreflight*.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModel*.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFileDuplicateResolution.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetView.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportSingleFileSheetSections.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySingleFileFooter.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportReplaceConfirmSheet.swift`
- `apps/macos/AreaMatrixTests/ImportSingleFile*.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

工作树里另有 `tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-*` 运行痕迹，未作为本次产品代码修复范围处理。

三、完成情况

- 核对清单：已按 S1-22 + C1-09 收敛，只保留 Duplicate 的 Skip / Keep Both 单页功能点。
- 真实闭环：`CoreImportSingleFilePreflight` 不再调用缺失的 `preview_import`，改为 Swift 计算源文件 SHA256，并通过真实 `CoreBridge.listFiles` / `CoreFileListing` 读取 Core 文件索引 hash 做导入前重复检测。
- 错误与状态：hash 检查显示 `Checking duplicate...`；重复默认 Skip；Keep Both 只在最终 Import 时用 Core `.keepBoth` 策略。
- 相邻页面：单文件 S1-22 不再进入 S1-24 replace confirmation；Replace sheet 只保留给已有 batch/folder 路径。
- Forbidden Touches：未修改 `core/src/**`。

四、工程质量

代码没有新增依赖，没有 mock-only 真实闭环，也没有用静态状态伪造 duplicate preflight。测试已从“断言 preview_import gap”改成真实 Core repo 初始化、真实 import、真实 `list_files` hash preflight 的集成证据。剩余非阻断项是 macOS 测试里已有 Swift 6 Sendable warning，当前 Swift 5/Xcode 门禁不失败。

五、验证情况

已通过：

```bash
xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO
```

结果：`BUILD SUCCEEDED`

已通过：

```bash
./dev test macos
```

结果：标准 `xcodebuild test` 仍被本地 `testmanagerd` sandbox 阻断；脚本按 repo-local validation-driver fallback 到 `xcrun xctest`，`261 tests, 0 failures`，最终命令退出码为 0。

额外检查：

```bash
git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj
```

结果：通过。

六、风险与后续

剩余风险：未执行真实 GUI 手动点击验证；当前证据来自 model/unit/integration tests 和 macOS build。建议下一个动作是进入 `2-2/task-22` 的 verify-ready 验收；若通过，再继续 `2-2/task-23`。  
