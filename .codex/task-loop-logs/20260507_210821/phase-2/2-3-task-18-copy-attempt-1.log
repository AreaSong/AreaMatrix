一、执行结论：已完成。

二、执行范围：单任务 `2-3/task-18`。修改集中在 `apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`apps/macos/AreaMatrix.xcodeproj/project.pbxproj`。未触碰 `core/src/**`。

三、完成情况：S1-27 的 `C1-04 / C1-08 / C1-20` 已串到整页闭环：repo config/health、Indexed 文件计数、generated overview、Finder reveal、Copy path、Change repository 返回语义、recovery 入口、diagnostics privacy confirmation 都已接线。新增 `RepositorySettingsPageIntegrationVerifyTests` 用真实 `CoreBridge` 初始化 repo、Indexed import、读取 config/list files/generated overview，避免 mock-only 通过。

四、工程质量：实现保持在当前页 integration 范围内，没有新增 control map 外能力，没有新增依赖。平台行为通过可注入 adapter 隔离，SwiftUI 视图不直接做文件 IO；错误态有 banner / recovery 文案；测试临时 repo/source 目录已补完整清理。

五、验证情况：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：已运行，构建通过，但本环境被 `testmanagerd` sandbox 通信限制拦截，`xcodebuild` 返回 65。
- `xcrun xctest .../AreaMatrixTests.xctest`：通过，359 tests，0 failures。
- `./dev test macos`：通过；脚本检测到同一 `testmanagerd` sandbox block 后复用 XCTest bundle，359 tests，0 failures。

六、风险与后续：剩余风险是本地沙箱导致原始 `xcodebuild test` 不能以 0 退出，但仓库认可的 fallback 已完整通过。当前工作树里仍有既有脏改动 `.codex/task-loop-runs/20260507_210821/summary.json` 和 `tasks/prompts/_shared/progress.json`，我没有修改它们。

