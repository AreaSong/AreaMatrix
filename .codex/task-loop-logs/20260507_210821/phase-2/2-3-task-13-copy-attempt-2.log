**一、执行结论**

已完成。`2-3/task-13` 的 P0 阻塞已修复：S1-26 保存的 `defaultMode = Moved` 现在会进入后续 ImportSheet 默认存储模式，而不是继续硬编码 `.copy`。

**二、执行范围**

单任务：`2-3/task-13`。

本轮产品修复涉及：

- `apps/macos/AreaMatrix/Models/ImportEntryRequest.swift`
- `apps/macos/AreaMatrix/Models/OnboardingModelInputs.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModelState.swift`
- `apps/macos/AreaMatrix/Models/ImportSingleFilePreviewModel.swift`
- `apps/macos/AreaMatrix/Models/ImportBatchCopyImportModel.swift`
- `apps/macos/AreaMatrix/Models/ImportFolderPreviewModel.swift`
- `apps/macos/AreaMatrixTests/GeneralSettingsImportDefaultModeTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

保留了上次 attempt 已有的 `GeneralSettingsPageFeatureTests.swift` 设置持久化回归测试和 task-loop 进度/日志脏项，未回滚。

**三、完成情况**

核对清单全部完成：

- 只处理 S1-26 + C1-07 默认 Move 功能点。
- 已读取 UX 页面、control map、Core API、C1-07 能力规格。
- 只接入默认存储模式到已有 CoreBridge/import 状态链路。
- 未实现相邻页面或新 Core 能力。

完成标准全部满足：

- S1-26 保存 Move 后，后续 single / batch / folder ImportSheet 默认选中 Move。
- 未使用 mock 伪造真实保存闭环；保存 Move 的真实 CoreBridge 回归测试仍覆盖。
- `core/src/**` 未触碰，Forbidden Touches 清晰。

**四、工程质量**

数据流已贯通为：

`RepoConfigSnapshot.defaultMode -> ImportEntryRequest.defaultStorageMode -> Import*Model.selectedStorageMode`

ImportSheet 仍可临时修改存储模式；默认值只在新 request 初始化时应用，不覆盖用户在 sheet 内的选择。没有新增依赖，没有新增 public Rust API，没有一次性脚本或硬编码通过态。

**五、验证情况**

已通过：

```bash
xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO
```

结果：`BUILD SUCCEEDED`

已通过：

```bash
./dev test macos
```

标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 通信限制阻断；脚本按仓库规则复用已构建 XCTest bundle 执行 `xcrun xctest`。结果：341 tests，0 failures。

补充检查：

- `git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：通过
- Forbidden Touches 检查：`core/src/**` 未出现在 diff 中

**六、风险与后续**

剩余风险：本地标准 `xcodebuild test` 的 testmanagerd 通信仍受 sandbox 限制，但 repo-local fallback 已完整执行 XCTest bundle 并通过。当前工作树还有 task-loop summary/progress/log 的既有脏项，属于运行证据，不是本轮产品修复扩散。

建议下一个任务：进入 `2-3/task-13` verify-ready 验收；通过后再继续后续 `2-3/task-14`。

