一、执行结论

已完成 `2-2/task-22` repair retry。

二、执行范围

单任务：S1-22 conflict-duplicate 中的 C1-09 detect-duplicate。

修改/新增集中在允许范围：
- `apps/macos/AreaMatrix/**`
- `apps/macos/AreaMatrixTests/**`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

另：工作树里仍有 task-loop 运行态文件 `tasks/prompts/_shared/progress.json`、`.codex/task-loop-*`，不是本次产品修复范围；未回滚。

三、完成情况

- 已把单文件导入预检改为：先 hash 源文件，再通过真实 `CoreBridgeBatchFileLoader`/`listFiles` 读取 Core 文件快照，比对 `hashSha256`，在导入前渲染 S1-22 duplicate conflict。
- 已补齐 S1-22 duplicate 策略：默认 Skip 不写入；Keep Both 使用 Core 列表计算自动编号预览并最终传 `.keepBoth`；Replace 在设置允许且 Trash 可用时显示，必须二次确认后才传 `.overwrite`。
- 已补测试覆盖 pre-import duplicate、Keep Both 真实 Core 路径、Replace 可见性/禁用/二次确认。
- Forbidden Touches：未触碰 `core/src/**`；`git diff --name-only` 未出现该路径。

四、工程质量

代码拆分为 preflight、duplicate resolution、presentation、footer action，避免继续堆在单个 view/model 文件里。错误路径保留了 iCloud placeholder、非法文件名、Core list 失败、Keep Both 无可用编号、Replace 未确认/Trash 不可用等显式阻断。未引入新依赖，未使用 mock-only 或静态状态作为最终闭环证据；单元测试中的静态 loader 只用于窄范围 preflight 行为断言。

五、验证情况

已通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
- `./dev test macos`：标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 拦截后，脚本按 validation driver fallback 使用 `xcrun xctest`，263 tests passed，0 failures。
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`：OK，637 tasks / 637 manifests。
- `git diff --check`：通过。

六、风险与后续

剩余风险：Core 侧仍没有专用 `preview_import` API，本次在不触碰 `core/src/**` 的约束下使用真实 Core `listFiles` + Swift hash 预检完成 S1-22 首次渲染闭环。建议下一个任务进入该队列的后续 verify 或相邻 S1-23/S1-24 专项，不要在本 task 里继续扩功能。

