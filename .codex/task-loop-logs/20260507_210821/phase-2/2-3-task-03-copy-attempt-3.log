一、执行结论

- 已完成

二、执行范围

- 单任务：`2-3/task-03`
- 本次 repair 直接修复：[MainExternalCreatedFileWatcher.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/App/MainExternalCreatedFileWatcher.swift:1)
- 延续当前 task 已落地链路：FSEvents Created watcher -> `AreaMatrixExternalCreatedFileRelay` -> `MainWindow` -> `MainRepositoryContentView` -> `MainFileListModel.syncExternalCreated` -> `CoreBridge.syncExternalCreated`

三、完成情况

- 核对清单：已只处理 S1-13 + C1-17，没有实现 C1-18/C1-19 或相邻页面。
- 完成标准：生产入口已补齐。`MainWindow` 启动当前主仓库 watcher，并把外部新增事件传入 S1-13 页面；页面触发真实 CoreBridge `.created` 同步，不靠测试注入。
- Forbidden Touches：未触碰。`git status --short -- core/src` 和 `git diff --name-only -- core/src` 均无输出。

四、工程质量

- 修复了 watcher 与 `@StateObject` 的生命周期契约，使 `MainExternalCreatedFileWatcher` 符合 `ObservableObject`。
- 将 watcher 的纯过滤函数标记为 `nonisolated`，避免测试为了 actor 隔离改写业务入口。
- watcher 只发布 Created 文件事件，过滤 root changed、must scan、history done、目录、仓库外路径和 `.areamatrix/**`，不删除、移动、覆盖用户文件。
- 无新增 public Rust API，无需 rustdoc；未新增依赖；无 mock-only 或硬编码通过态。

五、验证情况

- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过，`BUILD SUCCEEDED`。
- `./dev test macos`：通过。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断，脚本按既有规则改用已构建 XCTest bundle 直接执行；`296 tests` 全部通过。
- `git diff --check`：通过。
- Forbidden Touches 检查：通过。

六、风险与后续

- 剩余风险：本地验证覆盖了 watcher 过滤、生产 relay、UI 触发、CoreBridge 真实同步和错误映射；未做真实长时间 FSEvents 人工交互观察。
- 回滚思路：回退本任务在 `apps/macos/AreaMatrix/**`、`apps/macos/AreaMatrixTests/**`、`project.pbxproj` 的改动即可；`core/src/**` 未变更。
- 建议下一个任务：进入 `2-3/task-03` 的 verify-ready 只读验收，重点复核生产触发入口是否已满足上一轮 P0。 

