一、执行结论

已完成。

二、执行范围

单任务：`2-3/task-32`，仅修复上次验收失败的两个点：

- S1-12 detail-meta 增加 `Rename...` 文件操作入口，并路由到已有 S1-33 sheet。
- S1-33 `New name` 输入框打开时聚焦，并默认选中文件名主体，扩展名保持可见。

主要修改文件：

- [MainRepositoryDetailPane.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/MainRepositoryDetailPane.swift:287)
- [MainRepositoryContentNoteDrafts.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/MainRepositoryContentNoteDrafts.swift:52)
- [RenameFileSheet.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/RenameFileSheet.swift:53)
- [RenameFilePageFeatureTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/RenameFilePageFeatureTests.swift:64)

三、完成情况

核对清单：4/4 完成。只处理 S1-33 + C1-22；未实现相邻页面能力；真实路径仍走 `CoreBridge` / `rename_file`；S1-12 detail 入口现在能打开同一个 rename sheet。

完成标准：3/3 满足。S1-33 用户可触发；没有 mock-only 闭环；未触碰 control map 外能力。

Forbidden Touches：未触碰。`git diff --name-only -- core/src` 无输出。

四、工程质量

代码结构清晰：detail pane 只新增入口；rename sheet 的焦点/选中逻辑封装为独立输入控件和 selection helper。没有新增依赖，没有 public Rust API，也未改 Core/UDL。错误处理沿用现有 rename failure 映射，失败时保留 sheet 和输入内容。

新增回归测试覆盖 detail-meta rename 路由、写锁禁用、默认主体选中且扩展名可见。

五、验证情况

已运行并通过：

- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`
- `./dev test macos`：标准 `xcodebuild test` 被本地 sandboxed `testmanagerd` 通信限制阻断；脚本按规则复用已构建 XCTest bundle，`xcrun xctest` 通过，`415 tests passed`
- `git diff --check`：通过
- Swift 120 行宽扫描：通过
- `python3 tasks/prompts/_shared/prompt_pipeline.py verify --task 2-3/task-32`：verify prompt 可正常生成

未运行：

- `swiftformat --lint .` / `swiftlint --strict`：当前环境未安装对应命令，`command -v` 无输出。

六、风险与后续

剩余风险：未做真实 UI 手工点击截图验证；但 build、XCTest 和新增测试已覆盖本次两个验收缺口。回滚方式是撤销本次 macOS app/test/project 相关改动。

建议下一步进入 `2-3/task-32` verify-ready 只读验收；通过后继续 `2-3/task-33`。

