一、执行结论：已完成。

二、执行范围：单任务 `4-1/task-136`。修改集中在 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**`，未触碰 `core/src/**`、`core/area_matrix.udl`、Xcode project 文件。

三、完成情况：
- 修复空手动选择作用域：关闭全量作用域且未选中行时，现在显示 `Select at least one conflict.`，并禁用 Apply / Ask per item，不再落到 `No conflicts remain`。
- 修复 `Ask per item` 闭环：不再只伪装成 batch apply；现在通过 Core preview/apply 的 `.askPerItem` 路径生成逐项队列，并暴露 S1-22 / S1-23 / S1-24 路由证据。
- 补齐 S2-21 集成测试证据：覆盖空选择、选中作用域刷新、blocked preview 不 apply、apply 后 Undo 加载与 undo。
- 未新增 control map 外能力；生产路径仍走 `CoreBridge`/Core 协议，不用 mock-only 闭环。

四、工程质量：
- 代码拆在既有 model / state / view / test support 内，没有新增 project 文件或新依赖。
- 关键文件行数已控制在阈值内：`ImportBatchConflictActions.swift` 500 行，`ImportBatchProgressItems.swift` 450 行，相关测试文件也不超过 500 行。
- 错误与边界路径已显式处理：空 scope、blocked preview、replace confirmation、ask-per-item queue、Undo 刷新。
- 高风险影响仅限 macOS 页面状态与 CoreBridge 调用 wiring；不执行真实用户文件删除、移动、覆盖。回滚方式是撤回本次 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**` 变更。

五、验证情况：
- 已运行并通过：`./dev check task 4-1/task-136`
- 该命令内含：
  - `prompt_pipeline.py doctor`: OK，tasks 637，manifests 637
  - `git diff --check`: 通过
  - `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination platform=macOS,arch=arm64 build CODE_SIGNING_ALLOWED=NO`: `BUILD SUCCEEDED`
- 未运行：`./dev test macos`，因为当前 manifest 的 Validation 上限是 `./dev check task 4-1/task-136`，本次 retry 未升级门禁。

六、风险与后续：
- 剩余风险：未做真实窗口点击级 UI smoke；当前证据为 model/view wiring + macOS build gate。
- 建议下一个任务：进入本 task 的 verify-ready 验收，重点复核 S2-21 的 `Ask per item` 路由队列和空手动选择状态。

