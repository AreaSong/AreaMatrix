一、执行结论

已完成。`4-1/task-137` 已按 S2-22 + C2-18 范围接入真实 `listRedoActions` / `redoAction` CoreBridge 路径，未触碰 `core/src/**`。

二、执行范围

单任务：`4-1/task-137`。

修改文件清单：
- `apps/macos/AreaMatrix/Bridge/CoreTagCRUD.swift`
- `apps/macos/AreaMatrix/Models/BatchRenameUndoAction.swift`
- `apps/macos/AreaMatrix/Models/MainFileListDetailSupport.swift`
- `apps/macos/AreaMatrix/Models/MainFileListDiagnosticsActions.swift`
- `apps/macos/AreaMatrix/Views/Main/BatchRenameTrigger.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportBatchCopyFooterSection.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportBatchOverviewSections.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportProgressListIntegration.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentCategoryMoveRefresh.swift`
- `apps/macos/AreaMatrixTests/ImportProgressPageIntegrationVerifyTests.swift`
- `apps/macos/AreaMatrixTests/S214BatchRenameUndoPageFeatureTests.swift`

三、完成情况

核对清单：已只处理 S2-22 中 C2-18 redo-action-log；已读取并对齐页面规格、control map、Core API、C2-18 能力规格；只接入 C2-18 的 CoreBridge、状态和错误映射；未实现同页 C2-07 之外的新能力。

完成标准：Redo 可在 S2-10 toast slot 与 S2-11 Undo History 中触发；使用真实 CoreBridge 调用，不是 mock/fixture/静态状态闭环；未触碰 control map 之外能力。

Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

实现把 redo snapshot、状态机、错误映射、toast feedback region、Undo History redo row 和 `Shift+Cmd+Z` 入口分层接入，避免把 Core 调用写进 View 内部业务分支。错误态覆盖 unavailable、cleared、blocked、expired、failed；Redo 失败会保留历史行和原因。未新增依赖，未加入 mock-only 或硬编码完成态。文档未改，因为本 task 的 Core API / UDL 已存在且无需同步。

五、验证情况

已通过：
- `./dev check task 4-1/task-137`
  - `prompt doctor`: OK
  - `git diff --check`: OK
  - macOS `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`: `BUILD SUCCEEDED`

补充验证：
- 尝试运行限定 XCTest：`xcodebuild ... test -only-testing:AreaMatrixTests/S214BatchRenameUndoPageFeatureTests -only-testing:AreaMatrixTests/ImportProgressPageIntegrationVerifyTests CODE_SIGNING_ALLOWED=NO`
- 结果：未通过，原因是 test target 构建先在未改动的 `ImportBatchDuplicateResolutionTests.swift:30/31/58/59` 暴露既有 actor-isolation 编译错误；未进入本 task 用例执行阶段。未按任务要求扩大修复范围。

六、风险与后续

剩余风险：本机 task gate 已通过，但 XCTest 目标当前有无关编译缺口，导致本 task 新增用例未能在 XCTest runner 中执行。回滚方式是回退上述 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**` 改动；不涉及用户文件、DB schema、迁移或 `core/src/**`。建议下一个任务先处理 macOS test target 的既有 actor-isolation 编译问题，再补跑相关 XCTest。工作区还存在 `.codex/...` 与 `tasks/prompts/_shared/progress.json` 的运行状态改动/日志，不计入本次产品实现范围。  
