一、执行结论

已完成。`4-1/task-125` 的 repair retry 已修复上次验收指出的 C2-11 command target 只展示不可执行的问题，并补了可触发/路由证据。

二、执行范围

单任务：`4-1/task-125`

修改文件清单：
- `apps/macos/AreaMatrix/Bridge/CoreBridge.swift`
- `apps/macos/AreaMatrix/Models/MainFileActionRoutingActions.swift`
- `apps/macos/AreaMatrix/Models/MainFileListModel.swift`
- `apps/macos/AreaMatrix/Models/MainFileRenameActions.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportProgressListIntegration.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportProgressView.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentView.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryMultiSelectionActions.swift`
- `apps/macos/AreaMatrixTests/ImportBatchICloudPageIntegrationTests.swift`
- `apps/macos/AreaMatrixTests/MainEmptyImportEntryTests.swift`
- `apps/macos/AreaMatrixTests/MainLoadingFixtures.swift`
- `apps/macos/AreaMatrixTests/MainRepoExternalRemovalTests.swift`

三、完成情况

核对清单已完成：只处理 S2-15 + C2-11；接入真实 `CoreBridge.listCommandTargets`；新增 command palette 状态、错误映射、target action/route 分发；未实现相邻页面能力。C2-04 Smart List 只复用既有入口，没有新增该能力实现。

完成标准已满足：Core 返回的 `CommandTarget` 现在渲染为可执行 `Button`，支持选中、Enter 执行、危险命令确认标记，并按 `action/route` 打开对应安全 route，例如 S2-13 删除确认，不在面板内直接执行危险动作。

Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 无输出。

四、工程质量

代码结构按 Core index snapshot、UI rows、route dispatcher、batch route builder 分层，避免把路由逻辑塞进 View。错误路径保留 `CommandPaletteLoadState.failed` 和 `mapCoreError`。无新增依赖；未新增 Rust public API，因此无 rustdoc 变更。产品路径没有 mock-only 或硬编码通过态，测试中的 stub 仅用于验证 CoreBridge 边界和 UI/route 行为。

五、验证情况

已通过：
- `git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests`
- `./dev test macos --only-testing AreaMatrixTests/MainEmptyImportEntryTests --keep-derived-data`：12 个测试通过；本机 `xcodebuild test` 被 `testmanagerd` sandbox 限制阻断后，repo runner 复用已构建 XCTest bundle，经 `xcrun xctest` 通过。
- `./dev check task 4-1/task-125`：`doctor: OK`、`git diff --check` 通过、macOS build gate `BUILD SUCCEEDED`。

六、风险与后续

剩余风险：当前未做真实窗口点击截图；本 task manifest 验证上限是 `./dev check task 4-1/task-125`，没有升级到全量门禁。风险控制点是危险命令只路由到确认/预览 sheet，不直接删除、移动、重命名或改写用户文件。回滚方式是回退上述 `apps/macos/AreaMatrix/**` 和 `apps/macos/AreaMatrixTests/**` 修改。

建议下一个任务：进入 `4-1/task-125` verify-ready 验收；随后再跑 S2-15 page integration verify，确认 C2-04 与 C2-11 在整页层面的覆盖完整性。

