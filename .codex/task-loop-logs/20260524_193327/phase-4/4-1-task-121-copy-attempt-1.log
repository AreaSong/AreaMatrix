一、执行结论

已完成 `4-1/task-121`。S2-14 批量重命名的 C2-10 preview/apply 闭环已接入真实 `CoreBridge`，没有 mock/fixture 伪造 Core 结果。

二、执行范围

单任务：`S2-14 + C2-10 batch-rename-preview`。

修改文件清单：
- `apps/macos/AreaMatrix/Bridge/CoreFileDeleting.swift`
- `apps/macos/AreaMatrix/Models/BatchDeleteState.swift`
- `apps/macos/AreaMatrix/Models/MainFileListModel.swift`
- `apps/macos/AreaMatrix/Models/MainFileListState.swift`
- `apps/macos/AreaMatrix/Views/Main/BatchDeleteConfirmSections.swift`
- `apps/macos/AreaMatrix/Views/Main/FileDropAdapter.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportProgressListIntegration.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentActionRouting.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentNoteDrafts.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentView.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryDetailPane.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryMultiSelectionActions.swift`
- `apps/macos/AreaMatrixTests/ImportBatchICloudPageIntegrationTests.swift`
- `apps/macos/AreaMatrixTests/MainRepoExternalRemovalTests.swift`

三、完成情况

核对清单通过：只处理 S2-14 的 C2-10；读取并按 UX/Core API/UDL 现有合同接入；未实现同页 C2-07 undo 消费。  
完成标准通过：用户可从多选详情、列表 context menu、command palette 打开批量重命名；预览与应用走 `previewBatchRename` / `batchRename` 真实 CoreBridge。  
Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

代码包含显式错误映射、预览新鲜度校验、选择集校验、失败展示和 apply 后刷新列表/详情。没有新增依赖、mock-only、硬编码通过态或一次性脚本逻辑。

残余技术债：受 manifest 禁止范围约束，本次没有修改 `AreaMatrix.xcodeproj`，因此把新类型收敛进已在 target 内的相邻 Swift 文件；可维护性不是最理想，但保证了 task 验证真实编译到实现。

五、验证情况

已运行：

```bash
./dev check task 4-1/task-121
```

结果：通过。包含 `prompt doctor`、`git diff --check`、macOS `xcodebuild build`。`xcodebuild` 有 CoreSimulator 连接 warning，但最终 `** BUILD SUCCEEDED **`。

六、风险与后续

High 风险已按静默授权继续执行；本次 UI 只触发批量重命名预览/应用，不删除、移动或覆盖用户原文件的额外路径。回滚方式是还原上述 app/test 文件改动。建议下一个任务继续执行 `4-1/task-122`，覆盖同页 C2-07 undo/log 相关能力。  
另有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍为既有脏文件，我未处理或回滚。

