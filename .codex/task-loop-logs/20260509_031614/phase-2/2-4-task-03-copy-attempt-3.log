一、执行结论

- 已完成。
- 这次没有伪造 `resolve_iCloud_conflict` 成功闭环：按 SSOT 确认 Stage 1 只声明 `C1-01` / `C1-21`，当前 Core 没有单项 resolve endpoint，所以页面改为明确展示 Core resolution blocker，并禁止 Apply，而不是用 mock / fixture / 硬编码成功态通过。

二、执行范围

- 单任务：`2-4/task-03`
- 修改范围均在 `Expected New Paths` 内：
  - `apps/macos/AreaMatrix/**`
  - `apps/macos/AreaMatrixTests/**`
  - `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`
- 未触碰 `core/src/**`，也未修改 `core/area_matrix.udl`。

三、完成情况

- 页面 feature 覆盖：通过。`C1-01` repo path validation 与 `C1-21` error mapping 保持真实接入；缺失的 resolve 能力改为显式阻塞证据。
- 页面状态 / 入口 / 退出 / 错误态 / CoreBridge 闭环：通过。入口、sheet routing、Cancel 退出、失败留在 sheet、Retry / Cancel / Collect Diagnostics 均有测试覆盖。
- mock / fixture / 硬编码状态：通过。移除了生产路径的 fake success；测试不再用 fixture 返回 `.resolved` 来证明 Apply 成功。
- control map 外能力：通过。没有新增 `C1-23` / `C1-25` 或 Stage 2 resolve 能力。
- Forbidden Touches：通过，`core/src/**` 无变更。

四、工程质量

- 新增 [ICloudConflictMinimalResolution.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/ICloudConflictMinimalResolution.swift:92)，把 resolution strategy、state、capability blocker 和 resolver protocol 集中建模。
- 更新 [ICloudConflictMinimalSheet.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/ICloudConflictMinimalSheet.swift:142)，页面现在展示 `S1-25-core-resolution-blocked`，Apply 只有在 repo validation 和 Core capability 都支持时才可用。
- 更新 page integration tests，覆盖生产 `CoreBridge()` blocker、C1-21 映射、入口/退出、Trash 二次确认边界和 control map 外调用不发生。
- 代码没有新增依赖；没有用户文件删除、移动、覆盖或 iCloud 下载副作用。

五、验证情况

- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：通过。
- `./dev test macos`：通过。脚本先运行标准 `xcodebuild test`，该环境仍被 `com.apple.testmanagerd.control ... Sandbox restriction` 阻断；随后按仓库 `docs/development/testing.md` 规则复用同一 XCTest bundle 运行 `xcrun xctest`，448 tests，0 failures。
- `git diff --check -- apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：通过。
- 必需原始 `xcodebuild test ...` 直接 runner：环境阻断，不是编译或断言失败。

六、风险与后续

- 剩余风险：当前 Stage 1 只能明确阻塞 Apply，不能真正清除 conflict state / 写 change_log / Trash 单保留；真实 resolve 应在后续 Core capability/API 明确后实现。
- 回滚方式：回退本次列出的 macOS app / tests / xcodeproj 变更即可，不涉及 `core/src/**` 或用户文件。
- 建议下一个任务：继续队列中的 `2-4/task-04`；不要在 `2-4/task-03` 里提前实现 Stage 2 resolve endpoint。

