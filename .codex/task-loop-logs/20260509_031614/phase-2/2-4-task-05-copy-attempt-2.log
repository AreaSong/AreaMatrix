一、执行结论

已完成。`2-4/task-05` 的 repair retry 已按上次验收失败点收口：去掉 S1-37 中越界的 C1-16 操作、修正 diagnostics snapshot 长任务调用、补齐 Cancel 返回来源错误页的路由证据。

二、执行范围

单任务：`2-4/task-05`

修改范围均在 Expected New Paths 内，主要文件：

- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`
- `apps/macos/AreaMatrix/Bridge/CoreBridge.swift`
- `apps/macos/AreaMatrix/Bridge/CoreMetadataRepairing.swift`
- `apps/macos/AreaMatrix/Models/DatabaseRepairConfirmModel.swift`
- `apps/macos/AreaMatrix/Models/OnboardingDatabaseRepairRouting.swift`
- `apps/macos/AreaMatrix/Models/OnboardingRoute.swift`
- `apps/macos/AreaMatrix/Models/OnboardingInitializationProgress.swift`
- `apps/macos/AreaMatrix/Models/AppShellModel.swift`
- `apps/macos/AreaMatrix/Views/Main/DatabaseRepairConfirmView.swift`
- `apps/macos/AreaMatrix/Views/MainWindow.swift`
- 相关 macOS tests：`DatabaseRepairConfirmPageFeatureTests.swift`、`MainRepoErrorMappingTests.swift`、`AdvancedSettingsPageFeatureTests.swift`、集成验收测试与 smoke tests

三、完成情况

- 核对清单：已完成。S1-37 仅保留 C1-26 repair/reindex metadata 功能点，未再暴露 `Resume` / `Clean up and retry` 等 C1-16 操作。
- 完成标准：已完成。页面通过真实 `CoreBridge` / UniFFI repair metadata 路径触发，不是 mock-only 或静态状态。
- `createDiagnosticsSnapshot`：已改为 `Task.detached(priority: .userInitiated)` 包裹，符合 Core API 长任务要求。
- Cancel 返回：已新增来源路由状态，支持从 validate path、main repo error、settings 等来源返回原页面。
- Forbidden Touches：未触碰。`git diff -- core/src` 为空。

四、工程质量

代码结构已拆分，`OnboardingInitializationProgress.swift` 控制在 491 行，新增 `OnboardingDatabaseRepairRouting.swift` 承担 S1-37 返回路由逻辑。错误路径保留显式映射；没有新增依赖、没有新增 Rust public API、没有文档或 UDL 改动。`git diff --check` 已通过，未发现空白格式问题。

五、验证情况

已运行并通过：

```bash
xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO
```

结果：`** BUILD SUCCEEDED **`

已运行：

```bash
./dev test macos
```

结果：标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断；脚本按仓库策略复用已构建 XCTest bundle 执行 `xcrun xctest`，结果通过：`465 tests, 0 failures`。

六、风险与后续

剩余风险：未做手工 UI 交互验证，CI 也未在当前环境运行。用户文件安全方面，本次只改 Swift UI/路由/Bridge 调用与测试，未修改 `core/src/**`，未加入删除、移动、覆盖用户文件的行为。

建议下一个任务：继续 `2-4/task-06`，处理 S1-37 同页 C1-16 secondary capability，或由后续 page integration verify 做整页覆盖验收。

