一、执行结论

- 已完成

二、执行范围

- 单任务：`2-4/task-06`
- 修改文件：
  - `apps/macos/AreaMatrix/Models/DatabaseRepairConfirmModel.swift`
  - `apps/macos/AreaMatrix/Views/Main/DatabaseRepairConfirmView.swift`
  - `apps/macos/AreaMatrixTests/DatabaseRepairConfirmPageFeatureTests.swift`
- 未触碰 `core/src/**`，未修改 `core/area_matrix.udl` 或 `project.pbxproj`。
- 工作树中另有既存 task-loop 状态文件变更：`.codex/task-loop-runs/.../summary.json`、`tasks/prompts/_shared/progress.json`，本次未作为产品改动处理。

三、完成情况

- C1-16 已接入 S1-37：页面打开后通过 `CoreStartupRecovering.recoverOnStartup(repoPath:)` 触发真实 CoreBridge 边界。
- 新增 startup recovery 状态：checking / completed / failed，并展示 cleaned staging、reverted rows、warnings、结构化错误和 retry。
- C1-26 原有 metadata repair / diagnostics 流程保持不扩展；startup recovery 检查中会禁用 repair/export，避免并发 metadata 写动作。
- 未实现相邻页面，未触碰 control map 外能力。
- Forbidden Touches：未触碰。

四、工程质量

- 结构保持在现有 Model/View/Test 模式内，没有新增不必要抽象。
- 生产路径没有 mock、fixture、硬编码成功态；测试 fake 只用于 XCTest。
- 错误通过现有 `CoreErrorMapping` 映射；非 Core error 继续走内部错误包装。
- 无 Rust public API 变更，因此不涉及 rustdoc；无文档或依赖变更。

五、验证情况

- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过。
- `xcrun xctest -XCTest AreaMatrixTests.DatabaseRepairConfirmPageFeatureTests apps/macos/DerivedData/AreaMatrix/Build/Products/Debug/AreaMatrixTests.xctest`：通过，10 tests。
- `./dev test macos`：通过；标准 `xcodebuild test` 被本机 `testmanagerd.control` sandbox 阻断后，脚本 fallback 到 `xcrun xctest`，全量 469 tests 通过。
- `git diff --check -- <本次3个文件>`：通过。
- `swiftformat` / `swiftlint`：当前 PATH 未找到可执行文件，未运行。

六、风险与后续

- 剩余风险：标准 Xcode test runner 在当前沙箱下不可直接通信，但仓库认可的 fallback 已跑过全量测试。
- 建议下一个任务：`2-4/task-07`，做 S1-37 page integration verify，检查 C1-26 + C1-16 整页覆盖闭环。 

