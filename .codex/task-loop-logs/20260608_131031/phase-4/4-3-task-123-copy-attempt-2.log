一、执行结论

- 已完成

二、执行范围

- 单任务：`4-3/task-123`
- 修改范围：仅 `apps/**`
- 修改文件：Windows OneDrive notice、Windows main window wiring、轻量 watcher status route/view、Windows tests
- 未触碰：`core/src/**`

三、完成情况

- page-feature task 覆盖：已补齐 `S4-WIN-03` 已连接说明态的 `Open watcher status` 入口。
- 页面状态 / 入口 / 退出 / 错误态 / CoreBridge 闭环：已补主窗口 OneDrive status 入口、OneDrive notice 已连接说明态、watcher status 退出回主窗口路径。
- mock / fixture / 硬编码状态：生产路径未新增 mock；新增 watcher view 只做路由占位与“rescan 必须先确认”的展示，不触发 rescan/Core 写入。
- control map 外能力：未新增 Core 能力调用；未实现 `C4-12/C4-19`，只做 S4-WIN-03 要求的导航入口。
- Forbidden Touches：`core/src/**` 无 diff。

四、工程质量

- 代码结构：入口状态放在 `OneDriveNoticeViewModel` / `WindowsMainWindowViewModel`，页面切换集中在 `MainWindow`，没有引入新依赖。
- 文件长度：`OneDriveNoticeViewModel.cs` 已压回 500 行阈值。
- 错误处理：沿用现有 OneDrive 检测、确认、打开文件夹错误路径；watcher route 不执行高风险动作。
- 测试证据：补了 ViewModel 与 smoke tests，覆盖 `Open watcher status`、主窗口 OneDrive 入口、watcher route、以及不直接触发 rescan。

五、验证情况

- 通过：`dotnet run --project apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj`
- 通过：`git diff --check`
- 通过：`./dev check task 4-3/task-123`
  - `doctor: OK`
  - `git diff --check`
  - macOS build gate：`BUILD SUCCEEDED`
- 额外尝试但未通过：`dotnet build apps/windows/AreaMatrix/AreaMatrix.Windows.csproj`，失败在 NuGet restore 环境层：无法加载 `https://api.nuget.org/v3/index.json`，`System.Net.CookieContainer` initializer 抛错；不是源码编译阶段失败。

六、风险与后续

- 剩余风险：当前机器未能完成 Windows WinUI app 直接 build，需在可访问 NuGet/Windows SDK 的环境补跑。
- 下一个任务建议：继续 `4-3/task-124`，正式实现 `S4-WIN-04 + C4-12 platform-watcher-status`，不要在 `task-123` 里提前扩展 watcher 能力。
- 工作区仍有既有本地状态：`tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260608_131031/`，我未把它们纳入本次功能修复。

