一、执行结论

未完成。`./dev check task 4-1/task-85` 通过，但人工交叉验收发现 C2-17 仍不能作为 S2-21 的完整真实 Core 依赖：Trash / recovery 不可用时，Core 预览没有把 Replace 行标为 `Blocked`，也没有让 `can_apply=false`。这会阻塞后续 UI 接入。

二、执行范围

单任务：`4-1/task-85`，只读验收。  
修改文件清单：无。  
未触碰 `apps/**`。工作区已有的 `tasks/prompts/_shared/progress.json` 修改和 `.codex/task-loop-runs/20260519_122204/` 未跟踪目录不是我本轮产生的。

三、完成情况

核对结果：

- capability spec / Core API / UDL / Rust 入口 / 测试：大体已对齐，`preview_import_conflict_batch`、`apply_import_conflict_batch`、请求/响应类型、错误码、UDL 均存在。
- 消费页面状态覆盖：不通过。S2-21 明确要求 `Blocked: Trash unavailable`，且 Trash 不可用时禁用 Replace；当前 [plan.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/import_conflict_batch/plan.rs:77) 只返回 `trash_available`，但 [can_apply](/Users/as/Ai-Project/project/AreaMatrix/core/src/import_conflict_batch/plan.rs:87) 不把 Replace + Trash unavailable 视为 blocked。
- control map 之外能力：未发现 C2-17 实现 iCloud、tag、search、AI、macOS UI 等越界能力。
- Forbidden Touches：未触碰。

四、工程质量

代码路径有真实 DB / FS / rollback / undo 逻辑，不是 mock-only。Replace 路径有可恢复归档与失败回滚证据，测试覆盖了二次确认、Ask per item、staging 保留、undo 预检和 late undo rollback。

残余工程质量风险：`core/src/import_conflict_batch/apply.rs` 当前 542 行，超过 500 行规范，应在修复轮顺手拆分；这不是本轮唯一阻断点，但属于后续 repair 需要收口的质量问题。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-85
```

关键输出：

- `prompt_pipeline.py doctor`: OK，637 tasks / 637 manifests
- `git diff --check`: 通过
- `cargo fmt --all -- --check`: 通过
- `cargo clippy --all-targets --all-features -- -D warnings`: 通过
- `import_conflict_batch_contract_api`: 4 passed
- `import_conflict_batch_failure_recovery`: 6 passed
- `import_conflict_batch_file_safety`: 5 passed
- `import_conflict_batch_implementation`: 2 passed
- `import_conflict_batch_validation`: 3 passed

未运行：`cargo test --workspace` / `./dev check all`。原因是本 task manifest 指定上限为 `./dev check task 4-1/task-85`，且 prompt 明确不要自行升级到更宽门禁。

六、风险与后续

剩余阻塞：补齐 Replace + Trash unavailable 的 Core preview 阻断语义，并加测试证明：

- `trash_available=false` 且 Replace in scope 时，相关行应为 `Blocked` 或等价不可应用状态；
- `can_apply=false` / `apply_blocked_reason` 明确说明 Trash unavailable；
- apply 不能绕过 preview token 与 blocked 状态。

建议下一个任务：针对 `4-1/task-85` 的 repair，只改 `core/src/import_conflict_batch/**` 与 `core/tests/import_conflict_batch_*`，补测试后重跑 `./dev check task 4-1/task-85`。