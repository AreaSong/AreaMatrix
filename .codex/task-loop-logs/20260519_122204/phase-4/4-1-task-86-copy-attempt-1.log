一、执行结论

已完成 `4-1/task-86: C2-18 contract-api`。

二、执行范围

单任务：C2-18 redo-action-log 合同/API/UDL/错误码/合同测试。

修改/相关文件：
- `core/src/redo.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/src/error.rs`
- `core/src/classifier_correction.rs`
- `core/src/import_conflict_batch/apply.rs`
- `core/area_matrix.udl`
- `core/tests/redo_action_log_contract_api.rs`
- `core/tests/error_mapping_contract_api.rs`
- `docs/api/core-api.md`
- `docs/api/error-codes.md`

未触碰 `apps/**`。工作区另有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 的既有运行状态改动，我没有编辑或回滚。

三、完成情况

- 已读取 C2-18 能力规格、Stage 2 control map、Core API、错误码。
- 已补齐 `list_redo_actions` / `redo_action` 的 Rust public API、UDL、Core API 文档和 `RedoActionRecord` / `RedoActionResult` / `RedoActionStatus`。
- 已加入 `ExpiredAction` 到 `CoreError` / `ErrorKind` / UDL / Core API / error-codes，并补齐两个现有 exhaustive match 的类型一致性。
- 已新增 `redo_action_log_contract_api`，锁定 API 签名、输出字段、错误码、文档/UDL/control map 一致性和消费方状态。
- 未实现 redo stack 持久化、文件操作、change log 写入或相邻能力。当前 contract 仍是无假成功入口，后续 `4-1/task-87` 实现业务逻辑。

四、工程质量

代码结构清晰，C2-18 合同集中在 `core/src/redo.rs`，API 入口只做桥接委派。新增 public Rust API 已有 rustdoc 和 `# Errors`。错误处理显式，无 mock-only 成功路径、无硬编码通过态、无生产 `unwrap/expect/panic`。未新增依赖。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-86
```

结果：
- `prompt_pipeline.py doctor`: OK，637 tasks / 637 manifests
- `git diff --check`: OK
- `cargo test --test redo_action_log_contract_api -- --nocapture`: 4 passed

额外范围审计：
- `git diff -- apps`: 无输出，确认未触碰 Forbidden Touches。

六、风险与后续

剩余风险：本任务只完成合同层，实际 redo 行为、事务回滚、redo stack 清空、文件系统 preflight 仍在后续 implementation / failure-edge / validation 任务中完成。

建议下一个任务：继续 `4-1/task-87: C2-18 implementation`，实现最小 Core 行为闭环。

