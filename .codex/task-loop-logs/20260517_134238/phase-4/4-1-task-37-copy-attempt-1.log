一、执行结论

已完成 `4-1/task-37`。

二、执行范围

单任务：C2-08 `batch-change-category` Core implementation。

修改 / 新增文件均在允许范围内：

- `core/src/batch_category.rs`
- `core/src/batch_category/apply.rs`
- `core/src/batch_category/path_plan.rs`
- `core/src/batch_category/plan.rs`
- `core/src/batch_category/token.rs`
- `core/src/db/mod.rs`
- `core/src/db/move_to_category.rs`
- `core/src/db/undo.rs`
- `core/src/db/undo/batch_file_actions.rs`
- `core/src/db/undo/file_actions.rs`
- `core/src/storage/dedup.rs`
- `core/src/storage/mod.rs`
- `core/tests/batch_change_category_contract_api.rs`
- `core/tests/batch_change_category_implementation.rs`

三、完成情况

核对清单结果：

- 只实现 C2-08：通过，未实现相邻 C2 能力。
- 输入 / 输出 / DB / 文件系统 / 错误映射：通过，preview 只读，apply 写 `files`、`change_log`、统一 batch undo；repo-owned 文件移动，Indexed metadata-only。
- Rust Core 不依赖平台 UI API：通过。
- 最小测试支撑：通过，新增 C2-08 implementation tests。

完成标准：

- C2-08 可被测试 / 桥接调用验证：通过。
- 未触碰用户文件安全边界之外行为：通过。Indexed 源文件不移动，`README.md` 不触碰，未触碰 `apps/**`。

四、工程质量

实现拆成 preview plan、token、apply、path plan、DB / undo 支撑，避免单文件继续膨胀。拆分后关键新增 / 修改文件均控制在 500 行以内。

错误与边界处理包括：

- stale preview token 检测。
- 目标路径冲突预览。
- repo-owned move rollback guard。
- DB transaction 失败时回滚已移动文件。
- 部分失败进入 item result，不静默跳过。
- 成功写入才创建统一 undo token。

无占位、硬编码通过态、mock-only 或一次性脚本化实现。

五、验证情况

已通过：

```bash
./dev check task 4-1/task-37
```

该命令实际运行并通过：

- prompt doctor
- git diff check
- `cargo test --test batch_change_category_contract_api -- --nocapture`
- `cargo test --test batch_change_category_implementation -- --nocapture`

额外确认：

```bash
cd core && cargo fmt --all -- --check
```

通过。

未运行 `cargo test --workspace`、全量 clippy、`./dev check core/all`，因为本 task 明确要求普通 atomic Core task 以 `./dev check task 4-1/task-37` 为验证上限。

六、风险与后续

剩余风险：未做 macOS UI 接入，符合本 task “不做页面接入”的边界。下一步建议进入 `4-1/task-38`，按队列继续 C2-08 后续 failure-edge / validation 类任务。

