一、执行结论

已完成 `4-1/task-36: C2-08 contract-api`。

二、执行范围

单任务：C2-08 batch-change-category 合同/API。

修改文件清单：
- `core/src/batch_category.rs`
- `core/tests/batch_change_category_contract_api.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `docs/api/core-api.md`
- `docs/core/capability-specs/stage-2-experience/C2-08-batch-change-category.md`

工作区另有既存 runner 状态改动：`.codex/task-loop-runs/.../summary.json`、`tasks/prompts/_shared/progress.json`，我未回退。

三、完成情况

核对清单已完成：
- 已读取 C2-08 能力规格、Stage 2 control map、Core API、错误码。
- 已补齐 `preview_batch_move_to_category` / `batch_move_to_category` 的 API、UDL、Rust public contract、错误码和输出类型。
- 只补合同、类型、桥接声明和 contract test；没有实现 C2-08 业务移动/DB 写入逻辑。
- 页面消费方状态已记录到 Core API：S2-12 可得到分类分布、preview token、影响计数、逐项状态、Apply 可用性、执行摘要、`updated_files`、`undo_token`；S2-10 只消费 undo token/C2-07 状态。

完成标准已满足：
- C2-08 Core API 合同可供 `4-1/task-37` 直接实现。
- 未引入 control map 之外的页面能力。
- 未触碰 `apps/**`。

四、工程质量

新增 public Rust API 和类型有 rustdoc / `# Errors`。合同 stub 不返回伪成功，当前实现阶段前统一返回结构化错误，避免 mock-only 或硬编码通过态。

新增文件低于 500 行；`core/src/api.rs` 是既有超 500 行文件，本轮只按现有 API 聚合模式补入口，未做范围外拆分。无新增依赖。

五、验证情况

已运行并通过：
- `cargo fmt --all`
- `cargo test --test batch_change_category_contract_api -- --nocapture`
- `./dev check task 4-1/task-36`

正式 task 验证结果：
- `prompt doctor`: OK，tasks 637 / manifests 637。
- `git diff --check`: 通过。
- targeted Rust test: `batch_change_category_contract_api`，4 passed。

未运行宽门禁：未运行 `cargo test --workspace`、clippy、`./dev check core/all`，符合本 atomic task 的验证上限。

六、风险与后续

剩余风险：C2-08 现在只是合同/API，不包含真实 DB、文件移动、undo 写入和回滚实现。

建议下一个任务：继续 `4-1/task-37`，实现 C2-08 最小 Core 行为闭环。