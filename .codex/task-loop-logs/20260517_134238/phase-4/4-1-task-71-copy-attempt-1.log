一、执行结论

已完成 `4-1/task-71: C2-15 contract-api`。

二、执行范围

修改文件清单：
- `core/src/classifier_rule_editor.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `core/tests/classifier_rule_editor_contract_api.rs`
- `docs/api/core-api.md`

未触碰 `apps/**`。另外工作区里已有的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 不是本任务改动。

三、完成情况

核对清单已完成：已读取 C2-15 能力规格、Stage 2 control map、Core API、错误码；补齐了 API/UDL/Rust 合同、DTO、错误边界和 contract test；没有实现相邻能力或页面功能。

完成标准已满足：C2-15 后续 implementation task 可直接基于当前合同实现。当前合同只覆盖 classifier rule editor 的 list/update/delete 状态与输入输出，没有引入 control map 之外的页面能力。

四、工程质量

代码只做合同层：新增 public Rust API 和 DTO 均有 rustdoc，错误路径显式返回 `CoreError`。实现函数只做输入校验后返回“implementation pending”的结构化错误，避免伪造成功、mock-only 闭环或提前写入 classifier.yaml。

高风险说明：本任务涉及 Core API / UDL 合同变更，已按静默授权继续执行；没有删除、移动、覆盖用户文件，回滚可通过撤销上述任务文件改动完成。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-71
```

结果包括：
- `prompt doctor`: OK，tasks/manifests 均为 637
- `git diff --check`: OK
- `cargo test --test classifier_rule_editor_contract_api -- --nocapture`: 4 passed

六、风险与后续

剩余风险：C2-15 业务实现仍未完成，当前 API 不是可用 CRUD 行为，只是稳定合同。建议下一个任务进入 `4-1/task-72`，实现 classifier rule editor 的真实读取、更新、删除及对应错误路径。

