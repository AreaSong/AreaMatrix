一、执行结论

已完成 `4-1/task-82`。

二、执行范围

单任务：只实现 C2-17 `import-conflict-batch` Core 行为闭环。

修改/新增的 task 范围文件：
- `core/src/import_conflict_batch.rs`
- `core/src/import_conflict_batch/{apply,path,plan,token}.rs`
- `core/src/db/import_conflicts.rs`
- `core/src/db/import_conflicts/status.rs`
- `core/src/db/import.rs`
- `core/src/db/mod.rs`
- `core/src/storage/mod.rs`
- `core/tests/import_conflict_batch_contract_api.rs`
- `core/tests/import_conflict_batch_implementation.rs`

工作区另有既存 dirty 文件未回滚：`.codex/task-loop-runs/20260517_134238/summary.json`、`tasks/prompts/_shared/progress.json`。

三、完成情况

核对清单：
- 只实现 C2-17：通过，未实现相邻 C* / UI 能力。
- 输入、输出、DB/文件系统变化、错误映射：通过，preview/apply 已连接 import session、conflict rows、staging files、`files`、`change_log`、undo action。
- Rust Core 不依赖平台 UI API：通过。
- 最小测试支撑：通过，新增 implementation test 覆盖 preview + apply 的 Skip / KeepBoth。

完成标准：
- C2-17 核心行为可测试/桥接调用验证：通过。
- 未触碰用户文件安全边界之外行为：通过；未触碰 `apps/**`。

四、工程质量

代码按入口、计划、token、路径安全、执行拆分，避免把逻辑堆在单文件。新增 public Rust API 已保留 rustdoc；错误路径显式返回 `Conflict`、`FileNotFound`、`PermissionDenied`、`StagingRecoveryRequired`、`Io`、`Db`。实现没有 mock-only、硬编码通过态或一次性脚本逻辑。

五、验证情况

已运行并通过：
- `cargo fmt --all`
- `./dev check task 4-1/task-82`
  - `prompt_pipeline.py doctor`: OK
  - `git diff --check`: OK
  - `cargo test --test import_conflict_batch_contract_api -- --nocapture`: 4 passed
  - `cargo test --test import_conflict_batch_implementation -- --nocapture`: 1 passed

未运行宽门禁；按任务要求没有升级到 `cargo test --workspace` / `./dev check core` / `./dev check all`。

六、风险与后续

剩余风险：Replace 的更多失败/回滚边界仍应由后续 `4-1/task-83` failure-edge 继续加强。建议下一个任务执行 `4-1/task-83`。

