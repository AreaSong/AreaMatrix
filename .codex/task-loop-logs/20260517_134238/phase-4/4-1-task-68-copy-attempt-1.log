一、执行结论

已完成 `4-1/task-68`。

二、执行范围

单任务。修改文件：

- `core/src/classifier_impact.rs`
- `core/tests/classifier_impact_preview_failure_recovery.rs`

未触碰 `apps/**`。工作树中原有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是既有本地状态改动，我没有回退。

三、完成情况

- 空态：新增测试证明空 repo 返回 0 影响、禁用 apply，且不写 DB/文件。
- 非法输入：覆盖空 repo path、非法 category、空 matcher basis、带点 extension，均返回 `Config`。
- 权限 / 错误码映射：补了 `PermissionDenied` 映射测试，确认不是 retryable。
- IO/DB 错误：DB 损坏和非法 metadata path 均返回 `Db`，且不改用户文件、不写 classifier config。
- 半成品：预览仍是只读；测试对比 user-visible paths、DB rows、change_log、undo、classifier.yaml，证明失败路径无半成品。
- 隐私/AI/远程调用：新增源码扫描测试，禁止 C2-14 预览路径出现网络、token/API key、日志或写/rename/delete 调用。
- 吞错修复：`metadata()` 读取只有 `NotFound` 会被当成 Missing；其他 metadata 错误现在返回 `Db`，不再静默降级。

四、工程质量

实现保持在现有 C2-14 模块内，没有新增依赖、mock-only、硬编码通过态或相邻能力。新增测试是独立 integration test，覆盖失败边界和只读安全证据。public API 未变更，因此未改 UDL。

五、验证情况

已通过：

- `cargo fmt --all -- --check`
- `cargo test --test classifier_impact_preview_failure_recovery -- --nocapture`：6 passed
- `cargo test --test classifier_impact_preview_contract_api -- --nocapture`：4 passed
- `cargo test --test classifier_impact_preview_implementation -- --nocapture`：4 passed
- `./dev check task 4-1/task-68`：通过，包含 prompt doctor、diff check、C2-14 三个 targeted Rust test

未运行全量 `cargo test --workspace` / clippy；本 task 明确要求普通 atomic Core task 以 `./dev check task 4-1/task-68` 为验证上限。

六、风险与后续

剩余风险：真实 macOS sandbox 权限行为仍需后续 UI/page integration 侧消费验证；当前 Core 层已经把 C2-14 只读失败边界和错误映射补齐。

建议下一个任务：继续 `4-1/task-69`，补 C2-14 validation 证据。