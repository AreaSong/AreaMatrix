一、执行结论

已完成 `4-1/task-67`。

二、执行范围

单任务：只实现 C2-14 classifier-impact-preview。修改文件：

- `core/src/classifier_impact.rs`
- `core/src/classifier_impact/config.rs`
- `core/src/classifier_impact/db.rs`
- `core/src/classifier_impact/matcher.rs`
- `core/src/classifier_impact/path.rs`
- `core/tests/classifier_impact_preview_implementation.rs`

未触碰 `apps/**`。工作树里原本已有的 `.codex/task-loop-runs/20260517_134238/summary.json` 和 `tasks/prompts/_shared/progress.json` 保持未处理。

三、完成情况

核对清单全部完成：只实现 C2-14；按能力规格落实输入、输出、只读 DB/文件系统检查和 `Config` / `Db` 错误映射；Rust Core 不依赖平台 UI API；补了最小 implementation 测试，不做页面接入。

完成标准满足：`preview_classifier_rule_impact` 现在可被 Rust 测试和 UDL 暴露合同验证；实现只读读取 classifier 配置、active file metadata 和文件存在/冲突状态，不保存规则、不重分类、不移动/删除/重命名用户文件。

四、工程质量

实现拆成 `config/db/matcher/path` 子模块，避免单文件继续膨胀；公开 API rustdoc 保留并更新。错误路径显式返回 `Config` 或 `Db`，没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本实现。

五、验证情况

已运行并通过：

```bash
cargo fmt --all
./dev check task 4-1/task-67
```

`./dev check task 4-1/task-67` 结果：`doctor: OK`，`git diff --check` 通过，`classifier_impact_preview_contract_api` 4 个测试通过，`classifier_impact_preview_implementation` 4 个测试通过。

六、风险与后续

剩余风险：本 task 未实现页面接入、保存规则、批量应用、Undo 或 change_log，这些都在 C2-14 范围外。建议下一个任务按队列进入 `4-1/task-68`，处理 C2-14 failure-edge。