一、执行结论：已完成。

二、执行范围：单任务 `4-1/task-70`。本轮修复集中在 C2-14 Core 合同、Rust 实现、UDL/Core API 文档和 targeted 测试；`apps/**` 未触碰。

三、完成情况：
- 已修复 RuleDraft 预览语义：新分类现在按“临时叠加草稿后的 classifier matcher”计算，复用 priority / keyword length / category order 语义，不再直接把目标分类当成结果。
- 已补齐 S2-18 Move preference：`ClassifierImpactPreviewRequest` 新增 `move_files`，Move 关闭时不做路径冲突阻断，Move 开启时才执行目标路径冲突 dry-run。
- 已同步 `core/area_matrix.udl`、`docs/api/core-api.md`、`docs/core/capability-specs/stage-2-experience/C2-14-classifier-impact-preview.md`。
- 已补测试覆盖高优先级既有规则影响 RuleDraft 结果，以及 Move on/off 冲突差异。
- Forbidden Touches：`git status --short apps` 无输出，未触碰 `apps/**`。

主要修改文件：
`core/src/classifier_impact.rs`、`core/src/classifier_impact/matcher.rs`、`core/src/classifier_impact/types.rs`、`core/area_matrix.udl`、`docs/api/core-api.md`、C2-14 capability spec，以及 4 个 `classifier_impact_preview_*` 测试文件。

四、工程质量：
代码结构已把类型、matcher、主 preview 流程分开；`core/src/classifier_impact.rs` 当前 475 行，低于 500 行软阈值。没有新增 mock-only、硬编码通过态或实际写入/移动文件行为；C2-14 仍保持只读 preview。public 类型 rustdoc 已覆盖，错误仍按 `Config` / `Db` 路径处理。

五、验证情况：
已运行并通过 `./dev check task 4-1/task-70`：
- `prompt doctor`：OK
- `git diff --check`：通过
- `cargo fmt --all -- --check`：通过
- `cargo clippy --all-targets --all-features -- -D warnings`：通过
- `classifier_impact_preview_contract_api`：4 passed
- `classifier_impact_preview_failure_recovery`：6 passed
- `classifier_impact_preview_implementation`：8 passed
- `classifier_impact_preview_validation`：4 passed

六、风险与后续：
C2-14 现在可以作为 S2-18 后续 page-feature task 的真实 Core 依赖。后续建议进入依赖它的 UI 接入任务 `4-1/task-129`，由页面任务验证 Swift/CoreBridge 消费状态，不在本 task 范围内提前实现。  
当前工作树还存在 task-loop 运行证据/进度文件和前序 retry 留下的同 task 改动，我没有回退这些文件。

