一、执行结论

已完成。

二、执行范围

单任务：`4-1/task-57`，只实现 C2-12 Core 行为。  
修改文件清单：

- `core/src/classifier_correction.rs`
- `core/src/db/mod.rs`
- `core/src/db/move_to_category.rs`
- `core/tests/classifier_correction_implementation.rs`

`core/area_matrix.udl` 已与 API 合同一致，未改。`apps/**` 未触碰。工作树里原已有 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 变更，不属于本次实现范围。

三、完成情况

核对清单全部满足：

- 只实现 C2-12，没有实现 C2-13/C2-14/C2-15。
- `correct_file_category` 已落实输入校验、输出 `ClassifierCorrectionResult`、DB 分类更新、`change_log` 写入、可选安全移动、错误映射。
- Rust Core 未依赖平台 UI API。
- 新增 targeted implementation tests。

完成标准满足：

- C2-12 可通过 Rust test/桥接入口验证。
- repo-owned 仅在 `move_file=true` 且 imported copied/moved 时移动；indexed/adopted 只更新 metadata，不移动用户原文件。
- `remember=true` 只返回 `ClassifierRuleDraft`，不写 `.areamatrix/classifier.yaml`。

四、工程质量

实现复用现有 `move_to_category` 安全移动与 rollback 路径，metadata-only 走 DB transaction 更新分类、写 `change_log` 并生成 undo action。没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本逻辑。公共 Rust API 已有 rustdoc，新增 helper 为内部函数。

五、验证情况

已运行：

- `cargo fmt --all`：用于格式化。
- `./dev check task 4-1/task-57`：通过。
  - `prompt_pipeline.py doctor`：OK，637 tasks / 637 manifests。
  - `git diff --check`：通过。
  - `cargo test --test classifier_correction_contract_api -- --nocapture`：4 passed。
  - `cargo test --test classifier_correction_implementation -- --nocapture`：5 passed。

未运行：没有运行 `cargo test --workspace`、clippy、`./dev check core/all`，因为本 atomic Core task 的 manifest 验证上限是 `./dev check task 4-1/task-57`。

六、风险与后续

剩余风险：未做 macOS 页面接入，本任务也不允许做页面接入；CI/人工 review 仍是合并前门禁。  
建议下一个任务：继续 `4-1/task-58`。

