一、执行结论

- 已完成

二、执行范围

- 单任务：`4-1/task-82`
- 最终修改文件：
  - `core/src/import_conflict_batch/apply.rs`
  - `core/src/import_conflict_batch/apply/replace.rs`
- `docs/architecture/data-model.md` 的越界 dirty diff 已清除，当前无 docs diff。
- `apps/**` 未触碰。

三、完成情况

- 只实现 / 修复 C2-17：通过，本轮只处理上次验收指出的 C2-17 工程质量与范围证据问题。
- 输入、输出、DB/文件系统变化和错误映射：保持既有实现语义，未扩展相邻能力。
- Rust Core 不依赖平台 UI API：通过。
- 最小测试支撑、不做页面接入：通过。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

- `apply.rs` 从 509 行拆到 382 行，低于 500 行阈值。
- Replace 专属逻辑拆到 `core/src/import_conflict_batch/apply/replace.rs`，187 行。
- 原超限的 `apply_replace` 被拆成短函数，入口约 12 行。
- 新增/调整文件未发现 `unwrap()`、`expect()`、`panic!`、`TODO`、mock-only 或硬编码通过态。
- Replace rollback / recoverable archive 语义保持不变：失败时恢复 staged 文件和 archived existing 文件。

五、验证情况

- 已运行：`./dev check task 4-1/task-82`
- 通过：
  - prompt doctor
  - `git diff --check`
  - `cargo test --test import_conflict_batch_contract_api -- --nocapture`：4 passed
  - `cargo test --test import_conflict_batch_file_safety -- --nocapture`：3 passed
  - `cargo test --test import_conflict_batch_implementation -- --nocapture`：2 passed
- 未运行宽门禁：按 manifest 原文，本 task 验证上限是 `./dev check task 4-1/task-82`，未升级到 `cargo test --workspace` / `./dev check core` / `./dev check all`。

六、风险与后续

- 剩余风险：仓库仍有前几次 task-loop 产生的 C2-17 相关未提交改动和日志，我未回滚用户/runner 既有改动。
- 建议下一个任务：进入 `4-1/task-82` verify-ready 验收；通过后再继续后续 task。  
