一、执行结论

已完成 `4-1/task-66`。

二、执行范围

修改文件清单：
- `core/src/classifier_impact.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `core/tests/classifier_impact_preview_contract_api.rs`
- `docs/api/core-api.md`

说明：`docs/api/core-api.md` 是本 task 的 Exact Doc，且 Core API 文档声明对外接口变化必须先与 UDL 对齐；本次只同步 C2-14 直接合同缺口，未改 manifest，未触碰 `apps/**`。

三、完成情况

- 已读取 C2-14 能力规格、Stage 2 control map、Core API、错误码，并交叉读取 S2-18 页面消费需求。
- 已新增 `preview_classifier_rule_impact(repo_path, rule) -> RuleImpactReport` 合同、UDL 声明、Rust public API 和 DTO。
- 合同只覆盖只读 impact preview：影响数量、样例、冲突、needs review、warning、can apply/blocked reason。
- 未实现保存规则、批量应用、Undo、C2-15 rule CRUD 或 Swift 页面。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

结构清晰，C2-14 单独模块承载合同类型与输入校验；public Rust API 已补 rustdoc 和 `# Errors`。错误边界限定为 `Config` / `Db`。当前实现不是业务闭环实现，只是 contract/API task 所需的合同与校验入口；真实 matcher/DB 读取应由 `4-1/task-67` 实现。

五、验证情况

已运行：
- `cargo fmt --all`
- `./dev check task 4-1/task-66`

结果：
- `prompt_pipeline.py doctor` 通过
- `git diff --check` 通过
- targeted test `cargo test --test classifier_impact_preview_contract_api -- --nocapture` 通过，4 passed

六、风险与后续

剩余风险：当前 C2-14 只完成合同/API，`preview_classifier_rule_impact` 对有效 repo 仍返回 `Db`，等待 implementation task 接入真实 classifier matcher 与文件 metadata。

建议下一个任务：继续执行 `4-1/task-67`，实现 C2-14 最小 Core 行为闭环。

备注：工作区里 `.codex/task-loop-runs/20260517_134238/summary.json` 和 `tasks/prompts/_shared/progress.json` 在本次开始前已是 modified，我未将其作为本任务改动处理。

