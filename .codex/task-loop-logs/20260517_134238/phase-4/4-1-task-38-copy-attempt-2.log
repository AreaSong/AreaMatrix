一、执行结论

已完成。4-1/task-38 的 retry 缺口已修复，C2-08 现在有通过真实 API 入口触发 `PermissionDenied` 和 `Io` 的测试证据。

二、执行范围

单任务：`4-1/task-38`

修改文件：
- [batch_change_category_failure_recovery.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/batch_change_category_failure_recovery.rs:81)
- [batch_category_failure.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/support/batch_category_failure.rs:259)

三、完成情况

- 核对清单 1：通过。新增 API 级 `PermissionDenied` 测试、API 级 `Io` 测试，并补了 sidecar IO blocked row 证据。
- 核对清单 2：通过。新增测试都在失败后比对 `snapshot`，证明 DB/FS 无半成品变化。
- 核对清单 3：通过。未改 AI/远程调用路径，沿用 rollback_safety 中默认关闭与不创建 `.areamatrix/ai|remote|secrets` 的证据。
- 核对清单 4：通过。失败通过 `CoreError` 或 preview blocked row 显式返回，不吞错。
- Forbidden Touches：未触碰，`git status --short -- apps` 无输出。

四、工程质量

测试 helper 新增 `assert_io_error`、`assert_permission_denied` 和 Unix 权限 guard，逻辑集中在测试支持文件，没有生产 mock、硬编码成功态或一次性脚本。新增权限测试用 `#[cfg(unix)]` 限定，IO 测试通过不可检查路径和 sidecar 读取失败覆盖 API 行为；无新增依赖，无需 UDL/API 文档同步。

五、验证情况

已运行并通过：

```bash
cargo test --test batch_change_category_failure_recovery -- --nocapture
cargo fmt --all
./dev check task 4-1/task-38
```

`./dev check task 4-1/task-38` 通过内容包括：prompt doctor、git diff check、cargo fmt check、cargo clippy，以及 C2-08 targeted tests：contract 4/4、failure_recovery 6/6、implementation 3/3、rollback_safety 4/4。

六、风险与后续

剩余风险：本轮只修复上次验收指出的权限/IO API 级证据缺口；工作区中仍有上一次 attempt 已存在的 C2-08 改动和 task-loop 运行记录，我没有回退。建议下一个动作是进入 `4-1/task-38` 的 verify-ready 验收。  

