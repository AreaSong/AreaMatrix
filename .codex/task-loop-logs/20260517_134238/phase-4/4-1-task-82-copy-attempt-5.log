一、执行结论

已完成。已修复 `4-1/task-82` 上一次验收失败的 undo 写入失败安全问题。

二、执行范围

单任务：`4-1/task-82`，仅修 C2-17。

修改文件清单：
- `core/src/db/mod.rs`
- `core/src/db/import_conflicts.rs`
- `core/src/import_conflict_batch/apply.rs`
- `core/src/import_conflict_batch/apply/replace.rs`
- `core/tests/import_conflict_batch_file_safety.rs`
- `core/tests/support/import_conflict_batch.rs`

三、完成情况

核对清单结果：
- 只实现 C2-17：通过，未实现相邻 C* 能力。
- 输入、输出、DB/FS 变化、错误映射：通过，undo 写入失败现在会在 mutation 前预检；若最终 undo 写失败，会回滚文件系统与 DB 状态。
- Rust Core 不依赖平台 UI API：通过。
- 最小测试支撑：通过，新增 undo 写入失败前置/后置安全测试，未做页面接入。

完成标准：
- C2-17 核心行为可被测试验证：通过。
- 未触碰用户文件安全边界之外行为：通过。
- `Forbidden Touches apps/**`：未触碰。

四、工程质量

代码结构保持在 C2-17 apply / DB helper / file-safety test 内。关键修复是把 undo 写入失败纳入安全闭环：preflight 阻止明显不可写状态，late failure 通过 rollback 对象恢复 staging 文件、旧文件、conflict status、session status 和相关 DB 行。

未引入新依赖；无 mock-only、硬编码通过态或一次性脚本实现。新增测试覆盖 undo 写失败后 staged 文件保留、旧文件恢复、DB 行回到安全状态。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-82
```

该命令内通过：
- `prompt doctor`
- `git diff --check`
- `cargo test --test import_conflict_batch_contract_api -- --nocapture`：4 passed
- `cargo test --test import_conflict_batch_file_safety -- --nocapture`：5 passed
- `cargo test --test import_conflict_batch_implementation -- --nocapture`：2 passed

未运行 `./dev check all`、`./dev check core`、`cargo test --workspace`，因为当前 atomic task manifest 的验证上限是 `./dev check task 4-1/task-82`。

六、风险与后续

剩余风险：当前修复覆盖本次验收指出的 undo 写失败安全语义；未做 UI 消费验证，因为本 task 不允许页面接入。

建议下一个任务：进入该任务 verify-ready 验收，确认 `4-1/task-82` 从 copy repair 转为 PASS。

