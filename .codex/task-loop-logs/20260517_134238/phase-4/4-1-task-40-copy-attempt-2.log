一、执行结论：已完成。

二、执行范围：单任务 `4-1/task-40`。  
修改文件：
- `core/src/batch_category/plan.rs`
- `core/tests/batch_change_category_validation.rs`

三、完成情况：
- 修复 C2-08 preview 语义：`MetadataOnly` 仍表示只更新 metadata，但 `index_only` 现在只在 `StorageMode::Indexed` 时为 `true`。repo-owned 文件在 `move_repo_owned_files=false` 时会是 `MetadataOnly + index_only=false + will_move_file=false`。
- 补充回归测试，覆盖 repo-owned metadata-only 与真正 Indexed metadata-only 的 UI 可消费状态差异。
- 未修改 API / UDL / docs。
- `Forbidden Touches`: `git status --short -- apps` 无输出，未触碰 `apps/**`。

四、工程质量：
- 改动为最小语义修复，没有新增抽象、依赖、mock-only 或硬编码通过态。
- 错误处理路径未改变；本次只修正 preview 状态字段。
- 新测试直接锁定上次验收失败点，防止 S2-12 VoiceOver / 影响摘要再次把 repo-owned metadata-only 误报成 Index-only。

五、验证情况：
- 已运行 `./dev check task 4-1/task-40`
- 通过：prompt doctor、git diff check、`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`
- 通过 targeted tests：
  - `batch_change_category_contract_api`: 4 passed
  - `batch_change_category_failure_recovery`: 6 passed
  - `batch_change_category_implementation`: 3 passed
  - `batch_change_category_rollback_safety`: 4 passed
  - `batch_change_category_validation`: 5 passed
- 额外复查：`git diff --check` 通过。

六、风险与后续：
- 剩余风险：工作区仍有既有 task-loop 运行痕迹和 progress/log 脏改动，不属于本次 Core 修复文件。
- 建议下一个任务：进入 `4-1/task-40` 的 verify-ready 验收，确认本次 retry 已解除 P0 阻塞。