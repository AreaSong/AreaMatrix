一、执行结论

已完成 `4-1/task-38: C2-08 failure-edge`。

二、执行范围

单任务执行。修改/新增文件均在 `Expected New Paths` 内：

- `core/src/batch_category.rs`
- `core/src/batch_category/apply.rs`
- `core/src/batch_category/plan.rs`
- `core/src/batch_category/fs_move.rs`
- `core/tests/batch_change_category_failure_recovery.rs`
- `core/tests/batch_change_category_rollback_safety.rs`
- `core/tests/support/batch_category_failure.rs`

未触碰 `apps/**`。`core/area_matrix.udl` 已核对，无需修改。

三、完成情况

核对清单结果：

- 空态、非法输入、权限/IO/DB、错误码映射：已覆盖空选择、非法 id、非法/未知分类、空 preview token、stale preview、DB change_log/undo 失败。
- 写入/移动失败不留半成品：已加 per-item SQLite savepoint；item 失败会回滚该 item 的 DB 写入，repo-owned 文件和 note sidecar 会回滚到原位置；undo action 写入失败会回滚整批 DB 与文件移动。
- 隐私/AI/远程调用：测试证明 C2-08 不创建 `.areamatrix/ai`、`.areamatrix/remote`、`.areamatrix/secrets`，且 `ai_enabled` 默认保持 false。
- 不吞错/不静默降级：失败项返回 `BatchCategoryResultStatus::Failed` 和错误摘要；阻断类 stale preview 返回 `Conflict`。

四、工程质量

实现上把文件移动回滚 guard 抽到 `core/src/batch_category/fs_move.rs`，`apply.rs` 聚焦批量执行和事务边界。新增测试拆成 failure-recovery、rollback-safety 和共享 fixture，单文件长度均低于 500 行。

没有新增依赖，没有 mock-only 或硬编码通过态。新增失败路径连接真实 SQLite、真实文件移动、真实 sidecar 文件和真实 undo action 写入。

五、验证情况

已通过：

```bash
./dev check task 4-1/task-38
```

该命令实际通过了：

- prompt doctor
- `git diff --check`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --test batch_change_category_contract_api -- --nocapture`：4 passed
- `cargo test --test batch_change_category_failure_recovery -- --nocapture`：3 passed
- `cargo test --test batch_change_category_implementation -- --nocapture`：3 passed
- `cargo test --test batch_change_category_rollback_safety -- --nocapture`：4 passed

六、风险与后续

剩余风险：本任务只覆盖 C2-08 failure-edge，不实现后续 validation/integration/UI wiring。工作树里仍有进入本任务前已存在的 task-loop 状态文件改动：`.codex/task-loop-runs/20260517_134238/summary.json`、`tasks/prompts/_shared/progress.json`，我未回滚它们。

建议下一个任务按队列进入 `4-1/task-39`。  
