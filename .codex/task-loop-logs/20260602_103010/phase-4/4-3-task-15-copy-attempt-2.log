一、执行结论

已完成。`4-3/task-15` 上次失败的 P1 阻塞项已修复：C4-03 现在通过 `FileEntry.availability_status` 结构化暴露 `Missing` 状态，后续 S4-IOS-02 可直接消费 Core payload 显示 `Missing` 徽标和保留缺失文件行。

二、执行范围

- 单任务：`4-3/task-15`
- 主要修改：
  - `core/src/domain.rs`、`core/area_matrix.udl`、`docs/api/core-api.md`
  - `core/src/db/mod.rs`、`core/src/api.rs`
  - 若干 `FileEntry` 构造点的编译适配：`core/src/db/import.rs`、`core/src/db/rename.rs`、`core/src/db/move_to_category.rs`、`core/src/search/repo.rs`、`core/src/classifier_impact/db.rs`
  - C4-03 证据测试：`core/tests/mobile_library_query_contract_api.rs`、`core/tests/mobile_library_query_implementation.rs`、`core/tests/mobile_library_query_validation.rs`
  - 其他 contract fixture 仅补新字段默认值
- 未触碰 `apps/**`。

三、完成情况

- 交叉检查 capability spec、Core API、UDL、Rust 实现和测试：通过，API/UDL/Rust/test 已同步 `FileAvailabilityStatus { Available, Missing }`。
- 消费页面需要的状态、错误和副作用：通过，`list_files` / `get_file` 返回 `availability_status = Missing`，且仍为只读，不触发 rescan、recovery 或 metadata 写入。
- control map 之外能力：未实现；恢复动作仍归 C4-18 / C4-07 后续入口。
- Forbidden Touches：未触碰。

四、工程质量

实现保持在现有 `FileEntry` 查询链路内，没有新增依赖、DB migration、mock-only 路径或硬编码通过态。缺失判断只做返回页内的只读 backing file 存在性判断；无法明确判定缺失的探测错误不扩大当前 C4-03 错误合同。

五、验证情况

已通过：

```bash
cd core && cargo check
./dev check task 4-3/task-15
```

`./dev check task 4-3/task-15` 通过项包括：prompt doctor、git diff check、`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`，以及：

- `mobile_library_query_contract_api`: 3 passed
- `mobile_library_query_failure_recovery`: 8 passed
- `mobile_library_query_implementation`: 4 passed
- `mobile_library_query_validation`: 3 passed

说明：我先在仓库根目录误跑过一次 `cargo fmt --all`，因根目录没有 `Cargo.toml` 失败；随后在 `core/` 下正确运行并通过，最终 manifest validation 已全绿。

六、风险与后续

剩余风险：`FileEntry` 对外合同新增字段，后续生成端绑定需要按 UDL 更新消费侧模型；本 task 禁止触碰 `apps/**`，所以未改 UI。建议下一个任务继续执行 S4-IOS-02 page-feature 接入，让页面直接消费 `availability_status`。

