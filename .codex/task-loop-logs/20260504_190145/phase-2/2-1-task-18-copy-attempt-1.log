一、执行结论

已完成。当前 `2-1/task-18` 的 first-launch 集成闭环已能用现有代码和测试证据证明通过；本轮未再修改文件，主要完成复核与验证。

二、执行范围

- 单任务：`2-1/task-18`
- 修改文件清单：当前工作区已有 macOS app/test/project 改动，范围均在允许路径内。
- 未触碰 `Forbidden Touches`：`git diff --name-only -- core/src` 无输出。
- `core/area_matrix.udl` 当前无差异。

三、完成情况

核对清单结果：
- 已完成：只做 S1-01 到 S1-07 first-launch 已有页面与能力之间的 wiring / 验收补齐。
- 已完成：`progress.json` 显示 `2-1/task-01` 到 `2-1/task-17` 均为 `completed`，`2-1/task-18` 仍为 `in_progress` 等待 runner 收口。
- 已完成：未发现 first-launch 生产路径 mock-only 闭环；fixture/helper 仅在 XCTest 内。
- 已完成：未新增 control map 外页面或 Core 能力。

完成标准：
- 用户路径已覆盖：welcome → choose path → validate → confirm init → initializing → failed/done → open repo。
- 失败项已有证据：初始化失败页有 diagnostics / retry / change path / quit；错误走 C1-21 mapping。

四、工程质量

结构已拆分到 `InitFailedStepView`、`OnboardingIntroStepViews`、`CoreBridgeSnapshots`、`DiagnosticsSnapshotState` 等文件，当前检查的非生成 Swift 文件均未超过 500 行。错误路径显式处理：已初始化 repo 只有成功打开后才保存 repo path；打开失败进入 `mainRepoError`；诊断失败留在 failed page 并显示映射错误。未新增依赖，未改 Rust public API。

五、验证情况

已运行：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：失败，退出码 65，原因是本地 sandbox 阻断 `com.apple.testmanagerd.control` 通信。
- `bash scripts/check-macos-tests.sh`：通过；检测到同一 sandbox 限制后，用同一 XCTest bundle fallback 执行，`65 tests, 0 failures`。
- `git diff --check -- apps/macos/AreaMatrix.xcodeproj/project.pbxproj apps/macos/AreaMatrix apps/macos/AreaMatrixTests`：通过。
- `git diff --name-only -- core/src`：无输出。
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`：通过，`637` tasks / `637` manifests。
- `python3 tasks/prompts/_shared/prompt_pipeline.py status`：通过，first task 仍是 `2-1/task-18`。

六、风险与后续

剩余风险：原始 manifest `xcodebuild test` 在当前执行环境不能直接 PASS，但失败原因是本机 sandbox/testmanagerd 通信限制；fallback 已完成同一 XCTest bundle。建议后续由 task-loop 执行 verify-ready 并将 `2-1/task-18` 标记 completed，然后进入 `2-1/task-19`。

