一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-02`。修改文件清单：
- [core/src/classify/mod.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/classify/mod.rs:26)
- [core/tests/classify_preview_contract.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/classify_preview_contract.rs:207)

工作区中已有的 `.codex/task-loop-runs/20260501_132850/summary.json` 和 `tasks/prompts/_shared/progress.json` 是本次前已存在的未提交修改，我未触碰。

三、完成情况：
- C1-05 implementation 范围：已收敛到 `predict_category` 分类预览。
- Exact Docs：已按 C1-05、control map、Core API、classify module、classifier yaml 交叉对齐。
- 未实现相邻 C1 能力或 UI 页面。
- 最小真实路径：分类规则读取、schema 校验、关键词/扩展名/default、suggested name 均走真实 Rust path。
- Forbidden Touches：未触碰 `apps/**`，`git diff --name-only -- apps` 为空。

四、工程质量：
- 收紧 `classifier.yaml` schema：未知字段拒绝、扩展名禁止带点、keyword/extension 去重、priority/template/display/description 边界校验。
- 修正 `naming_template`：`{ext}` 不带点，`{date}` 为 `YYYY-MM-DD`，支持 `{date_iso}`，未知占位符保留。
- 新增失败路径和模板行为测试；无新增依赖；无 mock-only、硬编码通过态或一次性脚本化实现。
- `core/src/classify/mod.rs` 415 行，`core/tests/classify_preview_contract.rs` 305 行，仍低于文件长度阈值。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace classify_preview`：通过，C1-05 相关 12 个测试全绿。
- 额外补跑 `cd core && cargo test --workspace`：通过，94 个测试全绿。
- `git diff --check`：通过。
- 必需验证项无未运行。

六、风险与后续：
- 剩余风险：本任务只覆盖 Core C1-05，未验证后续 macOS UI 消费链路；这属于后续 page-feature / integration verify 范围。
- 建议下一个任务：`1-2/task-03`，即 C1-05 validation。

