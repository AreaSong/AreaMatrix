一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-15`，只处理 C1-08 contract-api。
修改文件：
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:213)：补明确 C1-08 Indexed 合同、错误码、文件副作用边界。
- [core/tests/import_index_file_contract.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_contract.rs:22)：新增 6 个合同测试，覆盖输入、输出、UDL/API/control map、错误码和相邻能力隔离。

三、完成情况：
- C1-08 contract-api 范围：完成。
- Exact Docs 已按 SSOT 读取并交叉检查：完成。
- 未实现相邻 C1 能力或 UI 页面：是。
- Core API / UDL 合同意图：`api.rs` 已校准；`core/area_matrix.udl` 已存在 Indexed 合同，无需改动，并由测试锁定。
- Forbidden Touches：未触碰 `apps/**`。
- 工作区中 `.codex/task-loop-runs/.../summary.json` 与 `tasks/prompts/_shared/progress.json` 是执行前已有脏状态，本次未修改。

四、工程质量：
- 代码结构保持合同层最小改动，没有新增依赖、mock-only、硬编码通过态或一次性脚本。
- 新增 public API rustdoc 边界覆盖 C1-08 的输入、输出、错误码、副作用：不复制、不移动、不删除源文件，不创建最终资料库副本。
- 本 task 是 contract-api，未提前实现 `1-2/task-16` 的 Indexed 真实导入逻辑。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace import_index_file`：通过，6 个新增 C1-08 合同测试通过。
- 额外补跑 `cd core && cargo test --workspace`：通过。
- 额外补跑 `git diff --check`：通过。

六、风险与后续：
- 剩余风险：Indexed 模式真实 FS/DB 行为仍未实现，这是 `1-2/task-16` 范围；当前 contract 已为后续实现锁定边界。
- 建议下一个任务：继续 `1-2/task-16`，实现 Indexed 导入真实路径，并补文件系统/DB/change_log 一致性测试。

