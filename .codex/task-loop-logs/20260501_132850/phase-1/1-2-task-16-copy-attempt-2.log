一、执行结论

已完成。`1-2/task-16` 上次阻塞的 indexed 源文件删除后 `FileNotFound` 恢复态已补齐，验证通过。

二、执行范围

单任务：`1-2/task-16`

本次 repair 修改：
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:286)：`list_files` / `get_file` 对 `Imported + Indexed` 记录做只读源文件存在性检查，缺失返回 `CoreError::FileNotFound`。
- [core/tests/import_index_file_implementation.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_index_file_implementation.rs:233)：补详情与列表两个入口的缺失源文件回归测试。

未触碰 `apps/**`。

三、完成情况

核对清单全部满足：只处理 C1-08 implementation；按 Exact Docs 的 C1-08 / Core API / storage / transactional import 语义实现；未扩展 UI 或相邻 C1 能力；实现是真实 API 查询路径，不是 mock。

完成标准满足：C1-08 indexed import 的成功路径、DB/change_log、无最终副本、缺失源文件 `FileNotFound` 都有测试证据。

四、工程质量

代码结构保持在 API 查询层，DB 仍只负责元数据读取；只对 `FileOrigin::Imported + StorageMode::Indexed` 做外部源探测，避免误伤 adopt-existing 的 indexed 元数据。新增 public API rustdoc 已说明 `FileNotFound`、`PermissionDenied`、`Db` 等错误路径。无新增依赖、无硬编码通过态、无 mock-only 路径。

五、验证情况

已运行并通过：
- `cd core && cargo fmt --all -- --check`
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`
- `cd core && cargo test --workspace import_index_file`：6 个 contract + 6 个 implementation 通过
- `cd core && cargo test --workspace`：完整 workspace 通过
- `git diff --check`
- `git ls-files apps --modified --others --exclude-standard`：无输出
- `python3 tasks/prompts/_shared/prompt_pipeline.py verify --task 1-2/task-16`：verify prompt 可生成

六、风险与后续

剩余风险：未做 macOS UI 消费验证，本任务无绑定 UI，属于后续 page-feature / integration task 范围。工作区进入本次修复前已有 task-loop 产生的其他脏文件，我没有回滚或改写它们。

建议下一个任务：进入 `1-2/task-17`，继续处理 C1-08 failure-recovery 边界。  
VERIFY_RESULT: PASS

