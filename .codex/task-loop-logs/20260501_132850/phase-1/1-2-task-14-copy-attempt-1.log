一、执行结论：已完成。

二、执行范围：单任务 `1-2/task-14`。只新增 [core/tests/import_move_file_integration_verify.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/import_move_file_integration_verify.rs:1)，未修改产品实现、`core/src/**` 或 `core/area_matrix.udl`，未触碰 `apps/**`。工作区原有的 `.codex/task-loop-runs/20260501_132850/summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是既有脏文件，我没有改它们。

三、完成情况：C1-07 integration-verify 已补齐证据。新增测试覆盖：
- Exact Docs / Core API / UDL / control map / S1-17、S1-20、S1-21、S1-26 消费上下文一致性。
- 真实 `StorageMode::Moved` 导入：源文件消失、最终文件存在、`files.storage_mode=moved`、`source_path`、`change_log imported`、staging 清空。
- DB promotion 失败回滚：源文件恢复、final 半成品和 staging/DB staging row 清理。
- `Indexed` 相邻能力仍保持 out of scope，不被 C1-07 测试伪装完成。

四、工程质量：新增代码是集成测试，不引入新依赖、不改 public API、不加 mock-only 或硬编码通过态。错误/边界证据覆盖了 DB 失败、staging 清理、final 半成品清理和相邻能力隔离；测试文件 293 行，低于仓库文件长度阈值。

五、验证情况：
- `cd core && cargo fmt --all -- --check`：通过。
- `cd core && cargo clippy --all-targets --all-features -- -D warnings`：通过。
- `cd core && cargo test --workspace import_move_file`：通过，C1-07 相关 21 个过滤测试通过，其中新增 integration-verify 4 个通过。
- 额外按 validation driver 扩展：`cd core && cargo test --workspace`：通过，完整 core 测试通过。
- `git diff --check`：通过。

六、风险与后续：当前剩余风险是 macOS UI 端尚未在本 task 范围内接入真实 Core；这属于后续 `2-*` 页面任务，不阻断 C1-07 Core integration-verify。建议下一个任务继续执行 `1-2/task-15`，进入 C1-08 contract/API 边界。

