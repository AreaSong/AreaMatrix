一、执行结论

已完成 `0-2/task-08: check-all 与 CI`。

二、执行范围

修改文件清单：

- [scripts/check-all.sh](/Users/as/Ai-Project/project/AreaMatrix/scripts/check-all.sh:1)
- [.github/workflows/core-ci.yml](/Users/as/Ai-Project/project/AreaMatrix/.github/workflows/core-ci.yml:14)
- [.github/workflows/macos-ci.yml](/Users/as/Ai-Project/project/AreaMatrix/.github/workflows/macos-ci.yml:10)

工作区另有既有未提交改动：`.codex/task-loop-runs/20260501_132850/summary.json`、`tasks/prompts/_shared/progress.json`，本次未处理。

三、完成情况

- 创建 / 校准 `check-all`：已完成。脚本现在聚合 governance、skills、task-loop、doctor、diff whitespace、Rust fmt/clippy/test、macOS build/test、SwiftFormat、SwiftLint。
- 校准 `core-ci` 与 `macos-ci`：已完成。两个 workflow 都新增 `prompt-doctor` job。
- CI 不绕过 doctor：已完成。`core-ci` 的 fmt/clippy/test/build/coverage 和 `macos-ci` 的 build/swiftlint/swiftformat 都通过 `needs: prompt-doctor` 前置约束。
- Forbidden Touches：未触碰 `core/src/**` 和 `apps/macos/AreaMatrix/**`。

四、工程质量

脚本按函数拆分，错误路径显式 `fail`，缺失命令会明确失败；没有 mock-only、硬编码通过态或一次性绕过逻辑。为避免 Apple Python 写受限 pyc cache，`check-all.sh` 默认导出可写的 `PYTHONPYCACHEPREFIX`。

五、验证情况

已通过：

- `bash -n scripts/check-all.sh`
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`
- `bash scripts/check-governance.sh`
- `bash scripts/check-skills.sh`
- `PYTHONPYCACHEPREFIX=/private/tmp/areamatrix-pycache bash scripts/check-task-loop.sh`
- Ruby YAML parse：`core-ci.yml`、`macos-ci.yml` 均 OK
- `git diff --check`

曾失败但已处理：

- `bash scripts/check-task-loop.sh` 直接运行时被本机 Xcode Python cache 权限阻断；`check-all.sh` 已通过默认 `PYTHONPYCACHEPREFIX` 规避该类本机缓存问题，带该环境变量复跑通过。

未运行：

- 未运行完整 `bash scripts/check-all.sh`，因为它会执行 `build-core.sh` 并可能写入 `apps/macos/AreaMatrix/**` 生成物，当前 task 明确禁止触碰该路径；同时本机缺少 `uniffi-bindgen`、`swiftformat`、`swiftlint`。

六、风险与后续

剩余风险：完整 check-all 的 Rust/macOS 全量执行需在允许生成物写入、且工具链齐全的环境中补跑。建议下一个动作是生成并执行 `0-2/task-08` 的 verify-ready 只读验收；通过后继续 `0-2/task-09`。

