一、执行结论

已完成。上次 P0 已修复：Bridge/Models 现在纳入 `AreaMatrix` target 的 Sources，`xcodebuild build` 能真实编译 `CoreBridge.swift`、`AppShellModel.swift`、`CoreBridgePlaceholderState.swift`。

二、执行范围

单任务：`0-2/task-11`

修改文件：
- [project.pbxproj](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix.xcodeproj/project.pbxproj:13)：加入 Bridge/Models file reference 与 Sources build phase。
- [AppShellModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/AppShellModel.swift:1)：从 `@Observable` 宏占位改为普通 `Equatable, Sendable` 值模型，避免 Phase 0 shell 引入宏插件依赖。
- 既有 task 改动继续保留：[CoreBridge.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Bridge/CoreBridge.swift:1)、[CoreBridgePlaceholderState.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/CoreBridgePlaceholderState.swift:1)。

三、完成情况

- 创建 CoreBridge 占位层：通过。
- Bridge 方法只声明后续接入边界：通过，调用统一抛 `generatedBindingsUnavailable`。
- 不得用 mock 通过后续真实闭环验收：通过，没有静态成功态。
- Bridge 编译通过：通过，`AreaMatrix.SwiftFileList` 已包含 Bridge/Models 三个文件。
- 占位状态可识别：通过，`CoreBridgePlaceholderState.phase0` 明确标识 placeholder。
- Forbidden Touches：未触碰 `core/src/**`。为修复上次 P0，额外触碰了 `project.pbxproj`，该文件不是 Forbidden Touches，且正是上次验收指出的缺口。

四、验证情况

已通过：
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`。
- `plutil -lint apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：OK。
- `git diff --check -- ...`：通过。
- `git diff --name-only -- core/src` / `git diff --cached --name-only -- core/src`：无输出。

补充证据：
- `AreaMatrix.SwiftFileList` 当前包含 `CoreBridge.swift`、`AppShellModel.swift`、`CoreBridgePlaceholderState.swift`。

未完全通过：
- `xcodebuild test ...`：被当前 sandbox 阻断，错误为 `com.apple.testmanagerd.control` 通信受限；app/test target 已编译，失败发生在 test runner 建联。
- `swiftformat` / `swiftlint`：当前环境未安装，对应 `command -v` 无结果。

五、风险与后续

剩余风险：当前仍是显式 placeholder，不是 UniFFI 真实闭环；`xcodebuild test` 需要在允许 testmanagerd 通信的本机环境补跑。

建议下一个任务：继续 `0-2/task-12`，补齐 macOS test target 的正式验收边界。  

