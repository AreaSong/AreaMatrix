一、执行结论

- 已完成：`1-1/task-01` 的 C1-01 `validate_repo_path` contract-api 范围已落地。
- 未声明完成：真实路径校验实现仍属于下一个任务 `1-1/task-02`。

二、执行范围

单任务：`1-1/task-01`

修改文件：

- [docs/api/core-api.md](/Users/as/Ai-Project/project/AreaMatrix/docs/api/core-api.md:31)：补齐 `validate_repo_path` API、DTO、错误码、副作用边界，并从 API 缺口表移除 C1-01。
- [core/area_matrix.udl](/Users/as/Ai-Project/project/AreaMatrix/core/area_matrix.udl:7)：新增 UniFFI 合同。
- [core/src/domain.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/domain.rs:36)：新增 `RepoPathValidation` / `RepoPathIssue` public domain types。
- [core/src/api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:29)：新增 Rust public API 签名和 rustdoc。
- [core/tests/validate_repo_path_contract.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/validate_repo_path_contract.rs:1)：新增合同级编译/结构测试。

三、完成情况

- 核对清单 1：通过，只处理 C1-01 contract-api。
- 核对清单 2：通过，已读取 manifest Exact Docs 并按 `docs/` 作为 SSOT。
- 核对清单 3：通过，未实现相邻 C1 能力或 UI 页面。
- 核对清单 4：通过，Core API / UDL / Rust public surface 已对齐。
- 核对清单 5：通过，文档明确输入、输出、错误码和只读副作用边界。
- 完成标准：contract-api 目标有 UDL 生成、Rust 编译和测试证据；没有扩展到未绑定能力。
- Forbidden Touches：未触碰 `apps/**`。

四、工程质量

- 结构清晰：合同类型集中在 `domain.rs`，FFI 签名在 UDL，public API 在 `api.rs`，测试在 `core/tests/**`。
- rustdoc 已补：新增 public enum、struct、API 都有说明；API 包含 `# Errors`。
- 错误与边界：文档明确 `InvalidPath`、`PermissionDenied`、`ICloudPlaceholder`、`RepoNotInitialized`，并明确不创建、不删除、不移动、不下载 iCloud placeholder。
- 占位说明：没有新增 mock-only 或硬编码通过态；`validate_repo_path` 的真实业务体仍沿用当前 Core 骨架 `not_implemented()`，这是 `1-1/task-02` 的实现范围，本任务不把它宣称为真实闭环。

五、验证情况

全部通过：

```bash
cd core && cargo fmt --all -- --check
cd core && cargo clippy --all-targets --all-features -- -D warnings
cd core && cargo test --workspace validate_repo_path
cd core && cargo test --workspace
git diff --check
```

`validate_repo_path` 过滤测试实际执行 2 个合同测试，全部通过。

六、风险与后续

剩余风险：当前只完成合同，不具备真实文件系统校验行为；后续 verify 不能把本任务当成 C1-01 完整闭环。

建议下一个任务：执行 `1-1/task-02`，实现 C1-01 的只读路径校验，包括空目录、非空目录、已初始化目录、`.areamatrix/` 内部路径、权限和 iCloud 边界。

