未开始修改。这里有一个硬约束冲突需要先确认：

- 本次 prompt 写明：`是否允许修改文件：是，仅限 Expected New Paths`
- Manifest 的 `Expected New Paths` 只有：
  - `core/src/**`
  - `core/area_matrix.udl`
  - `core/tests/**`
- 但上一次验收失败把 `docs/api/core-api.md` 仍声明 `validate_repo_path` 是 “Stage 1 API 缺口”列为 P0/P1 级失败证据；要彻底修复这个失败，需要同步修改 `docs/api/core-api.md`，这超出了当前 Expected New Paths。

我的建议是扩大本次重试允许范围，额外允许修改 `docs/api/core-api.md`，只移除/校准 C1-01 这一项缺口描述，不改相邻能力。风险是 Core API 文档、UDL、Rust public API 会同时变化；验证会跑 manifest 三条命令，并额外核对 `validate_repo_path` 在 docs / UDL / Rust / tests 一致。回滚方式是只回退这次触碰的 `docs/api/core-api.md`、`core/area_matrix.udl`、`core/src/**`、`core/tests/**` 改动。

请确认：是否允许本次为修复上次验收失败，额外修改 `docs/api/core-api.md`？  
