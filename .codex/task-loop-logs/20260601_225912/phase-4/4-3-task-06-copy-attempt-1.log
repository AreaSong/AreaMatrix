一、执行结论

已完成 `4-3/task-06: C4-02 contract-api`。

二、执行范围

单任务，仅修改 / 新增：

- [api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/src/api.rs:80)：补充 C4-02 mobile repo connect 合同说明，明确 iOS security-scoped URL / provider path 由平台层授权，Core 只接收授权后的路径。
- [mobile_repo_connect_contract_api.rs](/Users/as/Ai-Project/project/AreaMatrix/core/tests/mobile_repo_connect_contract_api.rs:40)：新增 C4-02 contract test，覆盖 Rust 签名、UDL、Core API、错误码、control map、消费页面状态路由。

`core/area_matrix.udl` 已检查，现有 `validate_repo_path`、`init_repo`、`load_config`、`RepoPathValidation`、`RepoInitOptions`、`RepoConfig` 和错误码合同已满足 C4-02，无需修改。未触碰 `apps/**`。

三、完成情况

核对清单全部完成：

- 已读取 C4-02 能力规格、Stage 4 control map、Core API、错误码。
- 已确认 API 名称、输入、输出、错误码、权限/隐私边界一致。
- 只补合同说明与合同测试，没有实现相邻能力。
- 已用测试记录消费方状态：已有 repo、空目录、非空目录分别能从 `RepoPathValidation` / `RepoConfig` 得到路由依据。

完成标准满足：

- C4-02 Core API 合同可被后续 implementation task 直接实现。
- 未引入 control map 之外的页面能力。

四、工程质量

代码结构保持清晰，没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本。新增 public API 相关说明放在已有 rustdoc 中，强调平台权限边界、确认页边界和只读配置读取边界。错误边界覆盖 `PermissionDenied`、`InvalidPath`、`ICloudPlaceholder`。

五、验证情况

已运行并通过：

```bash
cargo fmt --all
./dev check task 4-3/task-06
```

`./dev check task 4-3/task-06` 实际执行了：

- prompt doctor：OK
- git diff --check：OK
- targeted Rust test：`cargo test --test mobile_repo_connect_contract_api -- --nocapture`，4 passed

未运行全量 `cargo test --workspace` / clippy / `./dev check all`，因为本 ordinary atomic Core task 的 manifest 验证上限是 `./dev check task 4-3/task-06`。

六、风险与后续

剩余风险：未做真实 iOS picker / security-scoped bookmark UI 验证，这属于后续 iOS page-feature 或 implementation task，不在本合同任务范围内。

建议下一个任务：继续 `4-3/task-07`，按 C4-02 implementation 边界实现或补齐具体移动端 repo connect 行为。工作区中仍有执行前已存在的 `.codex/task-loop-runs/20260601_225912/summary.json` 与 `tasks/prompts/_shared/progress.json` 改动，未纳入本任务变更清单。