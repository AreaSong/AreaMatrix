一、执行结论

已完成。

二、执行范围

单任务：`4-3/task-01`，C4-01 Core API / UDL 合同补齐。

修改文件清单：
- `core/src/cross_platform_ffi.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `core/tests/cross_platform_ffi_contract_api.rs`
- `docs/api/core-api.md`

`docs/api/core-api.md` 是本 task 的 Exact Doc；为避免 Core API SSOT 与 UDL / Rust 合同漂移，已同步补齐。

三、完成情况

核对清单：
- 已读取并对齐 C4-01 能力规格、Stage 4 control map、Core API、错误码。
- 已新增 `inspect_binding_contract(BindingContractRequest) -> BindingContractReport` 合同，覆盖 target platform、binding version、supported APIs、type mappings、missing capabilities。
- 错误边界为 `Config` 和 `Internal`，无 repo、DB、文件系统、平台 UI 或用户文件副作用。
- 页面消费方 `S4-X-02 platform-differences` 可从结构化 report 得到 Core version、API/type mapping、缺失或受限能力状态，不需要 UI 猜测。

完成标准：
- C4-01 Core API 合同已可供后续 implementation task 直接实现或桥接。
- 没有引入 control map 之外的页面能力，也没有实现 C4-17 等相邻平台能力。
- 未触碰 `Forbidden Touches`：`apps/**` 没有改动。

四、工程质量

代码结构集中在独立 `cross_platform_ffi` 模块，`api.rs` 只暴露 UniFFI 公共入口，`lib.rs` 只导出合同类型。新增 public Rust API 和类型均有 rustdoc；错误路径显式返回 `CoreError::Config` / `CoreError::Internal`。

实现是只读合同检查，没有 mock-only、硬编码通过态或一次性脚本化实现；未新增依赖。

五、验证情况

已通过：
- `./dev check task 4-3/task-01`
  - `prompt doctor`: OK
  - `git diff --check`: OK
  - `cargo test --test cross_platform_ffi_contract_api -- --nocapture`: 4 passed
- `cargo fmt --all -- --check`: 通过

未运行宽门禁：按本 atomic Core task 要求，验证上限是 `./dev check task 4-3/task-01`，未升级到 `cargo test --workspace` / `clippy` / `./dev check all`。

六、风险与后续

剩余风险：这是合同/API task，尚未生成或验证真实 Kotlin/Python/Swift 绑定产物；后续平台 implementation / integration task 需要继续验证生成绑定和平台 shell 消费。

建议下一个任务：进入 C4-01 后续 implementation 或 platform binding 验证任务，基于当前 `inspect_binding_contract` 合同接入真实绑定生成与平台消费检查。

备注：工作树中还显示 `tasks/prompts/_shared/progress.json` 和 `.codex/task-loop-runs/20260601_225912/` 的 task-loop 运行产物；它们不是本次 C4-01 合同代码改动范围。