一、执行结论

已完成 `4-2/task-16: C3-04 contract-api`。

二、执行范围

修改文件：
- `core/src/ai_classification_suggestion.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `core/tests/ai_classification_suggestion_contract_api.rs`
- `docs/api/core-api.md`

未触碰 `apps/**`。工作区里原本存在的 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 仍是未关联 dirty 文件，我未修改它们。

三、完成情况

核对清单已完成：
- 读取并对齐 C3-04 能力规格、Stage 3 control map、Core API、错误码。
- 补齐 `suggest_category_with_ai(repo_path, request) -> AiCategorySuggestion` 的 Rust / UDL / Core API 合同。
- 输入覆盖 `file_id`、上下文提取策略、隐私策略引用；输出覆盖建议分类、confidence、reason、本地/远程 route、used context、skipped reason、privacy rule id、call log id、`requires_user_confirmation`。
- 合同明确：用户确认前不改 `files.category`、不移动文件、不写 classifier rule、不实现 C3-05/C3-09/C3-10。
- 页面消费方 S3-04 / S3-10 可从合同拿到建议态、跳过态、隐私跳过、fallback 所需状态。

四、工程质量

代码结构保持为独立 C3-04 合同模块，API rustdoc 明确错误、隐私、AI gate 和副作用边界。当前实现只做合同与输入边界校验，返回明确 `Config` pending error，不生成 AI 结果、不接 provider、不写 DB、不触碰用户文件；后续 `task-17` 负责真实行为闭环。

五、验证情况

已运行：
- `cargo fmt --all`
- `cargo fmt --all -- --check`：通过
- `./dev check task 4-2/task-16`：通过
  - prompt doctor：OK
  - `git diff --check`：OK
  - `cargo test --test ai_classification_suggestion_contract_api -- --nocapture`：4 passed

六、风险与后续

剩余风险：C3-04 目前是 contract/API 阶段，不包含真实 AI 建议生成、AI call log 写入、隐私规则评估或 fallback reason matrix 实现。

建议下一个任务：`4-2/task-17`，实现 C3-04 的最小 Core 行为闭环。