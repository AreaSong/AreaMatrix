一、执行结论

已完成 `4-2/task-33: C3-07 failure-edge`。

二、执行范围

单任务，未触碰 `apps/**`。

实现改动清单：
- `core/src/ai_tags_suggestion/implementation.rs`
- `core/src/api.rs`
- `core/tests/ai_tags_suggestion_failure_recovery.rs`
- `core/tests/support/ai_tags_suggestion_failure.rs`
- `core/tests/support/ai_tags_suggestion_common.rs`

工作区另有 task-loop 运行状态文件已处于 modified：`.codex/task-loop-runs/20260527_221647/summary.json`、`tasks/prompts/_shared/progress.json`。它们不是本次产品实现改动范围。

三、完成情况

核对清单全部完成：
- 空态：AI 默认关闭、runtime 返回空建议均有测试。
- 非法输入：缺失 file、非法 tag、未确认 apply、重复建议、敏感 privacy ref 均返回明确错误且不写入。
- 权限与 IO/DB：保留 `PermissionDenied` / `Io`，DB 损坏映射为 `Db`，并验证 error mapping。
- 写入失败回滚：验证单 item savepoint 回滚、undo/call-log 失败整笔 transaction 回滚，且用户可见文件列表不变。
- 隐私/远程：默认关闭不调用 runtime，privacy skip 不调用 provider，远程 key/reference 不进入错误、report 或 AI call log。
- 未使用吞错或静默降级掩盖失败。

四、工程质量

结构保持在 C3-07 现有实现内，只修正错误映射并补充失败路径测试；没有新增依赖、mock-only 产品路径或一次性硬编码通过态。新增 helper 仅服务集成测试隔离与故障注入。`core/src/api.rs` 只同步 rustdoc 错误说明；UDL 未改，因为现有签名和错误枚举已覆盖。

五、验证情况

已通过：
- `cargo fmt --manifest-path core/Cargo.toml --all -- --check`
- `cargo test --manifest-path core/Cargo.toml --test ai_tags_suggestion_failure_recovery -- --nocapture`，10 passed
- `./dev check task 4-2/task-33`

`./dev check task 4-2/task-33` 内部通过：
- prompt doctor：637 tasks / 637 manifests
- `git diff --check`
- widened Core quality gate：`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`
- `ai_tags_suggestion_contract_api`：4 passed
- `ai_tags_suggestion_failure_recovery`：10 passed
- `ai_tags_suggestion_implementation`：3 passed

说明：我曾从 repo 根直接跑 `cargo fmt --all -- --check`，因根目录无 `Cargo.toml` 无法运行；随后用 `core/Cargo.toml` 和 task 门禁完成了有效验证。

六、风险与后续

剩余风险：`core/src/api.rs` 是既有超 500 行共享 API 文件，本次只做局部 rustdoc 同步，未扩大重构。建议下一个任务继续按 manifest 执行 `4-2/task-34`，不要顺手扩展 C3-07 以外能力。

