**一、执行结论**

已完成。

**二、执行范围**

单任务：`4-2/task-08`，只处理 C3-02 local-model-status failure-edge。

修改文件：
- `/Users/as/Ai-Project/project/AreaMatrix/core/src/local_model_status.rs`
- `/Users/as/Ai-Project/project/AreaMatrix/core/src/local_model_status/inspection.rs`
- `/Users/as/Ai-Project/project/AreaMatrix/core/src/local_model_status/snapshot.rs`
- `/Users/as/Ai-Project/project/AreaMatrix/core/tests/local_model_status_contract_api.rs`
- `/Users/as/Ai-Project/project/AreaMatrix/core/tests/local_model_status_failure_recovery.rs`

未触碰 `apps/**`。工作区里 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 已是本地修改状态，不纳入本次实现范围。

**三、完成情况**

核对清单结果：
- 空态：覆盖 missing model folder / locate missing folder，且不创建目录。
- 非法输入：覆盖 cached status mismatch、负 size、敏感 last_error / diagnostics。
- 权限：覆盖 status cache 写入权限失败，返回 `PermissionDenied`，用户 README 保持不变。
- IO/DB：覆盖 manifest IO 失败、DB/cache 损坏映射，失败不写缓存。
- 错误码映射：覆盖 `Config` / `PermissionDenied` / `Io` 的 UI mapping。
- 隐私/AI/远程：敏感 manifest/runtime/cached 错误会脱敏或拒绝；不启用 remote fallback，不写 AI call log。
- 不吞错：非法 manifest availability 现在返回明确 `last_error` 和 diagnostics，不再静默泛化。

完成标准：
- C3-02 失败路径已有明确返回和测试证据。
- 高风险路径具备恢复/回滚语义：本 API 只读模型目录；唯一写入是成功后的 AreaMatrix-owned status cache，失败测试证明不留下缓存半成品、不改用户文件。

**四、工程质量**

结构上把 snapshot 构造逻辑拆到 `local_model_status/snapshot.rs`，`inspection.rs` 回落到 436 行，避免文件长度和 clippy 问题。没有新增依赖，没有 mock-only、硬编码通过态或一次性脚本实现。未修改 UDL，因为现有 `core-api.md` / `area_matrix.udl` 合同已覆盖 C3-02。

**五、验证情况**

已运行并通过：

```bash
./dev check task 4-2/task-08
```

该命令实际完成：
- `prompt_pipeline.py doctor`: OK
- `git diff --check`: OK
- `cargo fmt --all -- --check`: OK
- `cargo clippy --all-targets --all-features -- -D warnings`: OK
- `cargo test --test local_model_status_contract_api -- --nocapture`: 5 passed
- `cargo test --test local_model_status_failure_recovery -- --nocapture`: 8 passed
- `cargo test --test local_model_status_implementation -- --nocapture`: 5 passed

过程中曾遇到 fmt/clippy 阻断，已修复并在最终验证后重新跑通。

**六、风险与后续**

剩余风险：未跑 macOS UI，因为本 task 不允许触碰 `apps/**`，且验证上限是 `./dev check task 4-2/task-08`。CI/Git checkpoint 仍需由 task-loop 或后续提交流程处理。

建议下一个任务：继续 `4-2/task-09`，进入 C3-02 validation。