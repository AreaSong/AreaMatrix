一、执行结论

已完成 `4-2/task-21: C3-05 contract-api`。

二、执行范围

单任务执行。修改/新增文件：

- `core/src/ai_call_log.rs`
- `core/src/api.rs`
- `core/src/lib.rs`
- `core/area_matrix.udl`
- `core/tests/ai_call_log_contract_api.rs`
- `docs/api/core-api.md`

`docs/api/core-api.md` 是本 task 的 Exact Doc。因 C3-05 Core API 合同缺失，按自动授权中的“直接相关 Core API / UDL 源事实漂移可同步修复”处理。

三、完成情况

核对清单：

- 已读取 C3-05 能力规格、Stage 3 control map、Core API、错误码。
- 已补齐 `list_ai_calls`、`clear_ai_call_log` 的输入、输出、错误码、隐私/权限边界。
- 只补合同、类型、UDL 桥接声明和 Core API 文档；未实现相邻 AI 能力。
- 已记录 S3-05 消费方可从合同得到列表、过滤、详情、远程/隐私 skipped、Provider Test、清除结果和 90 天保留状态。

完成标准：

- C3-05 Core API 合同已可供后续 `4-2/task-22` implementation task 直接实现。
- 未引入 control map 之外页面能力。
- 未触碰 `apps/**`；确认无 `apps/**` diff 或未跟踪文件。

四、工程质量

新增 Rust public 类型/API 均有 rustdoc，合同层显式校验 filter、pagination、clear scope、selected ids 和 retention cutoff。有效业务行为仍返回明确 `Db` unavailable 错误，避免 contract task 伪造成功闭环；真实 list/clear 行为留给 implementation task。

无新增依赖，无 mock-only wiring，无硬编码通过态。没有删除、移动、覆盖用户文件，也没有触碰 DB migration、远程调用或 macOS UI。

五、验证情况

已运行并通过：

- `cargo fmt --all -- --check`
- `cargo test --test ai_call_log_contract_api -- --nocapture`
- `git diff --check`
- `./dev check task 4-2/task-21`

`./dev check task 4-2/task-21` 结果：通过；包含 `prompt doctor: OK`、`diff check`、targeted test `ai_call_log_contract_api`，4 tests passed。

未运行：

- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --workspace`
- `./dev check core`
- `./dev check all`

原因：本 task manifest 验证上限是 `./dev check task 4-2/task-21`，普通 atomic Core task 不升级宽门禁。

六、风险与后续

剩余风险：C3-05 目前是合同/API 层，真实 DB list/clear 行为尚未实现，后续 UI 不能把它当作真实闭环。

建议下一个任务：`4-2/task-22`，实现 C3-05 最小 Core 行为闭环。当前工作区里 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 是开始前已有的 task-loop 状态改动，我未处理。