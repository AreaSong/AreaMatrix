一、执行结论

已完成。

二、执行范围

单任务：`4-3/task-128`，只修复上次验收指出的 C4-21 Replace repair 问题。

本次 repair 直接改动：
- `apps/windows/AreaMatrix/Features/Import/DesktopImportModels.cs`
- `apps/windows/AreaMatrix/Features/Import/DesktopImportReplaceModels.cs`
- `apps/windows/AreaMatrix/Features/Import/WindowsImportViewModel.Replace.cs`
- `apps/windows/AreaMatrix/Features/Import/WindowsImportFileProbe.cs`
- `apps/windows/AreaMatrixTests/Import/WindowsImportReplaceViewModelTests.cs`
- `apps/windows/AreaMatrixTests/Import/WindowsImportViewModelTests.cs`
- `apps/windows/AreaMatrixTests/Import/WindowsImportTestSupport.cs`
- `apps/windows/AreaMatrixTests/Import/WindowsImportSmokeTests.cs`

三、完成情况

核对清单：
- 只处理 S4-WIN-05 + C4-21：通过。
- 已对齐页面规格、control map、Core API、C4-21 能力规格：通过。
- 只接入 C4-21 对应 Core preview/apply、状态和错误映射：通过。
- 未实现相邻页面、相邻平台或同页其他能力：通过。

完成标准：
- Replace 可见功能仍通过 `Preview Replace` / `Apply Replace` 和二次确认触发：通过。
- 无 Core import session / conflict id 的 direct name-conflict 不再可执行 Replace：通过。
- 不再用 `.areamatrix` 可写或 `trash-pending` 目录冒充 Recycle Bin preflight：通过。
- `ApplyReplaceAsync` 不再走 `ImportFileWithResultAsync(...Overwrite...)` fallback：通过。
- Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 无输出。

四、工程质量

代码结构保持在现有 Windows import ViewModel/模型/测试结构内，没有新增跨模块抽象。  
Replace 现在只有 Core conflict batch preview/apply 一条执行路径；无 Core preview 时只显示阻断原因，不生成确认态、不 apply、不覆盖文件。  
错误和边界处理覆盖了无 session、无 conflict id、未二次确认、Move 未确认，以及 direct fallback 不应调用 import overwrite。  
未新增 public Rust API，无 rustdoc 需求；未新增依赖；无 mock-only 或硬编码通过态替代真实 Core 闭环。

五、验证情况

已通过：
- `./dev check task 4-3/task-128`
  - `doctor: OK`
  - `git diff --check` 通过
  - 映射的 `xcodebuild ... build CODE_SIGNING_ALLOWED=NO` 最终 `BUILD SUCCEEDED`
- `dotnet run --project apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj`
  - 输出：`AreaMatrix Windows tests passed.`

补充说明：
- `dotnet test apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj` 返回 0，但该项目是 `OutputType=Exe`，只显示 restore，不作为主要断言证据。
- 未运行 `./dev check all`、`./dev check core`、`cargo test --workspace`，因为当前 manifest 验证上限是 `./dev check task 4-3/task-128`。

六、风险与后续

剩余风险：当前在本机 macOS 环境无法做真实 Windows Recycle Bin UI/系统集成实机验证；本次证据覆盖的是 Windows C# 状态机、Core preview/apply 强制路径和静态 wiring。  
建议下一个任务进入 `4-3/task-129` 或对应 verify，继续按 S4-WIN-05 的 page-feature/page integration 边界验收整页覆盖。