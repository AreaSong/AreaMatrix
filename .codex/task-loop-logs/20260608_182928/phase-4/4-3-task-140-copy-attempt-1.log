一、执行结论

- 已完成。
- 本次未做代码改动；现有 `apps/**` 实现已满足 `4-3/task-140` 页面集成验收，验证通过。

二、执行范围

- 单任务：`4-3/task-140`
- 修改文件清单：无
- 未触碰 `Forbidden Touches`：`core/src/**` 未被修改
- 工作树中原有的 `.codex/task-loop-runs/20260608_182928/summary.json`、`tasks/prompts/_shared/progress.json` 已存在修改，我未触碰。

三、完成情况

- page-feature task 覆盖：`4-3/task-138` 覆盖 C4-13，`4-3/task-139` 覆盖 C4-21；control map 中 S4-LNX-05 仅声明 `C4-13, C4-21`。
- 页面闭环：Linux import dialog 已包含文件选择、文件夹选择、拖拽、预览、Copy/Move、Move 确认、结果、重试、关闭刷新、Replace 二次确认路径。
- CoreBridge：C4-13 走 `PredictCategoryAsync` / `ImportFileWithResultAsync`；C4-21 走 `PreviewImportConflictBatchAsync` / `ApplyImportConflictBatchAsync`，没有用 import overwrite 兜底伪造 Replace。
- mock / fixture / 硬编码状态：生产路径未发现 mock-only 闭环；测试 fake 仅在 `apps/linux/AreaMatrixTests/**` 内作为验证替身。
- 未新增 control map 之外功能。

四、工程质量

- 代码结构维持现状，未引入新抽象或一次性脚本。
- 本次未新增注释、rustdoc 或文档。
- 错误态已有映射：DB busy、权限、无效路径、冲突、失败重试、Move retained、Replace blocked 等均有测试或状态路径。
- 高风险文件安全边界未改 Core 和真实文件删除逻辑；Move/Replace 仍由既有 preflight 与 Core 合同约束。

五、验证情况

- 已运行：`./dev check task 4-3/task-140`
- 结果：通过，退出码 0
- 关键输出：`prompt doctor` OK、`git diff --check` 通过、`xcodebuild ... build CODE_SIGNING_ALLOWED=NO` 最终 `BUILD SUCCEEDED`
- 运行中有 CoreSimulator / FSEvents 相关环境 warning，但未导致验证失败。

六、风险与后续

- 剩余风险：本次验证是 task 映射下的本地 targeted gate，不等同于全量 CI 或真实 Linux GTK 手工交互验收。
- 建议下一个任务：继续 `4-3/task-141`，它依赖当前 `4-3/task-140`。