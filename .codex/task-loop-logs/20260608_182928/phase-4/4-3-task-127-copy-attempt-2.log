一、执行结论

已完成。已修复上次验收指出的 C4-13 冲突预览状态与 Move preflight 缺口；当前 task manifest validation 已通过。Windows 产品 WinUI 构建仍需 Windows 环境补跑，原因是 macOS 无法执行 WinUI `XamlCompiler.exe`。

二、执行范围

单任务：`4-3/task-127`

修改范围在 `apps/windows/**`：
- `apps/windows/AreaMatrix/Features/Import/**`
- `apps/windows/AreaMatrix/Core/AreaMatrixNativeCoreClient.DesktopImport.cs`
- Windows 主窗口 Import 路由相关文件
- `apps/windows/AreaMatrixTests/Import/**`
- Windows 测试项目/入口

未触碰 `core/src/**`。工作区里已有 `tasks/prompts/_shared/progress.json` 与 `.codex/task-loop-runs/**` 变动/输出，不属于我本次源码实现范围。

三、完成情况

核对清单：
- 只处理 S4-WIN-05 的 C4-13：通过。
- 已读取页面规格、control map、Core API、C4-13：通过。
- 只接入 C4-13 的 `predict_category` / `import_file_with_result`、状态与错误映射：通过。
- 未实现 C4-21 Replace / Overwrite / Index in place：通过，smoke test 明确断言这些入口不存在。

完成标准：
- 用户可见功能：Import 按钮、对话框、文件/文件夹/拖拽入口、预览、Move 确认、结果区已接入。
- 无 mock-only 闭环：生产路径使用 `DesktopImportCoreBridge(coreClient, new WindowsImportFileProbe())` 和真实 native symbols。
- 未触碰 control map 之外能力：通过。

四、工程质量

补齐了 `Ready / Duplicate / NameConflict / Unreadable` 四类文档要求状态，移除了不在 S4-WIN-05 状态表内的 `NeedsCategory`。`WindowsImportFileProbe` 增加 hash duplicate、name conflict、源可读、源可移除、repo/staging 可写 preflight；Move preflight 失败会禁用 Import，不会静默降级为 Copy。

测试补了真实 file probe 的 staging 缺失 preflight 覆盖。无新增 Rust public API，无 rustdoc 需求；无新增依赖；未发现占位、硬编码通过态或 mock-only 生产 wiring。

五、验证情况

已通过：
- `dotnet run --project apps/windows/AreaMatrixTests/AreaMatrix.Windows.Tests.csproj`
- `./dev check task 4-3/task-127`
- `git diff --check`
- `git diff -- core/src --name-only && git diff --cached -- core/src --name-only` 无输出

额外产品构建补证：
- 直接 `dotnet build apps/windows/AreaMatrix/AreaMatrix.Windows.csproj --no-restore` 先受 NuGet service index / `CookieContainer GetDomainName: -1` 影响。
- 我用 `/private/tmp` 本地 NuGet 源补齐依赖后，restore 成功。
- 随后 build 进入 WinUI XAML 编译阶段失败：macOS 无法执行 Windows `XamlCompiler.exe`，错误为 `cannot execute binary file`。因此真实 Windows 产品构建和 UI 点击需要 Windows runner 补跑。

六、风险与后续

剩余风险：本机无法提供真实 Windows WinUI 窗口点击证据；当前证据覆盖 C# ViewModel、XAML smoke、native bridge 符号与 manifest validation。

建议下一个任务：在 Windows 环境补跑 `dotnet restore/build` 和一次真实 WinUI Import dialog 操作验收，或进入 `4-3/task-128` 继续 C4-21 Replace 边界。