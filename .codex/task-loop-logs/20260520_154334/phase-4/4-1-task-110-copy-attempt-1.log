一、执行结论

已完成 `4-1/task-110`。

二、执行范围

单任务：只实现 `S2-09 batch-add-tags` 中的 `C2-06 batch_add_tags` 页面功能接入。

修改文件：
- `apps/macos/AreaMatrix/Bridge/CoreTagCRUD.swift`
- `apps/macos/AreaMatrix/Views/Main/ImportEntrySheetHelpers.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentNoteDrafts.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryDetailPane.swift`
- `apps/macos/AreaMatrixTests/DetailMultiListFilesPageFeatureTests.swift`
- `apps/macos/AreaMatrixTests/ChangeCategoryPageIntegrationVerifyTests.swift`
- `apps/macos/AreaMatrixTests/ICloudConflictMinimalPageIntegrationVerifyTests.swift`

三、完成情况

- 已接入 `CoreBridge.batchAddTags(repoPath:fileIDs:tags:)`，通过真实 `AreaMatrix.batchAddTags` 调用 Core，不是 mock/fixture 产品路径。
- Detail multi 里新增 `Add tag...` 入口，打开 `BatchAddTagsSheet`。
- Sheet 支持标签输入、pending tags、非法/重复/只读禁用、Apply、执行中状态、结果摘要、失败详情、undo token 提示。
- 只覆盖 `C2-06`；未实现 `C2-07` 的 Undo 执行链路。
- 未触碰 Forbidden Touches：`core/src/**` 没有变更。
- 未修改 `.xcodeproj`，因此测试补在已有 target 文件内。

四、工程质量

代码结构按 UI 触发、输入校验、Core action、结果展示拆开；没有新增依赖，没有硬编码成功态，没有 mock-only 闭环。新增测试覆盖 pending tag 归一化/重复/非法/只读禁用，以及 `CoreTagCRUD.batchAddTags` 请求透传和错误映射。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-110
```

该命令实际执行：
- `python3 tasks/prompts/_shared/prompt_pipeline.py doctor`：OK，`tasks: 637`，`manifests: 637`
- `git diff --check`：通过
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination platform=macOS,arch=arm64 build CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`

六、风险与后续

剩余风险：本轮按 manifest 上限只跑 task check，没有额外运行全量 macOS test 或真实窗口交互；但这是当前 task 明确的验证上限。高风险点在批量标签 metadata 写入，回滚方式是 revert 上述 macOS app/test 文件；未触碰用户原文件、文件路径、Core 源码或 DB migration。

建议下一个任务：`4-1/task-111`，继续实现同页 `C2-07 undo-action-log`。当前工作树还显示 `.codex/task-loop-runs/.../summary.json` 和 `tasks/prompts/_shared/progress.json` 为 dirty，这是 task-loop 运行证据，不属于本次产品代码实现范围。

