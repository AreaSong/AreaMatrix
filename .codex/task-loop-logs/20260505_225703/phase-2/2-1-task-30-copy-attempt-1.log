一、执行结论

已完成 `2-1/task-30`。

二、执行范围

单任务：S1-11 main-repo-error + C1-01 validate-repo-path。  
修改文件都在允许范围内：

- `apps/macos/AreaMatrix/App/AreaMatrixApp.swift`
- `apps/macos/AreaMatrix/Bridge/CoreBridge.swift`
- `apps/macos/AreaMatrix/Models/AppShellModel.swift`
- `apps/macos/AreaMatrix/Models/CoreBridgePlaceholderState.swift`
- `apps/macos/AreaMatrix/Models/OnboardingInitializationProgress.swift`
- `apps/macos/AreaMatrix/Views/MainWindow.swift`
- `apps/macos/AreaMatrixTests/AreaMatrixShellTestSupport.swift`
- `apps/macos/AreaMatrixTests/AreaMatrixShellTests.swift`
- `apps/macos/AreaMatrixTests/CoreBridgeRepositoryTests.swift`

三、完成情况

核对清单通过：只处理 S1-11 的 C1-01；已读取页面规格、control map、Core API、C1-01；只接入 `validate_initialized_repo_path` 对应 CoreBridge / 状态 / 错误映射；未实现相邻页面或 C1-19/C1-21。

完成标准通过：S1-11 现在有可触发的 `Retry`，会先走真实 CoreBridge `validateInitializedRepoPath`，成功后复用现有打开 repo 流程，失败则保留错误页并展示结构化映射。生产路径没有 mock、fixture 或静态伪造。

Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

结构保持在现有 `OnboardingModel` / `CoreBridge` / `MainWindow` 模式内，没有新增不必要抽象或依赖。错误页加入重试中禁用、防重复点击、进度状态、用户文件未丢失提示；失败路径通过 `CoreErrorMapping` 显式映射。测试使用 recording test doubles，只用于 XCTest，不进入生产闭环。

五、验证情况

已运行并通过：

```bash
xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO
```

已运行并通过：

```bash
./dev test macos
```

`./dev test macos` 中标准 `xcodebuild test` 被本地 sandbox 的 `testmanagerd` 通信限制阻断，脚本按仓库规则复用同一 XCTest bundle 执行 `xcrun xctest`，结果 `113 tests, 0 failures`。

六、风险与后续

残余风险：未做手工 UI / VoiceOver 验证；S1-11 的 C1-19、C1-21 仍按 task 边界留给后续任务和 page integration verify。工作区中 `.codex/task-loop-runs/.../summary.json` 与 `tasks/prompts/_shared/progress.json` 仍有既有脏改动，不在本 task Expected New Paths 内，我没有回滚或改动它们。

建议下一个任务：`2-1/task-31`，继续同页其他能力边界。

