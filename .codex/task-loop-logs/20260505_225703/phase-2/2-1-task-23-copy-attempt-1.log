一、执行结论

已完成 `2-1/task-23: S1-09 + C1-12 get-file-detail`。

二、执行范围

单任务。修改文件：

- `apps/macos/AreaMatrix/Bridge/CoreBridge.swift`
- `apps/macos/AreaMatrix/Bridge/CoreFileListing.swift`
- `apps/macos/AreaMatrix/Models/MainFileListModel.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentView.swift`
- `apps/macos/AreaMatrixTests/MainListFilesTests.swift`

三、完成情况

- 只处理了 S1-09 中 C1-12 的 `get_file` 详情读取。
- 已读取并对齐 S1-09 页面规格、MVP control map、Core API、C1-12 capability spec。
- Swift UI 现在通过 `CoreBridge.getFile(repoPath:fileID:)` 调用真实 generated UniFFI `getFile`，没有 mock-only 闭环。
- 列表单选会触发详情加载，Detail 面板显示真实 `FileEntry` 元数据；`FileNotFound` 等 Core 错误会走统一错误映射并显示 inline retry。
- 未实现 C1-11/C1-15 的新增功能，未实现相邻页面。
- Forbidden Touches：未触碰 `core/src/**`。`git diff --name-only -- core/src` 为空。

四、工程质量

代码结构按现有 `CoreFileListing` / `MainFileListModel` / `MainRepositoryContentView` 模式扩展，没有新增依赖、没有一次性脚本、没有静态伪造状态。错误路径显式映射为 `CoreErrorMappingSnapshot`，切换分类会清空当前 Detail，避免陈旧详情残留。未改 Rust public API，因此无 rustdoc/doc sync 需求。

五、验证情况

已运行并通过：

- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
- `./dev test macos`：标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断后，仓库脚本按规则 fallback 到 `xcrun xctest`，最终 `81 tests, 0 failures`
- `git diff --check`

六、风险与后续

剩余风险：未做真实 UI 手工点击截图验证；当前证据覆盖 build、model 行为、错误映射和真实 CoreBridge get-file 闭环。工作区原本已有未由我产生的 `.codex/task-loop-runs/20260505_225703/summary.json` 与 `tasks/prompts/_shared/progress.json` 修改，我没有触碰或回滚。

建议下一个任务：继续 `2-1/task-24`，覆盖同页剩余的 `C1-15 list_tree_json` 页面功能点。  

