一、执行结论

已完成。上一次验收的阻塞项已修复：`recover_on_startup` 现在在 Swift 生产路径中通过 `Task.detached(priority: .userInitiated)` 调用，符合 Core API SSOT。

二、执行范围

单任务：`2-1/task-28`

本次 repair 只修改了：
- [StartupRecoveryModels.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/StartupRecoveryModels.swift:30)

说明：工作树里仍有上一轮 task-loop 已落地的相关改动和日志，我没有回滚或改写它们。

三、完成情况

核对清单：
- 只处理 S1-10 + C1-16：通过。
- 已重读页面规格、MVP control map、Core API、C1-16 能力规格：通过。
- 只修复 C1-16 CoreBridge 调用规范：通过。
- 未实现相邻页面或相邻 Core 能力：通过。

完成标准：
- S1-10 的 C1-16 恢复功能可触发：通过，`recoverMainOpeningResidue` 仍在打开 repo 前调用恢复。
- 无 mock-only / 静态伪造：通过，生产默认走 `CoreBridge()`；测试替身仅在 XCTest 内。
- 未触碰 control map 之外能力：通过。
- Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 无输出。

四、工程质量

代码结构保持最小改动，没有新增抽象、依赖或文档漂移。错误仍由原 Core 调用抛出并沿现有 UI error mapping 处理；`RecoveryReportSnapshot` 转换逻辑未变。未新增 Rust public API，所以 rustdoc 不适用。没有占位、硬编码通过态、mock-only wiring 或一次性脚本化实现。

五、验证情况

已运行并通过：
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`
- `./dev test macos`：标准 `xcodebuild test` 被本地 `testmanagerd` sandbox 限制阻断后，脚本按仓库规则回退 `xcrun xctest`，107 tests，0 failures
- `git diff --check -- apps/macos/AreaMatrix/Models/StartupRecoveryModels.swift apps/macos/AreaMatrix apps/macos/AreaMatrixTests apps/macos/AreaMatrix.xcodeproj/project.pbxproj`：通过
- `StartupRecoveryModels.swift` 行数检查：59 行
- `core/src/**` 禁止触碰检查：无 diff

无法运行：
- `swiftlint --strict`：本机未找到 `swiftlint`
- `swiftformat --lint .`：本机未找到 `swiftformat`

六、风险与后续

剩余风险：本地未覆盖 SwiftLint/SwiftFormat 和远端 CI；UI 真机交互未手工点测。另有一个 `/tmp/areamatrix-2-1-task-28-verify-prompt.txt` 临时文件清理命令被本地策略拒绝，不在仓库内，不影响 task 范围。

建议下一个任务：进入 `2-1/task-29` page integration verify，检查 S1-10 的 C1-03、C1-15、C1-16 整页闭环覆盖。

