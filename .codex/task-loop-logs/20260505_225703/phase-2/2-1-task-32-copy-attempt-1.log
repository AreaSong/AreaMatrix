一、执行结论：已完成。

二、执行范围：单任务 `2-1/task-32`，仅处理 `S1-11 + C1-21 error-mapping`。修改/新增文件：
- `apps/macos/AreaMatrix/App/AreaMatrixApp.swift`
- `apps/macos/AreaMatrix/Models/OnboardingInitializationProgress.swift`
- `apps/macos/AreaMatrix/Models/RepositoryErrorPresentation.swift`
- `apps/macos/AreaMatrix/Views/MainWindow.swift`
- `apps/macos/AreaMatrix/Views/Main/RepositoryErrorView.swift`
- `apps/macos/AreaMatrixTests/MainRepoErrorMappingTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

三、完成情况：
- 已读取 task、共享规则、切片规则、工程质量规则、编码规范、repo-local skill、依赖图、phase manifest 与全部 Exact Docs。
- S1-11 仅接入 C1-21 的错误映射展示与主操作分流。
- `CoreErrorMappingSnapshot.kind` 映射到用户可见标题、说明、主操作和技术细节展示；未用字符串 `contains` 做错误分类。
- DB/config/repo-not-initialized 类错误可从 S1-11 打开现有 DB repair confirmation 路由，但没有实现实际 repair 能力。
- 未实现 C1-01、C1-19，也未触碰相邻页面。
- Forbidden Touches：未触碰 `core/src/**`。

四、工程质量：
- 新增 presentation model 拆出错误展示逻辑，View 只负责渲染和动作分派。
- 错误路径保留 raw context 的 technical details，并继续提示用户文件未被删除。
- 新增测试覆盖 missing path、iCloud placeholder、DB/config repair、configured repo open failure 映射、repair 路由。
- 未新增依赖；未引入 mock-only 产品实现、硬编码通过态或一次性脚本。

五、验证情况：
- `git diff --check -- <changed files>`：通过。
- `xcodebuild build -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`：通过。
- `./dev test macos`：通过。标准 `xcodebuild test` 被本地 `testmanagerd` sandbox restriction 拦截，脚本按仓库 validation driver 规则复用已构建 XCTest bundle；`xcrun xctest` 执行 121 tests，0 failures。

六、风险与后续：
- 剩余风险：未做人工 UI 截图/真机交互验收；当前证据覆盖 build、路由和单元测试。
- 回滚思路：回退以上 app/test/project 文件即可，不涉及 Core、DB migration 或用户文件。
- 建议下一个任务：进入 `2-1/task-33` 或对应 S1-11 page integration verify，检查同页 C1-01、C1-19、C1-21 的整页闭环覆盖。

