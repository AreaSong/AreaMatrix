一、执行结论

已完成。S1-11 整页集成补齐了缺失的诊断导出与 Reveal 入口，并保留已有 C1-01 / C1-19 / C1-21 真实 CoreBridge 闭环。

二、执行范围

单任务：`2-1/task-33`

修改文件：
- `apps/macos/AreaMatrix/Models/AppShellModel.swift`
- `apps/macos/AreaMatrix/Models/DiagnosticsSnapshotState.swift`
- `apps/macos/AreaMatrix/Models/MainRepoErrorActions.swift`
- `apps/macos/AreaMatrix/Views/Main/RepositoryErrorView.swift`
- `apps/macos/AreaMatrix/Views/MainWindow.swift`
- `apps/macos/AreaMatrixTests/AreaMatrixShellTestSupport.swift`
- `apps/macos/AreaMatrixTests/AreaMatrixShellTests.swift`
- `apps/macos/AreaMatrixTests/MainRepoErrorMappingTests.swift`
- `apps/macos/AreaMatrix.xcodeproj/project.pbxproj`

未触碰：`core/src/**`、`core/area_matrix.udl`。

三、完成情况

核对清单：
- page-feature task 覆盖已核验：C1-01 retry/reconnect、C1-19 external removed sync、C1-21 error mapping 都有现有真实 CoreBridge wiring 和测试。
- 页面闭环已补齐：错误页现在有主恢复动作、选择其他资料库、外部删除确认、诊断导出、Reveal last known folder。
- mock / fixture / 硬编码通过态：未新增；测试 fixture 仅用于 XCTest。
- control map 之外功能：未新增。

完成标准：
- S1-11 可按 page spec/control map 验收。
- 页面声明的 C1-01、C1-19、C1-21 均接入真实 CoreBridge；诊断导出使用 `create_diagnostics_snapshot`，只作为 S1-11 页面规格入口补齐。

四、工程质量

代码结构保持在 macOS 层，新增 `MainRepoErrorActions.swift` 承载 S1-11 错误页动作，避免继续撑大现有 500 行文件。诊断导出必须先进入隐私确认态，文案明确不包含用户文件内容、不自动上传、路径和用户名脱敏。Reveal 仅调用 Finder platform service，不删除、移动、覆盖用户文件。

五、验证情况

已运行：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 构建完成，但测试 runner 被本机 sandbox 阻断：`com.apple.testmanagerd.control ... Sandbox restriction`。
- `./dev test macos`
  - PASS。按 repo-local validation driver 复用同一 XCTest bundle 执行，`128 tests, 0 failures`。

六、风险与后续

剩余风险：未做人工 UI 视觉走查；本轮完成了 model/view wiring 和 XCTest 证据。当前工作树中 `.codex/task-loop-runs/20260505_225703/summary.json` 与 `tasks/prompts/_shared/progress.json` 在我开始前已是 dirty，本轮未处理。建议下一个任务继续进入 `2-1/task-34`。  

