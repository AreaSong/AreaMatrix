一、执行结论

已完成 `2-1/task-25` repair retry。上次阻塞的 S1-09 当前列表错误态已补齐 `Collect Diagnostics...`，并接入真实 `CoreBridge.createDiagnosticsSnapshot(repoPath:)`，不是空按钮或 mock-only 闭环。

二、执行范围

本次 repair 直接改动：

- [MainFileListModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/MainFileListModel.swift)
- [MainRepositoryContentView.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Views/Main/MainRepositoryContentView.swift)
- [MainListDiagnosticsTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/MainListDiagnosticsTests.swift)
- [project.pbxproj](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix.xcodeproj/project.pbxproj)

当前工作树还保留了上次 attempt 的 S1-09 相关改动与 task-loop evidence，我没有回滚它们。

三、完成情况

- page-feature 覆盖：C1-11 / C1-12 / C1-15 仍经 `CoreBridge` 真实接入。
- 错误态闭环：当前 List 查询失败保留 Tree/Toolbar，错误面板现在同时提供 `Retry`、`Collect Diagnostics...` 和 technical details。
- 诊断闭环：收集中、收集成功、收集失败映射都有状态；失败会通过 `CoreErrorMapping` inline 展示。
- Control map：未新增 S1-09 浏览/列表/详情/树之外的产品功能；`Collect Diagnostics...` 是 page spec 明确要求的错误态入口。
- Forbidden Touches：未触碰 `core/src/**`，`git diff --name-only -- core/src` 无输出。

四、工程质量

结构清晰：诊断状态放在 `MainFileListModel`，视图只展示和触发动作；Core 调用仍走 `CoreBridge` 注入协议。没有新增依赖、没有 Rust public API、没有 `try!` / 强制解包 / mock-only 生产路径。新增测试只使用 XCTest 替身验证成功、失败、重试清理三个关键路径。

五、验证情况

已运行：

- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 失败：退出 65；构建完成后被本地 sandbox 阻断 `com.apple.testmanagerd.control`。
- `./dev test macos`
  - 通过：fallback 直跑 XCTest，`89 tests passed`，包含新增 `MainListDiagnosticsTests` 3 项。
- `xcodebuild -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' build CODE_SIGNING_ALLOWED=NO`
  - 通过：`BUILD SUCCEEDED`。
- `git diff --check`
  - 通过。
- `swiftlint --strict` / `swiftformat --lint .`
  - 未运行：当前环境缺少 `swiftlint`、`swiftformat` 命令。

六、风险与后续

剩余风险：原始 `xcodebuild test` 仍受当前执行环境 sandbox 限制，需在非受限本机或 CI 再跑一次原生命令。回滚方式是撤销上述 macOS app/test/project 文件改动；不涉及用户文件删除、DB migration 或 `core/src/**`。

建议下一个任务：进入 `2-1/task-25` verify-ready 只读验收，重点复核 S1-09 错误态和 diagnostics 证据。

