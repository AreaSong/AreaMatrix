一、执行结论

已完成。S1-10 的普通启动入口现在也会在 `mainLoading` 阶段刷新 C1-03 scan session 与 C1-15 Tree 状态，不再只等最终 repository open。

二、执行范围

单任务：`2-1/task-29`

修改文件：
- [AppShellModel.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/AppShellModel.swift)
- [OnboardingInitializationProgress.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrix/Models/OnboardingInitializationProgress.swift)
- [MainLoadingAdoptExistingTests.swift](/Users/as/Ai-Project/project/AreaMatrix/apps/macos/AreaMatrixTests/MainLoadingAdoptExistingTests.swift)

三、完成情况

- page-feature tasks 覆盖确认：C1-03、C1-15、C1-16 均已有 CoreBridge 路径和测试证据。
- 页面闭环补齐：已保存 repo 的启动入口现在显示 Tree loading，并在 open 仍运行时读取 latest scan session 与 Tree。
- mock / fixture / 硬编码：生产代码未新增 mock-only 或硬编码通过态；新增 fixture 只在 XCTest 中用于行为验证。
- Forbidden Touches：未触碰 `core/src/**`。

四、工程质量

代码复用现有 `refreshMainLoadingState`，只把其可见性从 `private` 调整为模块内方法，避免复制一套并发刷新逻辑。错误处理仍复用现有 Core error mapping；取消 token 继续防止 late open result 覆盖用户取消后的 route。未新增依赖，未改 UDL/Core API，未写用户文件。

五、验证情况

已运行：
- `xcodebuild test -project apps/macos/AreaMatrix.xcodeproj -scheme AreaMatrix -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO`
  - 第一次暴露编译问题：`refreshMainLoadingState` private 可见性，已修复。
  - 修复后再次运行：编译/链接通过，但测试 runner 被本地沙箱阻断：`com.apple.testmanagerd.control ... Sandbox restriction`。
- `./dev test macos`
  - 按仓库策略在同类 testmanagerd 沙箱阻断后复用已构建 XCTest bundle。
  - `xcrun xctest` 通过：108 tests, 0 failures。
- `git diff --check -- ...`
  - 通过，无 whitespace 问题。

六、风险与后续

剩余风险：标准 `xcodebuild test` 在当前沙箱无法直接以 0 退出，需要在非沙箱 macOS/Xcode 环境或 CI 再跑一次标准命令。建议下一个任务进入 `2-1/task-30`，继续 S1-11 main-repo-error 页面闭环。

