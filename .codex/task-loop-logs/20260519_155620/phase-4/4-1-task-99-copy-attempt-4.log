一、执行结论

已完成。

二、执行范围

单任务：`4-1/task-99`，只保留 `apps/macos/AreaMatrix/**` 与 `apps/macos/AreaMatrixTests/**` 范围内的实现/测试改动。

主要修改：
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentNoteDrafts.swift`
- `apps/macos/AreaMatrix/Views/Main/SearchFiltersPopover.swift`
- `apps/macos/AreaMatrix/Models/MainListVisibleFileFiltering.swift`
- `apps/macos/AreaMatrix/Views/Main/MainRepositoryContentView.swift`
- `apps/macos/AreaMatrix/Models/MainFileListModel.swift`
- `apps/macos/AreaMatrixTests/CoreBridgeRepositoryTests.swift`
- `apps/macos/AreaMatrixTests/MainRepoExternalRemovalTests.swift`
- `apps/macos/AreaMatrixTests/MainWindowIntegrationVerifyTests.swift`

三、完成情况

核对清单：
- 只处理 S2-02 + C2-02：通过。已移除上次失败指出的 S2-08 route/sheet 越界实现。
- 已读取页面规格、control map、Core API、C2-02 能力规格：通过。
- 只接入 C2-02 搜索过滤状态、facet、错误与 CoreBridge 闭环：通过。
- 未实现相邻页面/能力：通过。`rg "S2-08|TagFilterSheetRouteView|tagFilterSheet|openTagFilterSheet|Choose tags"` 无残留。

完成标准：
- S2-02 中 C2-02 过滤器、chips、reset、facet loading/error、Smart List draft 语义可触发：通过。
- 生产路径未使用 mock/fixture/static state 伪造 Core 闭环：通过。
- 未触碰 control map 之外能力：通过。
- Forbidden Touches：未触碰，`git diff -- core/src core/area_matrix.udl` 无输出。

四、工程质量

代码结构保持在现有 SwiftUI/MainList 模式内，没有新增依赖。新增 `SearchFilterStateRouting` 让普通 search state 与 Smart List draft state 的写入边界可测试，banner chips 已改为同一套 draft-aware binding。

文件长度复查通过：本次相关生产/测试文件均低于 500 行。没有新增 Rust public API，无 rustdoc 需求。未引入占位、硬编码通过态、mock-only wiring 或一次性脚本实现。

五、验证情况

已运行并通过：

```bash
./dev check task 4-1/task-99
```

结果：
- prompt doctor：OK
- `git diff --check`：OK
- macOS `xcodebuild ... build CODE_SIGNING_ALLOWED=NO`：`BUILD SUCCEEDED`

补充核验：
- S2-08 越界符号扫描无残留。
- `core/src/**` 与 `core/area_matrix.udl` 无 diff。
- 未运行 `./dev test macos`，因为当前 task manifest 的验证上限只有 `./dev check task 4-1/task-99`，本次未扩大门禁。

六、风险与后续

剩余风险：新增/调整的 XCTest 作为回归证据保留，但未在本 task 验证上限内单独执行 test target。建议下一步进入 `4-1/task-99` verify-ready 验收；若通过，再继续 `4-1/task-100`。

