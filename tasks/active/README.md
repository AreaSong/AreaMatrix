# Active Lightweight Tasks

This directory holds current lightweight tasks that can be indexed by future
`./dev tasks` commands.

Task directories use:

```text
<number>.<slug>/
```

Each task directory must contain `task.yaml`, `task.md`, `verify.md`, and
`evidence.md`. See [../README.md](../README.md) for the protocol.
