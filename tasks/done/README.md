# Completed Lightweight Tasks

This directory archives completed lightweight tasks by year.

Completed task directories keep their original number and slug:

```text
YYYY/<number>.<slug>/
```

Future `./dev tasks show <number>` should search this archive after checking
`tasks/active/`.
