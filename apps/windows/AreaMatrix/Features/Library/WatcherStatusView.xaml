<UserControl
    x:Class="AreaMatrix.Features.Library.WatcherStatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock
                Text="File watcher status"
                Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock
                x:Name="WatcherRouteTextBlock"
                TextWrapping="Wrap" />
        </StackPanel>

        <Border
            Grid.Row="1"
            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="16">
            <TextBlock
                Text="Manual rescan must open the rescan confirmation flow before scanning."
                TextWrapping="Wrap" />
        </Border>

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="CloseWatcherStatusButton"
                Grid.Column="1"
                Content="Close"
                Click="CloseWatcherStatusButton_Click" />
        </Grid>
    </Grid>
</UserControl>
