# Closeout

本命令供兼容发现；Agent 须静默执行 skill `areamatrix-closeout`，勿要求用户输入本斜杠。

按该 skill 完成：确认改动归属、按改动面选最小验证、doc-sync 漂移检查、`.cursor/plans/` 更新与归档、residual / 能力 canvas 快照同步、30 秒 AAR（过录入门槛则录入对应 owner skill）。
