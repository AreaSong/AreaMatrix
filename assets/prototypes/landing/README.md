# Landing Prototype

`index.html` 是 AreaMatrix landing / welcome 页面视觉原型。它是展示材料，不是产品、架构、API 或 UX 源事实。

## Files

- `index.html`：可直接在浏览器中打开的单文件页面。
- `tools/`：早期迭代时使用的一次性检查、修补和优化脚本。

## Notes

- 页面中的品牌图形引用 `../../brand/final/`。
- `tools/*.js` 默认从本目录执行，例如 `node tools/check_braces.js`。
