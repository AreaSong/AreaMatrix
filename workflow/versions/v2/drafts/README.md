# v2 Drafts

Drafts are review artifacts generated from v2 changes.

```bash
./dev changes generate
./dev changes generate --write
```

They do not enter `tasks/prompts/**`, do not update `progress.json`, and do not
start `./task-loop`.
