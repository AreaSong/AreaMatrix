version: 1
updated: "2026-07-21"
scope: global
authority: index-only
source_of_truth:
  product: docs/
  release_evidence: workflow/versions/<version>/evidence/
  closeout: workflow/versions/<version>/closeout/
  task_state: tasks/active/ and tasks/done/
statuses:
  - open
  - blocked-external
  - blocked-decision
  - mixed-blocked
  - deferred
  - reference-only
  - template-only
  - accepted-exception
  - closed
items:
  - id: global-product-restore-file-contract
    status: closed
    type: product-contract-gap
    title: Public restore_file contract removed
    source: docs/api/core-api.md
    supporting_sources:
      - core/area_matrix.udl
      - core/src/api/history.rs
      - apps/macos/AreaMatrix/Bridge/CoreBridge.swift
    owner: core-storage
    current_impact: none
    executable_task: false
    promotion_required: false
    close_condition: Completed. restore_file was removed from the public UDL, Rust facade, generated bindings, Bridge capability inventory, and Core API; supported recovery remains token-based Undo/Redo only.
    validation:
      - cargo fmt --all -- --check
      - cargo clippy --all-targets --all-features -- -D warnings
      - cargo test --workspace
      - ./dev bindings verify
      - macOS build and file-safety acceptance evidence
    notes: Closed after public-contract removal. This is not an active restore implementation task, and internal restore_file_state remains part of the supported Undo/Redo inverse.
  - id: global-product-metadata-reader-write-flags
    status: closed
    type: file-safety-gap
    title: Read-only repository metadata reader no longer has writable fallback
    source: apps/macos/AreaMatrix/Bridge/CoreBridgeUnavailableState.swift
    supporting_sources:
      - docs/architecture/macos-frontend-architecture.md
      - docs/api/core-api.md
    owner: macos-bridge
    current_impact: none
    executable_task: false
    promotion_required: false
    close_condition: Completed. The reader uses SQLITE_OPEN_READONLY for live WAL databases and an immutable read-only URI for clean databases without sidecars; missing and corrupt databases fail without creating files or modifying index.db or its WAL.
    validation:
      - macOS metadata reader unit and integration tests
      - xcodebuild macOS build and test
      - file-safety acceptance evidence showing no DB creation or mutation on reads
    notes: Closed after read-only open-mode and negative-path verification. Existing SQLite shared-memory lock coordination may update index.db-shm during a read-only WAL read; the reader does not create sidecars or modify index.db or index.db-wal.
  - id: global-product-soft-delete-retention
    status: closed
    type: product-contract-gap
    title: Soft-delete 30-day retention cleanup is not implemented
    source: docs/adr/0003-source-of-truth-strategy.md
    supporting_sources:
      - docs/adr/0006-icloud-support.md
      - core/src/db/sync.rs
      - core/src/recovery.rs
      - core/src/db/delete.rs
    owner: core-storage
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Either implement the documented retention cleanup for soft-deleted rows with file-safety tests, or amend ADR-0003/ADR-0006 to remove the 30-day retention promise.
    validation:
      - cargo test --test recover_on_startup_implementation
      - ADR-0003/0006 and core-api.md updated for purge-on-startup metadata-only retention
    notes: Closed 2026-07-21. recover_on_startup purges files.status=deleted rows with deleted_at older than 30 days; user source files and Trash are not hard-deleted.
  - id: global-product-ui-localization
    status: closed
    type: product-contract-gap
    title: UI string localization pipeline from ADR-0008 is not implemented
    source: docs/adr/0008-naming-and-i18n.md
    supporting_sources:
      - apps/macos/AreaMatrix/Localizations/en.lproj/Localizable.strings
      - apps/macos/AreaMatrix/Features/Settings/GeneralSettingsView.swift
      - apps/macos/AreaMatrix/Features/Onboarding/WelcomeStepView.swift
    owner: macos-frontend
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Either ship the .lproj/Localizable.strings pipeline for UI copy, or amend ADR-0008 to scope localization to classifier display names only.
    validation:
      - xcodebuild AreaMatrix succeeded with Localizable.strings in app bundle
      - Settings/Onboarding key surfaces use String(localized:)
    notes: Closed 2026-07-21. Pipeline landed for en/zh-Hans; zh-Hant mirrors en. Remaining surfaces may localize progressively.
  - id: global-docs-core-module-doc-coverage
    status: closed
    type: product-doc-marker
    title: Module-level docs exist for only 5 of 50 core modules
    source: docs/governance/governance-register.yaml
    supporting_sources:
      - docs/architecture/core-internal-architecture.md
      - docs/modules/repo-init.md
      - docs/modules/semantic-search.md
    owner: doc-sync
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Either write module docs for the high-risk undocumented modules (storage adjacency, sync, recovery already covered by architecture docs), or accept API-contract-level coverage as the permanent documentation depth and record that decision in docs/architecture/core-internal-architecture.md.
    validation:
      - High-risk module docs added under docs/modules/*
      - governance-register authority_docs updated
      - core-internal-architecture.md coverage strategy recorded
    notes: Closed 2026-07-21. High-risk subset documented; remaining modules stay on core-api.md per architecture coverage strategy.
  - id: global-governance-ios-bindings-verify-gap
    status: closed
    type: file-safety-gap
    title: Tracked iOS FFI artifacts have no binding-drift verify fallback
    source: docs/governance/governance-register.yaml
    supporting_sources:
      - apps/ios/Carea_matrixFFI/area_matrixFFI.h
      - apps/ios/Carea_matrixFFI/module.modulemap
      - scripts/dev_tools/build.py
    owner: macos-bridge
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Extend bindings verify (or an equivalent governance check) to cover apps/ios generated artifacts, or stop tracking generated iOS artifacts in git.
    validation:
      - python3 -m unittest scripts.dev_tools.test_build_tools (iOS subset cases)
      - ./dev bindings verify runs macOS full + iOS subset when apps/ios/Carea_matrixFFI exists
    notes: Closed 2026-07-21. iOS subset symbol checks added; no full byte-identical header requirement.
  - id: global-ai-classification-call-log-gate
    status: closed
    type: product-contract-gap
    title: suggest_category_with_ai remote path lacks pre-call log gate required by Core API
    source: docs/api/core-api.md
    supporting_sources:
      - core/src/ai_classification_suggestion/implementation.rs
      - core/src/ai_classification_suggestion/call_log.rs
    owner: core-storage
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Add a pre-remote call-log gate to suggest_category_with_ai matching summary/tags, with privacy and failure-recovery tests proving no remote send when the gate fails.
    validation:
      - cargo test --test ai_classification_suggestion_validation
      - cargo test --test ai_classification_suggestion_failure_recovery
    notes: Closed 2026-07-21. ensure_classification_call_log_gate runs after route selection and before execute_suggestion.
  - id: global-ai-semantic-search-remote-route
    status: closed
    type: product-contract-gap
    title: Semantic search remote route and RateLimited/Timeout fallbacks are not implemented
    source: docs/api/core-api.md
    supporting_sources:
      - core/src/semantic_search/implementation.rs
      - core/src/semantic_search/executor.rs
    owner: core-storage
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Either ship remote semantic search with rate-limit/timeout mapping and contract tests, or amend Core API / UDL to mark remote semantic route as unsupported until a later version.
    validation:
      - cargo test --test semantic_search_implementation
      - cargo test --test semantic_search_failure_recovery
      - cargo test --test ai_fallback_implementation
    notes: Closed 2026-07-21. Remote runtime wired via AREAMATRIX_AI_SEMANTIC_REMOTE_RUNTIME with RateLimited/Timeout fallbacks and call-log gate.
  - id: global-ref-areaflow
    status: reference-only
    type: historical-reference
    title: AreaFlow vision reference
    source: workflow/references/areaflow-vision.md
    owner: workflow-planning
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Keep marked as non-current reference unless AreaFlow is separately chartered as an independent project.
    notes: Does not define current AreaMatrix product scope, active backlog, or task-loop work.
  - id: global-template-vtemplate
    status: template-only
    type: template-reference
    title: v-template blocked-by-design artifacts
    source: workflow/versions/v-template/README.md
    owner: workflow-planning
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Remains template-only; real versions must be created separately from discussion gate.
    notes: apply --write is intentionally blocked for v-template.
  - id: global-ref-closed-backlog-packages
    status: reference-only
    type: backlog-reference
    title: Closed backlog prompt packages
    source: tasks/backlog/README.md
    owner: workflow-planning
    current_impact: none
    executable_task: false
    promotion_required: true
    close_condition: Keep closed packages read-only unless a future planning gate explicitly re-slices them.
    notes: Current backlog prompt package count is 5 closed, 0 open.
  - id: global-marker-product-doc-status-words
    status: reference-only
    type: product-doc-marker
    title: Product docs use unfinished/pending/blocked as product behavior states
    source: docs/product/user-stories.md
    supporting_sources:
      - docs/product/prd.md
      - docs/api/core-api.md
      - workflow/versions/v1-mvp/residuals/source-doc-markers.md
    owner: doc-sync
    current_impact: none
    executable_task: false
    promotion_required: false
    close_condition: Product docs avoid task-like checkbox ambiguity while retaining product behavior semantics.
    notes: Examples include unfinished import recovery and iCloud downloading states; these are product behavior, not task state.
version_residuals:
  - version: v1-mvp
    source: workflow/versions/v1-mvp/residuals/residuals.yaml
    status: mixed-blocked
    status_breakdown:
      - blocked-external
      - blocked-decision
      - deferred
      - accepted-exception
    summary: V1 distribution remains uncleared by distribution evidence and decisions; technical queue is complete.
  - version: v2
    source: workflow/versions/v2/residuals/residuals.yaml
    status: mixed-blocked
    status_breakdown:
      - open
      - deferred
    summary: V2 governance authoring remains bounded by independent review, execution authorization, and remote merge-control evidence.
